# 휴대폰 본인인증 도입 기준

## 목적

Ticketground의 본인인증은 단순히 회원가입 때 사용자를 확인하기 위한 기능이 아니다. 암표상이 계정 정보, 휴대폰 번호, 이메일, 이름을 변경해 티켓 발권 대상자를 바꾸는 문제를 막기 위해 필요하다.

핵심 원칙은 다음과 같다.

```text
계정 프로필 변경
!= 티켓 소유자 변경
!= 현장 발권 대상자 변경
```

사용자가 휴대폰 번호나 이름을 변경할 수는 있어야 한다. 다만 그 변경이 이미 예매된 티켓의 권리자 변경으로 처리되면 안 된다. 티켓 권리자는 예매 시점의 본인확인 결과에 고정하고, 공식 양도 또는 공식 재판매가 완료된 경우에만 바꾼다.

## 결론

휴대폰 본인인증은 단순 SMS 인증번호 서비스가 아니라, 방송미디어통신위원회 지정 본인확인기관의 본인확인 서비스를 사용해야 한다.

도입 조건은 다음이다.

```text
본인확인 결과로 CI/DI 또는 그에 준하는 동일인 식별값을 받을 수 있어야 한다.
```

실무 후보는 다음과 같다.

- NICE평가정보 본인확인
- KCB / OK-Name 본인확인
- SCI평가정보 본인확인
- 통신 3사 PASS 기반 휴대폰 본인확인
- 본인확인기관과 계약된 PG사 또는 인증 대행사 연동

Ticketground 초기 MVP에는 NICE 또는 KCB 계열 본인확인 연동이 가장 현실적이다. 이유는 단순히 휴대폰 번호 소유 여부를 확인하는 것이 아니라, 재인증 시 기존 티켓 권리자와 같은 사람인지 비교해야 하기 때문이다.

## SMS OTP만으로는 부족한 이유

SMS OTP는 다음만 증명한다.

```text
이 사용자가 지금 이 휴대폰 번호를 사용할 수 있다.
```

하지만 Ticketground에 필요한 것은 다음이다.

```text
이 사용자가 예매 당시 티켓 권리자와 같은 사람이다.
```

암표상은 구매자에게 이름, 휴대폰 번호, 이메일을 받아 계정 정보를 바꿀 수 있다. 이때 SMS OTP만 쓰면 새 번호를 사용할 수 있다는 사실만 확인할 뿐, 예매 당시 사람과 같은 사람인지 확인하지 못한다.

따라서 Ticketground는 SMS OTP가 아니라 실명 기반 본인확인 결과값을 받아야 한다.

## CI/DI 사용 기준

본인확인기관은 본인확인 결과로 이름, 생년월일 같은 개인정보뿐 아니라 온라인 동일인 식별에 사용하는 CI 또는 DI 계열 정보를 제공할 수 있다.

- CI: 여러 기관 사이에서 동일인을 식별하기 위한 연계정보
- DI: 특정 이용기관 안에서 동일인을 식별하기 위한 중복가입확인정보

Ticketground는 원문 CI/DI를 그대로 저장하지 않는다. 서버 비밀키로 HMAC 처리한 값만 저장한다.

```text
identityKeyHash = HMAC(serverSecret, provider + ":" + ciOrDi)
```

이 값은 다음 두 위치에 저장한다.

```text
users.identityKeyHash
ticketEntitlements.holderIdentityKeyHash
```

계정의 현재 이름과 휴대폰 번호는 바뀔 수 있지만, `ticketEntitlements.holderIdentityKeyHash`는 공식 양도 또는 공식 재판매 완료 전에는 바뀌면 안 된다.

## 예매 시점 플로우

```text
1. 사용자가 예매를 진행한다.
2. 결제 전 또는 고위험 티켓 구매 전 본인확인을 요구한다.
3. 본인확인기관에서 CI/DI 계열 동일인 식별값을 받는다.
4. 서버에서 identityKeyHash를 만든다.
5. 결제 완료 시 티켓 권한에 holderIdentityKeyHash를 고정한다.
```

저장 예시는 다음과 같다.

```text
ticketEntitlements
- id
- ticketId
- holderUserId
- holderIdentityKeyHash
- holderNameSnapshot
- holderPhoneLast4Snapshot
- purchasedAt
- identityLockedAt
```

`holderNameSnapshot`과 `holderPhoneLast4Snapshot`은 현장 확인과 고객센터 대응용이다. 동일인 판정 기준은 이름/번호 문자열이 아니라 `holderIdentityKeyHash`다.

## 이름 또는 휴대폰 번호 변경 플로우

사용자가 이름 또는 휴대폰 번호를 변경하려면 다시 본인확인을 한다.

```text
1. 사용자가 새 이름 또는 새 휴대폰 번호로 변경을 요청한다.
2. 본인확인기관으로 재인증한다.
3. 새 identityKeyHash를 계산한다.
4. 기존 users.identityKeyHash 및 티켓의 holderIdentityKeyHash와 비교한다.
```

판정은 다음과 같다.

```text
새 identityKeyHash == 기존 holderIdentityKeyHash
→ 같은 사람
→ 계정 프로필 변경 허용
→ 티켓 권리 유지

새 identityKeyHash != 기존 holderIdentityKeyHash
→ 다른 사람 가능성
→ 티켓 권리 이전 불가
→ 공식 양도/재판매 또는 고객센터 심사로 이동

identityKeyHash 비교 불가
→ 자동 승인 금지
→ 수동 심사
```

## 이름과 휴대폰 번호가 동시에 바뀌는 경우

이름과 휴대폰 번호가 동시에 바뀌면 기본값은 고위험이다. 개명, 번호 변경, 외국인 이름 표기 변경 같은 정상 사유가 있을 수 있지만, 암표 거래에서도 가장 자주 쓰일 수 있는 패턴이다.

정책은 다음과 같다.

```text
이름만 변경
→ 중간 위험
→ identityKeyHash 동일 시 허용

휴대폰만 변경
→ 중간 위험
→ identityKeyHash 동일 시 허용

이름 + 휴대폰 동시 변경
→ 고위험
→ identityKeyHash 동일해도 공연 임박 시 추가 확인
→ identityKeyHash 다르면 티켓 권리 이전 불가
```

공연 임박 시점에는 더 강하게 본다.

```text
공연 7일 전 이전
→ 같은 identityKeyHash면 승인

공연 7일 전 ~ 24시간 전
→ 같은 identityKeyHash + 기존 기기 또는 결제수단 일부 확인

공연 24시간 이내
→ 같은 identityKeyHash라도 QR/현장 발권 일시 보류 후 추가 확인 가능
```

## 계정 양도형 암표 차단 기준

암표상은 다음 흐름을 만들려고 한다.

```text
암표상 계정으로 예매
→ 구매자 이름과 휴대폰 번호로 계정 정보 변경
→ 구매자가 정상 예매자처럼 현장 발권
```

Ticketground는 이 흐름을 다음 규칙으로 막는다.

```text
계정 정보 변경은 가능
티켓 발권 대상자 변경은 불가능
티켓 소유권 변경은 공식 양도/공식 재판매만 가능
```

즉, 계정 프로필이 구매자 정보로 바뀌어도 티켓 권리의 `holderIdentityKeyHash`가 기존 예매자 값이면 현장 발권 대상자는 바뀌지 않는다.

## 현장 발권 기준

실물 티켓을 발권해야 하는 뮤지컬/공연장은 현재 계정 정보가 아니라 티켓 권한에 고정된 발권 대상자를 기준으로 처리한다.

현장 또는 관리자 화면에는 다음 정보만 내려간다.

```text
예매번호
공연명
좌석
발권 가능 상태
발권 대상자명
휴대폰 뒤 4자리
공식 양도 여부
위험 상태
```

다음 정보는 현장 화면이나 사용자 공개 API에 노출하지 않는다.

```text
CI/DI 원문
identityKeyHash 원문
QR nonce
QR signature
내부 admissionCredential
ownerId
```

## 정상 사용자 예외 처리

정상 사용자가 실제로 휴대폰 번호를 바꾸거나 개명할 수 있으므로 차단만 하면 안 된다. 동일인으로 확인되면 변경을 허용한다.

정상 예시는 다음과 같다.

```text
개명 + 번호 변경
→ 재인증 identityKeyHash 동일
→ 정상 변경
→ 티켓 유지

번호 변경
→ 재인증 identityKeyHash 동일
→ 정상 변경
→ 티켓 유지

기존 번호 사용 불가
→ 새 번호 본인확인 + 기존 기기/결제수단/고객센터 확인
→ 같은 사람으로 확인되면 복구
```

단, 다른 사람의 identityKeyHash가 나오면 정상 번호 변경이 아니라 티켓 권리 이전 시도로 본다.

## 구현 상태값

계정 변경과 티켓 접근 심사를 분리하기 위해 다음 상태값을 둔다.

```text
PHONE_CHANGE_REQUESTED
NAME_CHANGE_REQUESTED
IDENTITY_REVERIFIED_SAME_PERSON
IDENTITY_REVERIFIED_DIFFERENT_PERSON
IDENTITY_REVIEW_REQUIRED
TICKET_ACCESS_REVIEW_REQUIRED
TICKET_ACCESS_RESTORED
```

티켓 권한에는 다음 상태를 둔다.

```text
ACTIVE
TRANSFER_PENDING
RESALE_PENDING
ACCESS_REVIEW_REQUIRED
QR_HOLD
PHYSICAL_ISSUED
ADMITTED
VOIDED
```

## API 설계 초안

```text
POST /api/identity/verify/start
→ 본인확인기관 인증 시작

POST /api/identity/verify/callback
→ 본인확인 결과 수신
→ CI/DI 검증
→ identityKeyHash 생성

POST /api/account/profile-change
→ 이름/휴대폰 변경 요청
→ 재인증 결과와 기존 identityKeyHash 비교

POST /api/tickets/:ticketId/access-review
→ 공연 임박 고위험 변경 시 티켓 접근 심사

POST /api/tickets/:ticketId/official-transfer
→ 공식 양도 완료 시에만 holderIdentityKeyHash 변경
```

## 도입 시 계약 체크리스트

본인확인 업체 또는 대행사를 고를 때 다음을 확인해야 한다.

- 방송미디어통신위원회 지정 본인확인기관 또는 그 위탁 대행사인지
- 휴대폰 본인확인 결과로 CI/DI 또는 동일인 비교 가능한 값을 제공하는지
- 재인증 시 기존 사용자와 동일인 비교가 가능한지
- API 방식 또는 웹 팝업 방식 연동을 제공하는지
- 본인확인 결과정보 저장/이용 범위가 계약과 동의문에 명확히 포함되는지
- 장애 대응 SLA와 운영 지원이 있는지
- 개인정보 처리, 파기, 접근통제, 암호키 관리 기준을 충족하는지

## 사용자 안내 문구

이름 또는 휴대폰 번호 변경 시:

```text
예매 티켓 보호를 위해 본인확인이 필요합니다.
정보 변경은 가능하지만, 이미 예매된 티켓의 발권 대상자는 본인확인 결과가 일치할 때만 유지됩니다.
```

이름과 휴대폰 번호가 동시에 변경된 경우:

```text
이름과 휴대폰 번호가 함께 변경되어 예매 티켓 보호를 위한 추가 확인이 필요합니다.
확인이 완료되기 전까지 입장 QR 및 현장 발권이 일시 제한될 수 있습니다.
```

다른 사람으로 확인된 경우:

```text
본인확인 결과가 예매 당시 정보와 일치하지 않습니다.
티켓 권리 이전은 공식 양도 또는 공식 재판매를 통해서만 가능합니다.
```

## 참고 자료

- KISA 본인확인기관 현황: https://identity.kisa.or.kr/web/main/contents/M010-03
- KISA 본인확인서비스 이용기관 교육자료: https://identity.kisa.or.kr/web/main/bbs/edu_userorg/52
- Ticketground 사업계획서: `사업계획서(260624).md`
- Ticketground 입장 QR 정책 메모: `입장qr 문제점.md`

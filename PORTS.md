# 포트 배정 (Ports)

이 저장소에서 작업하는 모든 에이전트(Claude, Codex 등)는 **페이지 수정/업데이트 작업을 시작하기 전에 반드시 이 파일을 먼저 확인한다.** 새 페이지나 새 포트를 배정받으면 그 즉시 이 파일에 반영해서 항상 최신 상태로 유지한다.

## 현재 배정

| 브랜치 | 용도 | 포트 | 비고 |
| --- | --- | --- | --- |
| `main` | 관리자 콘솔 | **7905** | `ADMIN_PORT=7905`, `/console` |
| `main` | 사용자 페이지 | **5501** | `PORT=5501`, 공개 사이트 |
| `block-book_ticket` | 기관/단체 예매 기능 확인용 | **5502** | |
| `sell_corporation` | 기업 판매자 등록 기능 확인용 | **7906** | |

## 작업 규칙

1. 어떤 페이지든 수정하거나 새로 배포/재기동하기 전에 이 표를 먼저 읽는다.
2. 새 브랜치나 새 기능 확인용으로 서버를 띄워야 하면, 위 표에 없는 새 포트를 정하기 전에 사용자에게 포트를 확인받거나 이미 정해진 포트를 그대로 쓴다.
3. 포트가 새로 정해지거나 바뀌면 이 표를 바로 갱신하고 커밋한다 — 표가 실제 배정과 다르면 그 자체가 버그로 취급한다.
4. main의 관리자(7905)/사용자(5501) 포트는 하나의 Node 프로세스가 함께 띄우는 짝이다. 각 기능 브랜치(`block-book_ticket`, `sell_corporation`)에 적힌 포트는 해당 브랜치 확인용 워크트리를 띄울 때 쓰는 대표 포트다.

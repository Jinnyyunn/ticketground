# TIG 개인정보처리방침 실행 계획서

> Fable 5 Advisor 자문 · 백엔드 코드 전수 조사 기반
>
> 이 문서는 엔지니어링·제품 실행계획이다. 방침에 들어갈 법률 문구 자체와 각 조항의 최종 근거는 법무 검토를 거쳐야 하며, 이 문서가 그 검토를 대신하지 않는다.

지금 `/privacy`와 `/terms`는 각각 세 문장, 다섯 문단짜리 안내문이다. 실제 서비스가 어떤 개인정보를 어디서 얼마나 수집하고 있는지 코드를 직접 읽어 확인한 뒤, 방침을 다시 쓰는 데 필요한 엔지니어링·제품·법무 작업을 순서대로 정리했다.

**현재 상태: 이 계획서의 구현 항목은 아직 하나도 착수되지 않았다.** 아래 `[구현 필요]` 표시는 전부 미착수 상태를 뜻하며, 이 문서의 Stage 0~4와 작업 분류가 착수 순서와 보호 경계 의존성의 기준이다.

---

## 목차

1. [현황 진단](#1-현황-진단)
2. [조사 결과 정정](#2-조사-결과-정정)
3. [핵심 격차 10가지](#3-핵심-격차-10가지)
4. [무엇을 먼저 고칠까](#4-무엇을-먼저-고칠까)
5. [단계별 실행 계획](#5-단계별-실행-계획)
6. [작업 분류](#6-작업-분류)
7. [방침 문서 골격](#7-방침-문서-골격)
8. [추가로 점검할 것](#8-추가로-점검할-것)
9. [열린 질문](#9-열린-질문)

---

## 1. 현황 진단

백엔드 소스를 직접 읽어 확인한, 실제로 존재하는 개인정보 처리 흐름.

> **검증 기준과 실행 제한:** 아래 표는 원문을 일시적으로 처리하는 단계, 영구 저장, 제3자 전달, 공개 노출을 구분한다. 또한 현재 정상화된 간편로그인 영역은 전역 보호 규칙의 적용 대상이다. Google·Kakao·Naver 로그인, 세션, 관련 테스트와 공급자 설정은 이 계획서 때문에 변경하지 않는다. 해당 영역의 보완은 위험과 검증 조건만 기록하고, 사용자가 전역 금지를 명시적으로 철회한 뒤 별도 승인·집중 인증 테스트를 거쳐 착수한다.

| 영역 | 수집·저장 방식 | 평가 |
|---|---|---|
| **계정 로그인**<br>Google·Kakao·Naver | 공급자 식별자와 프로필 이름을 처리한다. Naver는 `name`을 `nickname`보다 우선해 실명을 `users[].name`과 원장 `providerName`에 저장할 수 있다. Google ID 토큰의 이메일은 보통 계정 생성·검증 중 일시 처리하지만 `name` claim이 없으면 원문 이메일을 `users[].name`으로 대체 저장하고 현재 공개 사용자 DTO에도 노출할 수 있다. 공급자 식별자는 안정 ID/해시 형태로 저장한다. | 공급자별 이름·닉네임·이메일 저장/공개와 원장 복제를 고지하고 삭제·정제 필요 · 인증 보호 영역 |
| **본인인증**<br>PortOne · 다날 | 입력 전화번호와 PortOne 응답의 전화번호·CI·DI뿐 아니라 응답에 따라 `identityKey` 또는 `customerId` 원문을 확인 과정에서 일시 처리하고, 선택된 식별자의 `personHash`와 전화 SHA-256 해시·마스킹 값(010-\*\*\*\*-1234)을 DB에 저장한다. 마스킹 값 때문에 후보 공간이 작아 비밀키 없는 해시는 원문 추측에 취약하다. 시작 후 완료하지 않은 `PENDING` 시도도 사용자 ID·전화 해시·마스킹 값을 만료 없이 계속 보유한다. | 모든 식별자 fallback의 HMAC/pepper 전환·보관 결정, 기존 계정 재인증, 미완료 시도 TTL·파기 필요 · `[구현 필요]` |
| **결제·환불**<br>TossPayments | 카드 입력은 위젯이 직접 처리하고 백엔드는 거래키·사용자·티켓·금액·수단·상태를 저장한다. 관리자의 계좌 환불 시 `refundReceiveAccount` 원문을 일시 처리해 Toss에 전달하지만 DB에는 저장하지 않는다. 결제상태 webhook은 현재 서명 검증 없이 최대 8 MB 본문과 caller-selected eventType·paymentKey·status를 받아 원장에 영구 기록한다. | 거래·환불계좌의 목적·전달·보관 구분과 webhook 진위·입력·용량 통제 필요 `[구현 필요]` |
| **기업/단체 대량티켓 신청**<br>`backend/group-booking.js` | 담당자 이름·전화·이메일, 자유서술 `requestNote`, 관리자 `reviewNote`, 사업자등록증을 저장한다. 거절 검토 메모는 원장에도 복제된다. 신규 문서는 비공개 디렉토리에 `0o600` 권한·랜덤 파일명으로 저장하고 DB에는 참조만 기록하지만(#142), 구버전 신청에는 인라인 `businessRegistrationFileUrl`이 남을 수 있다. | 항목별 목적·상태별 보관/파기·원장 정제와 레거시 마이그레이션 필요 `[구현 필요]` |
| **기관 구매자 계정** | 단체 신청 승인 시 담당자 이름·전화·이메일을 별도 `users` 레코드에 복제하고 티켓·결제·신청과 연결한다. 셀프서비스 폐쇄 경로가 없다. | 신청 만료와 연동한 보관·익명화·유료 티켓 처리 필요 `[구현 필요]` |
| **기업 판매자 등록 신청**<br>`backend/seller-applications.js` | 대표자명, 법인 주소·웹사이트·사업자등록번호, 담당자 이름·직함·전화·이메일, 행사·판매조건·수수료·기타 자유서술, 사업자등록증과 포스터, 정산 은행·계좌번호·예금주를 저장하고 관리자 API에 반환한다. 특히 사업자등록증·포스터는 base64 데이터 URL로, 계좌번호·예금주명은 평문으로 공유 JSON DB에 남는다. | 항목별 목적·보관·삭제·legacy 이관을 고지하고 민감 파일·정산정보 최우선 조치 · `[구현 필요]` |
| **기업 판매자 계정·행사 포스터** | 승인 후 `sellerAccounts`에 법인명, 사용자명, 비밀번호 파생값, 신청·판매자 ID를 저장하고 행사·원장과 연결한다. 행사 생성·수정은 최대 16 MB 본문을 버퍼링하고 포스터를 고유 파일로 저장하며, 수정으로 교체된 이전 파일을 지우지 않는다. 발급·비밀번호 변경은 있으나 탈퇴·파기 경로도 없다. | 보유기간·비활성화/삭제·세션 폐기·연결 행사 처리와 포스터 용량·교체 파일 수명주기 필요 `[구현 필요]` |
| **관리자 계정** | `adminAccounts`에 담당 직원·수탁자를 식별할 수 있는 사용자명, 비밀번호 파생값, 역할, IP 허용목록과 활성 상태를 저장한다. 별도로 환경변수 `TIG_ADMIN_USERNAME`·`TIG_ADMIN_PASSWORD`의 항상 활성 `bootstrap-admin`이 DB 계정 비활성화 수명주기를 우회하고, 기존 bootstrap 세션도 만료 전까지 남는다. 안정 관리자 ID·사용자명·IP 규칙은 감사 원장뿐 아니라 단체신청 `reviewedBy`, 판매자신청 검토 로그 `by`, 게이트 세션 `createdBy`에도 복제된다. | bootstrap 포함 퇴직·계약종료 시 계정/세션 폐기·credential 회전, 하위 참조 보관·가명화/파기 필요 · 인증 보호 영역 |
| **예매·재판매·위험 프로필** | 결제 거래에 사용자·티켓·금액·수단·상태를, 재판매 풀에 판매자·구매자 참여와 당첨 이력을, 계정에 잔액·신뢰점수·제재를 저장한다. | 항목별 목적·보관·탈퇴 시 익명화 기준 필요 `[구현 필요]` |
| **입장·신뢰 기기** | `trustedDevices`에 사용자·기기 ID, 기기명, 플랫폼, 검증시각을 저장하고 `admissionCredentials`, `qrIssueLogs`, 티켓의 현재 QR 필드에 사용자·티켓·기기·입장 이력을 연결해 저장한다. 게이트 검증은 위조·이미 사용된 QR을 포함한 모든 시도마다 영구 원장 항목을 남기며 gate별 rate limit·중복 제거·저장 상한이 없다. | 수집 고지, 보관기간, 탈퇴 시 삭제·익명화와 게이트별 원장 남용 통제 필요 · `[구현 필요]` |
| **게이트 오프라인 대기열** | 공유 게이트 브라우저 `localStorage`에 최대 30개의 원문 QR payload(소유자·티켓 ID, nonce, 만료, 서명)를 저장하며 현재 나이 제한·토큰 해제 시 정리가 없다. | 짧은 TTL, 전송 성공·토큰 해제·로그아웃/폐기 시 삭제 필요 `[구현 필요]` |
| **게이트 브라우저 자격증명** | 공유 게이트 브라우저 `localStorage`에 원문 gate bearer token과 label을 만료 메타데이터 없이 저장하고, 서버는 `ACTIVE` 토큰에 만료를 검사하지 않는다. 기기 인계·분실 뒤에도 입장 처리를 계속 승인할 수 있다. | 짧은 서버 만료·회전·즉시 폐기와 로컬 초기화 필요 `[구현 필요]` |
| **관심목록·문의** | `watchlist`, `notificationJobs`에 사용자별 공연 선호·채널을 저장한다. `supportThreads`에는 안정 user ID, 자유서술 제목, 분류, 전체 고객 메시지와 관리자 답변을 지속 저장하고, `operatorAlerts`에는 사용자 ID와 문의 내용 일부를 다시 복제한다. | 문의 상태별 보관·파기, 탈퇴 범위, 알림 취소와 사본 동시 삭제 검증 필요 `[구현 필요]` |
| **공개 상태 API** | 인증 없는 `GET /api/state`가 전체 사용자 안정 ID·이름뿐 아니라 `resalePools[].sellerId` 등 중첩 DTO의 안정 식별자도 반환한다. | 모든 공개 DTO의 사용자 식별자 제거 또는 목적·근거 확정 `[구현 필요]` |
| **비인증 사용자 읽기·변경 API** | 세션이 없으면 `resolveActorId`가 요청의 userId·sellerId·buyerId를 신뢰해 본인인증·구매·재판매·신뢰기기·QR 등 변경 API의 주체로 사용한다. 또한 `TIG_DEMO_*` 플래그는 운영에서도 활성화될 수 있고 caller-selected ID로 세션·티켓·관심목록·전체 문의 스레드를 읽게 한다. 공개 `/api/state`의 사용자 ID와 결합하면 익명 열람·열거가 가능하다. | 운영 demo read/write 무조건 비활성화 또는 인증 principal 강제 필요 `[보호 영역 결정 필요]` |
| **감사 원장** | 해시체인의 최상위 `actorId`와 payload의 buyerId·sellerId·targetUserId 외에도 공급자 실명·닉네임(`providerName`), 문의 제목, 법인·기관명, 자유서술 검토 메모가 평문으로 들어갈 수 있다. | 현재 데이터를 정제하기 전 삭제 예외로 취급 불가 · `[구현 필요]` |
| **자동 수집 IP** | 단체 신청, 판매자·관리자 로그인에서 요청 IP를 남용 방지·접근통제 목적으로 처리한다. 단체 신청 Map은 1시간 주기로 만료 항목을 정리하지만 그 시간 안의 최대 항목 수 제한이 없다. 판매자 로그인 Map은 만료된 IP도 같은 IP가 다시 시도하거나 프로세스가 재시작하기 전까지 남고 5분 창 안의 최대 항목 수도 없으며, 관리자 Map은 5분 간격으로 만료 항목을 정리하지만 최대 크기 제한은 없다. | 세 Map 모두 최대 크기 또는 동등한 bounded limiter와 회전 IP·축출·만료 테스트 필요 `[구현 필요]`; 관리자·단체 `server.js` 변경은 보호 영역 |
| **회원 탈퇴 / 파기** | 삭제·보관기간·익명화 로직이 백엔드 어디에도 없음. 프론트에도 탈퇴 진입점 없음. | **기능 자체가 없음 · `[구현 필요]`** |
| **관리자 접근 통제**<br>`backend/admin-acl.js` | 세션 관리자에는 개인정보 범주별 권한 키(`accounts.manage`, `groupBooking.manage`, `finance.read` 등)를 강제한다. 그러나 운영 필수 `TIG_ADMIN_TOKEN` 요청은 세션·CSRF·역할 권한 검사를 건너뛰어 모든 관리자 데이터와 향후 민감 엔드포인트에 접근할 수 있다. | ACL을 완전한 안전조치로 고지하면 안 됨 · 토큰 제거/범위화·회전 필요 · 인증 보호 영역 |
| **쿠키·브라우저·앱 로컬 저장** | 필수 인증 쿠키로 판매자 안정 ID·CSRF·발급시각이 든 서명 토큰(8시간), 관리자 랜덤 세션 ID·서명(8시간), OAuth 공급자·클라이언트·nonce·발급시각이 든 state(10분), 공급자·안정 user ID·발급시각이 든 일회성 소셜 세션(5분)을 쓴다. 모두 HttpOnly·SameSite=Lax이고 운영 HTTPS에서는 Secure다. 판매자 로그아웃은 브라우저 쿠키만 지워 복사된 stateless 토큰이 8시간 동안 계속 유효하고 비밀번호 변경도 이를 폐기하지 않는다. 만료된 `nativeSessions` DB 레코드는 만료 credential 제시·로그아웃·프로세스 재시작으로 삭제되지 않아 정리 배치가 없으면 DB에 무기한 남고, 서버 메모리의 `adminSessions`는 같은 cookie가 다시 제시되거나 로그아웃·재시작되기 전까지 남을 수 있다. 브라우저 `localStorage` bearer credential은 서버에서 30일 뒤 거부되지만 credential과 안정 user ID 키 자체에는 만료 메타데이터가 없고 만료 오류도 이를 지우지 않아 무기한 잔존할 수 있다. 마지막으로 시작·완료한 소셜 로그인 공급자(`google`·`kakao`·`naver`)도 `localStorage`에 무기한 남아 공유 브라우저의 다음 사용자에게 표시된다. 이 밖에 iOS Keychain의 native credential·안정 user ID, 브라우저 `localStorage`·iOS `UserDefaults`의 영구 Toss `customerKey`, 계정 비구분 취소 이력, 게이트 오프라인 QR이 남는다. | 필수 쿠키별 값·목적·기간·실제 삭제 범위를 고지하고 판매자 세션 폐기, 만료 native session 주기 파기와 브라우저 키 정리, 마지막 로그인 공급자를 포함한 로컬 항목의 계정 범위·TTL·전달·로그아웃/탈퇴/명시적 초기화 필요; 소셜 인증 저장 변경은 보호 영역 |
| **국외 이전** | Google JWKS 요청은 공개 서명키 조회이며 개인정보 이전 근거가 아니다. 실제 Google GIS/네이티브 SDK 흐름, 다른 벤더의 브라우저·서버 통신, 배포·저장 리전은 아직 확정하지 못했다. | 전수 인벤토리 완료 전 고지 확정 불가 · `[조사 필요]` |
| **방침·약관 페이지** | 각각 3문단·5문단짜리 요약문. 항목별 수집내역, 보관기간, 제3자 목록, 담당자 연락처 전부 없음. | **전면 재작성 필요 · `[구현 필요]`** |
| **동의 UI** | 로그인 폼의 동의 문구는 실제 방침에 연결되지 않지만 전역 보호 범위라 이 계획에서 변경하지 않는다. 기업/단체 대량티켓 신청은 동의가 없고, 판매자 신청은 `true`만 저장해 정책 버전 증거가 없다. | 신청 API의 버전 동의 구현 필요 · 로그인은 보호 영역 |

---

## 2. 조사 결과 정정

이 계획서는 두 차례 재검증을 거쳤다. 1차 조사, 2차 Fable 교차검증, 그리고 실제 `origin/main` 코드와의 직접 대조 — 세 번째 단계에서 2차 교차검증 자체의 오류가 드러났다. 최종 결론만 신뢰하지 말고 이 과정을 그대로 남긴다.

> **🔴 2차 검증도 틀렸다 — "System A"는 실제로 존재한다**
> 1차 조사는 사업자등록번호·대표자명·정산 계좌를 저장하는 "개별 판매자 셀프서비스 신청" 시스템이 있다고 보고했다. 2차 Fable 검증은 이를 "코드베이스 어디에도 없다"고 정정했는데, **이 정정이 틀렸다.** `origin/main`의 `backend/seller-applications.js`(`createSellerApplicationBackend`)를 직접 읽어 확인한 결과, 이 시스템은 실제로 존재하며 `bizRegistrationDocDataUrl`(사업자등록증을 base64로 JSON DB에 인라인 저장) 외에 **`settlement.{bankName, accountNumber, accountHolder}` — 정산 계좌번호와 예금주명을 원문으로 저장**한다. `listSellerApplications()`는 이 값을 관리자 API 응답에 그대로 포함해 반환하며, 별도로 걸러내는 DTO가 없다. 개인 예금주명과 은행 계좌번호는 사업자등록증보다 더 민감한 항목이라, 이 계획서의 "최우선 조치 대상"은 아래에서 `seller-applications.js`로 옮겨졌다.

> **✅ group-booking.js의 신규 쓰기 경로는 이미 고쳐져 있었다**
> 2차 검증은 `backend/group-booking.js`가 사업자등록증 파일을 아무 하드닝 없이 저장한다고 결론지었는데, 이는 조사 시점의 스냅샷이 `origin/main`보다 뒤처져 있었기 때문이었다. 실제 `origin/main`의 커밋 #142는 신규 파일을 `fs.writeFile` + `0o600` 권한 + `businessRegistrationDir` 옵션으로 저장하고 DB에는 파일 참조만 기록한다. 그러나 DTO가 구버전 인라인 `businessRegistrationFileUrl`을 계속 지원하므로 **신규 쓰기 하드닝과 별개로 기존 레코드 마이그레이션은 필요하다.**

이 정정을 반영해, 우선순위와 실행 계획은 `seller-applications.js`를 최우선 조치 대상으로 놓고 다시 짰다. 앞으로 이 코드베이스를 다시 조사할 일이 있다면, 로컬 체크아웃이 아니라 `git show origin/main:<path>`로 항상 직접 대조할 것.

---

## 3. 핵심 격차 10가지

1. **회원 탈퇴·파기 기능이 전혀 없다.** 개인정보보호법 제21조의 불필요 개인정보 파기 의무와 제36조의 정보주체 정정·삭제 요구에 대응할 코드 경로가 프론트·백엔드 어디에도 없다. 구체적 적용 범위는 법무 검토가 필요하다. `[구현 필요]`
2. **`seller-applications.js`가 사업자등록증 파일과 정산 계좌번호·예금주명을 하드닝 없이 저장한다.** base64 blob과 원문 계좌번호·예금주명이 공유 JSON DB 안에 그대로 들어있고, 관리자 조회 API가 걸러내지 않고 그대로 반환한다. `group-booking.js`는 신규 쓰기만 #142로 해결됐고 구버전 인라인 문서는 별도 이관 대상이다. `[구현 필요]`
3. **개인정보보호책임자·사업자등록번호 등 필수 고지가 어디에도 없다.** 개인정보보호법 제30조·전자상거래법이 요구하는 최소 요건이다. 값 자체는 콘텐츠지만 표시할 슬롯은 `[구현 필요]`
4. **원장(ledger)과 삭제 요구권이 구조적으로 충돌하고, 현재 payload에도 개인정보가 있다.** 기존 평문 payload를 마이그레이션·재체인하거나 암호화 후 키 파기하는 설계가 먼저다. 단순히 “해시 원장”이라는 이유로 삭제 예외를 적용할 수 없다. `[구현 필요]`
5. **기업/단체 신청 동의가 서버에서 강제되지 않는다.** UI 체크만으로는 API 직접 호출을 막지 못하며 정책 버전과 동의 시각도 남지 않는다. `[구현 필요]`
6. **공개 API가 계정 ID와 이름, 재판매 판매자 ID를 인증 없이 노출한다.** `/api/state`의 최상위 사용자 목록만이 아니라 모든 중첩 DTO를 검사해 안정 사용자 식별자를 제거하거나 공개 목적과 근거를 확정해야 한다. `[구현 필요]`
7. **입장·기기 데이터가 파기 범위에서 빠져 있다.** 신뢰 기기, 입장 자격, QR 발급·게이트 사용 이력과 티켓 QR 필드를 함께 다뤄야 한다. `[구현 필요]`
8. **본인인증 해시가 사전대입 공격에 취약하다.** 신규 인증은 HMAC/pepper로 바꾸고, 원문이 없는 기존 계정은 값을 변환하지 말고 재인증으로 교체해야 한다. 본인인증 모듈은 소셜 간편로그인 보호 경계 밖이므로 독립 작업으로 진행한다. `[구현 필요]`
9. **국외 이전 인벤토리가 미완성이다.** 클라이언트 SDK·서버 벤더 호출·호스팅 및 저장 리전을 실제 배포 환경에서 확인해야 한다. `[조사 필요]`
10. **비인증 공개 변경 API가 요청 본문의 사용자 ID를 신뢰한다.** 일반 배포에서는 모든 사용자 변경을 인증된 principal에 묶고, caller-selected ID fallback은 명시적 비운영 데모 모드에서만 허용해야 한다. 이 수정은 세션·인증 전역 보호 범위이므로 위험 등록과 목표 설계만 지금 확정하고, 전역 금지 철회 후 별도 보안 PR로 구현한다. `[보호 영역 결정 필요]`

---

## 4. 무엇을 먼저 고칠까

저장 취약점과 탈퇴 기능 부재, 둘 다 P0다. 굳이 하나를 먼저 고른다면 탈퇴 기능이다 — 위험이 발생하는 방식이 다르기 때문이다.

| | 저장 취약점 (seller-applications.js) | 탈퇴·파기 기능 부재 |
|---|---|---|
| **위험이 발생하는 조건** | 뭔가 잘못돼야 터진다 — 외부 침해, 실수로 남긴 로그, 잘못된 익스포트. 다만 계좌번호·예금주명이 걸러지지 않고 관리자 API 응답에 그대로 실리므로, 그 API를 호출할 수 있는 누구에게나 이미 노출돼 있다는 점에서 group-booking.js의 예전 문제보다 노출 표면이 넓다. | 아무것도 잘못될 필요 없다 — 사용자가 삭제를 요청하는 순간 바로 미이행 상태가 된다 |
| **이미 있는 완화 장치** | DB 파일 자체에 `0600` 권한, `sellerApplications.manage` ACL로 조회 자체는 게이팅됨 — 그러나 저장 형태 자체(평문 계좌번호·예금주명, 인라인 blob)는 무방비 | 없음 |
| **별도 데드라인** | 없음 | Apple App Store 가이드라인 5.1.1(v) — 계정 생성을 지원하는 앱은 인앱 계정 삭제가 필수. iOS 앱이 이미 출시돼 있다면 지금 이 순간 심사 위반 상태일 수 있다. |

**결론:** 둘 다 이번 스프린트에서 같이 처리한다. 하나만 먼저 끝낼 수 있다면 탈퇴 기능부터 — 관리자가 수동으로 처리하는 버전이라도, "요청해도 아무 경로가 없는" 지금보다는 낫다. iOS 심사 위반 여부는 별도로 지금 바로 확인할 가치가 있다.

---

## 5. 단계별 실행 계획

TossPayments 연동 때와 같은 방식 — 백엔드 → 웹 → iOS → 하드닝 순서로 단계를 나누되, 사실수집은 0단계로 전체 기간 동안 병행한다.

### Stage 0 — 병행 · 즉시 시작 · 전체 기간 지속
**비즈니스·법무 사실수집.** 담당자 연락처, 사업자등록번호, 보관기간 확정치, 미성년자 정책, 마케팅 동의 여부, 벤더별 위탁계약서 존재 확인. 엔지니어링을 막지 않는다 — 지금 시작해서 가장 오래 걸리는 일이 나중에 병목이 되지 않게 한다.

동시에 현재 코드에서 이미 확인된 수집항목·목적·제3자/위탁 후보·실제 저장 상태와 이메일/고객센터 수동 권리행사 채널을 담은 **현행 기준 임시 개인정보처리방침을 즉시 게시**한다. 미확정 보관기간·법적 근거는 미확정이라고 표시하고, 아직 구현하지 않은 파기·암호화·세션 폐기 안전조치를 완료된 것처럼 쓰지 않는다. Stage 1~3 출시 때마다 구현 사실과 확정 수치를 반영해 개정한다.

### Stage 1 — 백엔드 프리미티브 `[구현 필요]`
저장 하드닝 + 삭제 기반 구축
- **공통 요청 admission 기본값:** POST·PUT·PATCH는 본문을 읽기 전에 method·정규화된 path를 allowlist와 대조해 미등록 경로·공개 surface로 잘못 유입된 관리자 경로를 거부하고, 인증이 필요한 경로는 credential의 형식·존재를 먼저 확인한다. 전체 서버의 동시 본문 수·누적 스트리밍 바이트 budget과 작은 기본 body limit을 먼저 적용한다. 그 뒤 인증 경로는 확인된 principal에, 공개 단체·판매자 신청은 allowlisted path와 requester/IP·전역 admission을 통과한 요청에 필요한 큰 route budget을 부여한다. unknown path·무인증 보호 경로·다수 경로 혼합 요청이 route dispatch 전에 메모리를 고갈시키지 않으면서 공개 신청은 정상 접수되는지 회귀 테스트를 둔다.
- `seller-applications.js`의 사업자등록증·포스터 파일을 `group-booking.js`(#142)와 동일한 패턴으로 `fs.writeFile` + 비공개 디렉토리 + 랜덤 파일명으로 저장하고 DB에는 참조만 기록한다. 다만 신규·이관 파일을 심사 UI가 읽을 보호 다운로드 경로가 먼저 배포돼야 하므로, 아래 인증 보호 경계 작업과 원자적으로 출시하기 전에는 legacy blob을 제거하지 않는다.
- 정산 계좌번호와 개인 예금주명은 실제 지급에 필요하므로 해시가 아니라 암호화 저장 + 마스킹 표시값으로 전환하고 관리자 목록·일반 상세 응답에서 두 원문 필드를 제거한다. 실제 지급에는 `finance.read`와 별도 정산 실행 권한을 모두 확인하고 사유·신청 ID·관리자 ID·열람/지급 결과를 감사하는 서버측 지급 작업을 우선한다. 지급 실행은 신청/정산채무별 고유 멱등키에 묶고 provider operation ID, `PENDING`·`SUCCEEDED`·`FAILED`·`UNKNOWN` 상태와 시도 결과를 지급 전후 내구 저장한다. timeout·응답 유실·DB 저장 실패의 `UNKNOWN`은 공급자 조회로 조정하기 전 재지급하지 않는다. 수동 지급 때문에 복호화가 불가피하면 동일 ACL·재인증·단건·짧은 TTL·감사 조건의 just-in-time 조회 API만 제공한다. 새 권한·지급/JIT 엔드포인트·관리자 UI는 `backend/admin-acl.js`와 `server.js`를 변경하므로 아래 인증 보호 경계 작업으로 이관하고, 그 전에는 마스킹 전환을 출시하지 않는다.
- **기존 `sellerApplications` 전수 마이그레이션:** 인라인 사업자등록증과 `posterImageDataUrl`을 비공개 파일로 추출하고 기존 계좌번호·예금주명을 암호화한 뒤, legacy blob·평문 `accountNumber`·`accountHolder`가 DB와 관리자 응답에서 사라졌는지 건수·샘플·재실행으로 검증한다. 사업자등록증과 포스터 각각에 `sellerApplications.manage` ACL이 적용된 다운로드 URL을 제공하고 관리자 심사 링크·상세·카탈로그 prefill DTO/UI를 전환한 뒤에만 legacy 필드를 제거하며, 이 라우팅은 아래 보호 경계 작업으로 수행한다.
- **기존 `groupBookingRequests` 마이그레이션:** 인라인 `businessRegistrationFileUrl`을 동일한 비공개 파일 저장소로 이관하고 legacy 필드를 제거한다. 담당자 이름·전화·이메일, `requestNote`, `reviewNote`와 원장 복제본을 상태별 보관·파기·가명화 범위에 포함하고 관리자 다운로드·재실행·실패 롤백을 검증한다.
- **신규 단체 신청 원자성·용량 통제:** `parseBody`와 base64 파싱 전에 요청자·전역 동시 본문 수·Content-Length/스트리밍 바이트 admission control을 적용해 분산 IP·프록시 변동 요청도 8 MB 버퍼 할당 전에 거부한다. 파일 저장 후 DB 영속화 실패 시 파일과 메모리 레코드를 함께 롤백하고 재시도 중복·고아 파일 탐지/정리를 검증한다. 업로드 디렉토리 전체 quota, `PENDING` 신청·총 파일 수/바이트 상한 또는 동등한 저장 admission control도 적용하고, 동시 초과 본문 거부·슬롯 반환·정리 후 재수락을 테스트한다.
- **단체 신청 영속 크기 통제:** orgName·contactName·전화·이메일·requestNote 등 정규화된 각 필드의 문자/바이트 상한과 전체 레코드 직렬화 바이트 상한을 적용하고, groupBookingRequests DB·원장 aggregate byte admission을 파일 quota와 원자적으로 확인한다. 제출 idempotency key로 응답 유실 재시도를 기존 신청/원장 결과에 연결하고, 회전 requester의 최대 텍스트·멀티바이트·동시 제출이 DB와 원장을 무제한 늘리지 않는지 검증한다.
- **판매자 신청 업로드 남용 방지:** `parseBody`와 base64 파싱 전에 요청자·전역 동시 본문 수·Content-Length/스트리밍 바이트 admission control을 적용해 거부할 요청은 16 MB 버퍼 할당 전에 중단한다. 전체·신청별 저장용량 quota, 파일 저장 후 DB 실패 롤백, 미완료·만료 파일 정리를 추가하고, 다수 IP의 동시 초과 본문이 메모리·이벤트루프를 고갈시키지 않으며 거부 뒤 용량 슬롯이 반환되는지 검증한다.
- **판매자 신청 영속 admission:** 작은 유효 파일/본문도 requester별 rate, 전역 `PENDING` 신청 수·DB/원장 직렬화 바이트 상한과 제출 idempotency key를 통과해야 저장한다. 레코드·파일·원장 capacity 확인과 append를 원자화하고, 회전 requester·작은 문서 churn·동시/응답 유실 재시도가 한 신청/원장만 만들며 상태 종료·파기 뒤 capacity를 반환하는지 검증한다.
- **판매자 행사 포스터 수명주기·용량 통제:** 인증된 판매자의 행사 생성·수정도 `parseBody`와 base64 파싱 전에 seller principal별·전역 동시 본문 수와 Content-Length/스트리밍 바이트를 제한하고, 판매자별·전체 포스터 파일 수/바이트 quota를 둔다. 새 파일 저장 뒤 DB 반영이 실패하면 새 파일을 롤백하고, 수정 성공 뒤 다른 행사에서 참조하지 않는 교체 전 포스터만 삭제한다. 기존 고아·미참조 파일 정리와 동시 수정·재시도·부분 실패를 검증하며, `server.js` 라우팅 변경은 아래 보호 경계 작업과 함께 수행한다.
- **판매자 행사 변경 멱등성:** 포스터를 포함하지 않은 행사 수정도 현재 영속 값과 정규화 비교해 동일하면 inventory·DB·원장을 no-op으로 반환한다. 실제 변경에는 seller/event idempotency key, 판매자별 request rate와 전역 ledger admission bound를 적용하고, 동일·상충 편집의 동시 실행과 응답 유실이 한 번의 상태·원장 변경만 만드는지 검증한다.
- **판매자 로그인 사전 제한:** `/api/seller/login`은 일반 8 MB 파서보다 앞에서 로그인 DTO에 맞는 작은 Content-Length/스트리밍 바이트 상한과 전역 동시 본문 admission control을 적용한다. IP Map도 5분 창 안의 최대 크기 또는 동등한 bounded limiter와 주기 정리를 함께 적용하고, 회전 IP·동시 초과 본문·축출·만료 뒤 정상 로그인의 의미가 보존되는지 검증한다.
- **결제상태 webhook 경계:** webhook 경로는 공통 8 MB `parseBody` 전에 전용 작은 Content-Length/스트리밍 바이트 상한과 전역/발신자 rate·동시성 제한을 적용한다. provider가 지원하는 서명·timestamp·replay 검증을 사용하고, 그렇지 않으면 paymentKey로 Toss 상태를 서버에서 조회해 기존 거래와 일치할 때만 기록한다. eventType·status 허용 enum과 각 필드 길이를 제한하고 provider event/payment 상태별 멱등키로 중복을 제거하며, 원장·DB의 aggregate admission bound와 초과/재전송/위조/조회 실패 테스트를 둔다.
- **운영 결제 fail-closed:** production은 live Toss client/secret key가 없거나 형식·환경 조합이 틀리면 startup health gate 또는 모든 checkout에서 실패하고 티켓 상태를 바꾸지 않는다. mock 영수증은 명시적 non-production 환경과 별도 test-only flag에서만 허용하며, 누락·오타·production mock flag·부분 설정 회귀 테스트를 원자 claim 상태 머신보다 먼저 통과한다.
- **QR 발급·게이트 검증 원장 남용 통제:** QR 발급은 user·device·ticket별 20초 생성 창과 rate limit을 강제하고 같은 창의 재시도는 기존 QR·`qrIssueLogs`·원장 결과를 반환한다. 발급 로그에는 상태별 보관기간과 전역 수/바이트 상한을 둔다. 게이트 검증은 credential별·장비별 짧은 rate limit과 원장 저장 admission bound를 두고 동일 QR nonce·동일 결과 재시도는 멱등 처리한다. 반복 발급과 위조·이미 사용·만료 QR 검증이 JSON DB를 무제한 늘리지 않게 하되 네트워크 응답 유실 뒤 합법 재시도는 기존 결과를 안전하게 반환하도록 abuse·legitimate retry 테스트를 추가한다.
- **신뢰기기 등록 admission:** principal별 활성 trusted device 수와 전역 레코드/원장 수·바이트 상한, 등록 rate를 적용한다. 동일 deviceId 재등록은 기존 행·원장 결과를 반환하고 한도 도달 시 명시적 기존 기기 폐기/교체를 원자적으로 수행하며, deviceId churn·동시 교체·응답 유실이 다른 정상 기기를 축출하거나 중복 원장을 만들지 않는지 검증한다.
- **서버 검증 입장 challenge:** QR 발급의 `otpVerified`·`delayAcknowledged` 같은 caller boolean을 신뢰하지 않는다. principal·device·ticket·action·nonce·발급/만료시각에 묶인 짧은 TTL 서버 challenge 상태 또는 서버 서명 proof를 요구하고 OTP 성공·실제 지연 경과 뒤에만 소비한다. 변조·재생·다른 기기/티켓 전용·동시 소비와 앱 attestation에 boolean만 바꾼 요청을 거부하는 테스트를 둔다.
- **원장 선행 정제:** 최상위 `actorId`, payload의 buyerId·sellerId·targetUserId·`deviceId`, 관리자·판매자 `username`, 관리자 `ipAllowlist`, 무키·단축 SHA-256 `emailHash`와 소셜 공급자 `subjectHash`, 전화·본인 파생값과 닉네임, 문의 제목, 법인·기관명, 자유서술 메모를 분류하고 keyed/crypto-erasable 가명화 후 재체인 또는 항목별 암호화와 키 파기 방식을 선택한다. 신뢰기기 등록·QR 발급에 복제된 deviceId와 계정 변경 원장의 username·IP 규칙, 소셜 가입 원장의 provider subject 파생값도 계정/기기/관리자 수명주기에서 같은 키 파기·재체인 범위에 포함한다. 재체인 전 개인정보를 복제하지 않는 서명된 기존 root/hash와 항목 수를 불변 보관하고, old→new 항목 매핑·허용 필드 변경 목록을 담은 마이그레이션 manifest를 생성한다. 원문 내보내기가 법적·장애복구상 꼭 필요하면 별도 암호화·최소권한·접근감사·확정 보관기간·만료 시 키 파기를 적용하고 계정 삭제 시 함께 crypto-erasure한다. 새 체인의 무결성뿐 아니라 누락·순서변경과 비승인 필드 변경이 없음을 대조 검증해 두 체인의 연속성을 입증한다. 기존 연결 식별자·복구 가능 파생값·평문 정제가 끝난 뒤에만 승인된 최소 필드를 보관 예외로 검토
- **본인인증 해시 전환:** 신규 인증은 전화번호와 PortOne의 `di` → `ci` → `identityKey` → `customerId` 선택 우선순위를 인벤토리·동의·테스트에 고정하고, 선택된 식별자마다 키 버전이 있는 HMAC/pepper를 사용한다. 기존 DB에는 원문 전화번호·CI/DI/identityKey/customerId가 없어 HMAC을 재생성할 수 없으므로 기존 인증에 재인증 만료일을 부여하고, 재인증 성공 시 HMAC으로 교체한다. 구 `phoneHash`·`personHash` SHA-256 비교는 짧은 전환 기간에만 격리하고 만료 후 제거하며, 각 fallback의 누락·우선순위·키 관리·회전·중복검사·재인증 UX를 검증한다. 소셜 로그인 코드는 변경하지 않는다.
- **미완료 본인인증 파기:** `PENDING` 시도에 짧은 만료시각을 부여하고 만료 시 사용자 ID·전화 해시·마스킹 값을 삭제한다. 만료 레코드가 중복검사를 막지 않고 같은 사용자가 안전하게 재시도할 수 있는지 배치·요청 시 정리와 회귀 테스트로 검증한다.
- **본인인증 생성·확인 admission:** 시작 API는 인증 principal을 강제한 뒤 principal·요청자별 rate limit, principal별 활성 `PENDING` 수, 전역 pending 레코드 수·바이트와 원장 append 상한을 적용한다. 시작·확인은 identity operation idempotency key를 공유하고 이미 `VERIFIED`인 시도 확인은 PortOne 재조회·DB rewrite·원장 append 없이 기존 결과를 반환한다. 확인에도 principal/operation rate와 provider/ledger capacity를 적용하고, 응답 유실·동시 replay·회전 전화번호·다수 계정 churn이 provider와 원장을 무제한 사용하지 않는지 검증한다.
- **운영 본인인증 fail-closed:** production에서 `TIG_PORTONE_IDENTITY_TEST_MODE` 또는 동등 test bypass가 켜지면 startup이나 모든 start/confirm 요청을 실패시킨다. caller phone 기반 test identity는 명시적 non-production에서만 허용하고, production flag 누출·누락된 PortOne credential·환경 오타가 인증 성공이나 구매 gate 통과로 이어지지 않는 회귀 테스트를 둔다.
- **프로필 변경 멱등성:** 정규화한 nickname·profile 값이 현재 상태와 같으면 DB·원장 no-op으로 반환한다. 실제 변경에는 principal idempotency key·rate limit·원장 capacity 확인을 적용하고, 반복·동시 동일/상충 PATCH와 응답 유실이 중복 `PROFILE_UPDATED` 항목을 만들지 않는지 검증한다.
- **예매 임시 레코드 파기:** 활성 계정도 `queueEntries`, `seatHolds`, `reservationDrafts`의 운영 TTL이 끝난 뒤 레코드가 남으므로 `EXPIRED`·`RELEASED`·`CANCELED`·`CONVERTED` 상태별 짧은 보관기간을 두고 사용자 ID·티켓 ID·금액·idempotency digest를 삭제/가명화한다. 만료 처리와 파기 배치를 분리하고 좌석 재고·완료 예매 감사에 필요한 최소치만 보관하는 회귀 테스트를 추가한다.
- **좌석 홀드 admission:** 홀드 생성 시 principal의 유효한 admitted queue entry와 행사 범위를 검증하고 principal별·행사별 활성 hold/좌석 수와 전역 active hold 상한을 원자적으로 적용한다. 새 idempotency key로도 동일 좌석·principal의 기존 홀드를 중복 생성하거나 만료를 무한 연장하지 못하게 하고, 한 계정이 판매 가능 재고를 독점하지 않으며 만료·취소 뒤 용량과 좌석이 반환되는 동시성 테스트를 둔다.
- **대기열 entry admission:** `performanceDateId` 길이·형식을 제한하고 실제 판매 가능한 performance/date 존재를 확인한 뒤 principal·performance별 단일 활성 entry, principal별·전역 active entry 상한과 request rate를 원자 적용한다. 동일 principal/performance 재시도는 기존 entry를 반환하고, 가짜 ID churn·동시 생성·만료 reconciliation이 JSON DB를 늘리거나 다른 사용자의 정상 entry를 축출하지 않는지 검증한다.
- **문의 스레드 파기:** 활성 계정의 `supportThreads`도 `OPEN`·`ANSWERED`·`CLOSED` 상태별 보관기간을 두고 종료 후 user ID·제목·분류·고객 메시지·관리자 답변을 삭제/가명화한다. 연결 `operatorAlerts` 사본과 알림 작업을 같은 배치에서 정리하고, 재개된 문의·법정 분쟁 보관·부분 실패를 검증한다.
- **문의 생성·답글 admission:** principal별·thread별 생성/답글 rate와 최대 `OPEN` 스레드·스레드별 메시지 수/바이트, 전역 OPEN 스레드·`PENDING` operatorAlert 수/바이트 상한을 둔다. alert·메시지·원장 capacity 확인과 append를 원자적으로 처리하고, 동일 idempotency key 재시도는 기존 스레드·메시지·알림·원장 결과를 반환한다. 한 계정의 새 키 flood·단일 thread 답글 flood·동시 생성·부분 실패와 정상 종료 후 용량 반환을 검증한다.
- **관심목록 알림 파기:** `notificationJobs`는 문의가 아니라 `watchlistId` 기준이므로 `SENT`·`CANCELED` 상태별 짧은 보관기간 후 user ID·event ID·선호 채널을 독립 파기한다. 관심목록 제거 시 예정 job 취소뿐 아니라 기존 완료/취소 job의 고아 사본도 정리하고, 재등록·전송 감사 최소치·부분 실패를 검증한다.
- **관심목록 변경 멱등성:** 동일 event·principal의 선호 채널과 상태가 같으면 DB·알림·원장 no-op으로 반환하고, 변경에는 idempotency key와 principal별 rate·원장 aggregate admission bound를 적용한다. 반복 PUT·동시 동일/상충 변경·응답 유실이 중복 원장과 job을 만들지 않는지 검증한다.
- **종료 재판매 기록 파기:** 활성 계정의 `resalePools`도 `CANCELED`·`MATCHED` 상태별 보관기간 뒤 seller ID·참여 buyer ID 목록·winner ID·ticket ID·가격·정산액을 삭제하거나 keyed 가명화한다. 결제·정산·분쟁에 필요한 최소 필드와 법정 보관 근거를 별도로 확정하고, 진행 중 풀과 완료 결제·원장 무결성을 훼손하지 않는 배치·부분 실패 테스트를 추가한다.
- **재판매 참여 멱등성:** buyer가 이미 같은 OPEN pool에 참여했다면 성공 상태를 그대로 반환하고 `POOL_JOINED` 원장을 다시 쓰지 않는다. 신규 참여에는 buyer/pool idempotency key, principal별 rate와 전역 원장 storage admission을 적용하고, 반복·동시 참여와 응답 유실이 membership·원장을 한 번만 생성하는지 검증한다.
- **재판매 listing 수명주기 admission:** seller·ticket별 동시 OPEN pool 하나와 list/cancel cooldown·rate를 적용하고 동일 상태 요청은 no-op으로 반환한다. list/cancel operation idempotency key와 pool/원장 aggregate admission을 사용해 반복 교대·동시 취소·응답 유실이 새 pool·원장 항목을 무제한 만들지 않는지 검증한다.
- **재판매 결제·추첨 권한:** purchase와 draw는 소유권·seller balance를 바꾸기 전에 buyer별 provider 결제를 내구 `PENDING`·`UNKNOWN`·`SUCCEEDED`·`FAILED` 상태와 멱등키로 확인한다. 공급자 조정 전 winner 확정·티켓 이전·판매자 잔액 적립을 하지 않고, 캡처 뒤 로컬 실패는 자동 보상으로 수렴한다. draw 실행은 일반 customer principal이 아니라 별도 `resale.draw.execute` 관리자/시스템 권한과 최근 재인증을 요구하고 익명·소비자·읽기전용 역할을 거부한다.
- **직거래 위반 집계:** 동일 owner·ticket·target·가격 범위의 반복 신고는 새 sanction/원장 행을 매번 추가하지 않고 집계 카운트·최초/최종 시각과 bounded evidence만 갱신한다. target ID·가격 길이/범위를 검증하고 principal별 rate·sanction/원장 전역 storage admission을 적용해, 신뢰점수 0 이후 반복·동시 요청도 유용한 위험 증거를 보존하면서 행 수를 무제한 늘리지 않는지 검증한다.
- 계정 삭제·익명화 백엔드 함수 구현. `users`, `identityVerifications`, `nativeSessions`, `nativeAuthHandoffs`, `supportThreads`, `operatorAlerts`, `watchlist`, `notificationJobs`, `trustedDevices`, `admissionCredentials`, `qrIssueLogs`, `queueEntries`, `seatHolds`, `reservationDrafts`, 티켓 `heldBy`·`reservationId`·`ownerId`·`virtualQr`·`currentQr`, `paymentTransactions`, `resalePools`, 잔액·신뢰점수·제재, 결제·예매·기업신청 연결키를 함께 분류한다. 활성 대기열·홀드·초안을 먼저 취소해 좌석을 반환하고, 보관 근거가 확정된 항목만 비식별화해 예외 보관. 해당 principal의 미소비 nativeAuthHandoffs를 같은 삭제 트랜잭션에서 폐기하는 보호 경계 작업이 먼저 완료되지 않으면 계정 삭제를 완료하지 않는다.
- **삭제 동시성 차단:** 삭제 시작을 원자적으로 `DELETION_PENDING` 상태로 표시한 뒤 checkout·재판매·문의·기업신청 등 모든 사용자 연결 변경과 신규 credential·social session·native session·handoff 발급/교환을 거부한다. 또는 이들 변경·세션 발급과 전체 삭제를 동일 직렬화 경계/트랜잭션에 넣어 sweep 뒤 새 credential이나 레코드가 생기지 않게 한다. pending·삭제 완료 identity의 신규 로그인 거부와 삭제·로그인 경합을 검증하며, 소셜 로그인 경로는 아래 보호 경계 작업이 완료되지 않으면 삭제 기능을 출시하지 않는다.
- **유료 티켓 소유권 선행 결정:** 탈퇴 시 소유·재판매 중 티켓을 자동 삭제하지 않는다. 공연 전 환불, 다른 계정으로 본인확인 이전, 법정 보관 후 가명 소유권 유지 중 하나를 제품·법무가 결정한다. 공연 전 환불을 선택하면 환불별 고유 멱등키와 provider operation ID, 내구 `PENDING`·`UNKNOWN`·`SUCCEEDED`·`FAILED` 상태를 provider 호출 전후에 저장하고, 응답 유실·DB 저장 실패의 `UNKNOWN`은 공급자 조회로 조정하기 전 재호출하지 않는다. 재판매 풀·입장 자격·결제/환불 정리가 모두 확정된 뒤에만 계정을 파기한다.
- **결제 전 원자적 좌석 claim:** checkout은 raw ticket ID의 구매 가능 여부만 읽고 Toss 확인을 시작하지 않는다. 유효한 principal hold/queue와 티켓을 하나의 원자적 claim으로 `PAYMENT_PENDING`에 전환하고 결제별 멱등키·만료·소유 principal을 내구 저장한 뒤 provider 확인을 호출한다. 동시 구매자는 provider 호출 전에 한 명만 claim을 획득한다. 캡처 성공 후 로컬 할당 실패·응답 유실은 `UNKNOWN` 상태에서 Toss 조회로 조정하고, 패자 캡처가 발생하면 내구 자동 환불/보상 상태 머신이 완료되기 전 티켓을 재판매 가능 상태로 풀지 않는다.
- **잔액·정산 선행 처리:** `users.balance`가 양수이거나 재판매 정산이 진행 중이면 계정 파기를 완료하지 않는다. 멱등 지급·다른 정산계정 이전·법적 근거가 있는 채권 보관 중 확정된 절차를 완료하고, 지급 실패·재시도·중복 지급을 감사 가능하게 기록
- **기관 구매자 수명주기:** group-booking 신청 담당자의 복제 전화·이메일과 synthetic user, 소유 티켓·결제·신청 연결을 상태별 보관기간에 맞춰 익명화한다. 신청 철회·만료·행사 종료 시 수동 삭제 채널과 유료 티켓/미수금 처리 순서를 둠
- **기관 대량티켓 결제 수명주기:** 신청 승인은 최대 5,000장을 즉시 소유·입장 가능 상태로 만들지 않고 만료시각이 있는 비입장 `INVOICE_PENDING` reservation/claim만 생성한다. invoice별 멱등 payment operation과 provider/수동 정산 증빙이 `SUCCEEDED`로 조정된 뒤 원자적으로 소유권·admission credential을 발급한다. 기한 만료·명시적 실패는 재고를 반환하되 `UNKNOWN`은 provider 조회·수동 입금 증빙으로 성공/실패를 확정하거나, 이미 성공한 결제가 있으면 내구 환불·보상 상태 머신을 완료하기 전 reservation을 해제하지 않는다. 동시 승인/입금·응답 유실·부분 발급·중복 정산을 검증한다.
- **삭제 재개 권한:** 삭제 시작 시 일반 변경 권한은 차단하되 기존 세션은 작업 완료 응답 직전까지 삭제 상태 조회·동일 작업 재개에만 사용할 수 있게 축소하거나, 일회성·짧은 TTL·해당 deletion job 전용 operation token을 발급한다. 응답 유실·부분 실패 뒤에도 같은 principal이 정상 기능을 복원하지 않고 기존 작업을 재개할 수 있어야 하며, 완료 후에만 모든 일반/삭제 전용 세션·토큰을 폐기한다. 토큰 탈취·다른 job/계정 사용·완료 후 재사용 거부와 멱등 재시도를 검증한다.
- 판매자 계정의 비활성화·삭제, 신청·소유 행사·원장 연결키의 보관/이관 절차를 별도 수명주기로 구현한다. seller session은 서버측 session version 또는 동등한 폐기 상태를 검증하게 하고 로그아웃·비밀번호 변경·계정 비활성화 때 원자적으로 회전해 복사된 기존 8시간 토큰의 재사용을 거부한다.
- 파기·보관 배치 작업 골격 구현
- sellerApplications와 groupBookingRequests의 `PENDING`·`REJECTED`·`APPROVED/REGISTERED` 상태별 보관기간, 신청자 철회·수동 삭제, 레코드와 참조 파일의 원자적 삭제를 배치에 구현
- sellerApplications의 대표자·주소·웹사이트·사업자등록번호, 담당자 이름·직함·전화·이메일, 행사·판매조건·수수료·기타 자유서술까지 상태별 보관·철회·삭제·legacy 이관 검증에 포함
- **레거시 동의 전환:** 동의 증거가 없는 기존 단체신청과 버전·시각 없는 boolean만 가진 판매자신청은 새 동의를 받은 것으로 소급 생성하지 않는다. 처리를 격리하고 정책 버전 재동의를 받거나 법무가 승인한 다른 처리 근거를 레코드별 기록하며, 어느 쪽도 없으면 문서와 레코드를 파기한 뒤에만 마이그레이션·심사를 계속한다.
- 판매자 로그인 IP Map에 만료 항목 주기 정리와 최대 크기를 두고, 실제 제거가 검증된 기간만 방침에 기재. 관리자 로그인 IP Map 정리는 `server.js` 인증 보호 경계이므로 아래 별도 작업으로 이관
- 관리자의 민감정보 "열람" 로그 존재 여부는 읽기 전용으로 확인하고, 현재 누락된 workspace 로깅 구현은 `backend/admin.js` 보호 경계이므로 아래 별도 작업으로 이관

### Stage 2 — 웹 서피스 `[구현 필요]`
탈퇴 UI + 구현 반영 방침 개정
- 마이페이지에 계정탈퇴 진입점 추가
- 계정 삭제 API는 caller-selected ID fallback을 절대 사용하지 않고 유효한 bearer session의 principal만 대상으로 삼되, 30일 credential만으로 실행하지 않는다. 최근 provider 재인증 또는 짧은 TTL·삭제 작업 전용 확인 토큰을 요구하고 재사용·다른 principal 사용을 거부한다. HttpOnly 쿠키 전환 뒤에는 CSRF 토큰과 Origin 검증을 함께 강제하며, 익명·타계정·탈취 세션·재생 요청을 상태 변경 전 거부한다. 필요한 소셜 로그인 변경은 보호 금지 철회 뒤 별도 인증 PR로 수행한다.
- 기업/단체 신청과 판매자 신청의 동의 체크박스를 실제 `/privacy` · `/terms`에 연결하고 수집항목 안내 문구를 보강. 로그인 UI·동의 문구·링크는 보호 범위이므로 이 계획에서 변경하지 않음
- `POST /api/group-booking/requests`와 `POST /api/seller-applications`에서 동의값·정책 버전을 필수 검증해 동의 시각과 함께 저장. 누락·구버전 직접 호출은 거부
- 공개 `/api/state`의 전체 사용자 ID·이름과 `resalePools[].sellerId`를 포함한 모든 중첩 DTO의 안정 사용자 식별자를 제거하는 것을 기본안으로 한다. 먼저 인증된 principal 전용 own-listings 조회·취소 경로를 배포하고 웹 스키마·재판매 UI와 iOS 모델을 비식별 공개 DTO 및 인증 DTO로 전환해 discovery·checkout·본인 listing 취소를 검증한다. 지원 클라이언트가 새 계약을 소비한 뒤 별도 서버 릴리스에서 공개 필드를 제거하며, 제품상 공개가 꼭 필요하면 공개 목적·범위·근거·사용자 통제를 먼저 확정한다.
- Toss `customerKey`를 계정/설치 기준으로 재설계하고 로그아웃·탈퇴·공유기기 계정 전환 시 초기화 여부를 Toss 계약·결제 연속성에 맞춰 확정. 브라우저와 iOS에서 동일한 고지·리셋 정책 적용
- 취소 이력 localStorage를 사용자별로 분리하고 로그아웃·탈퇴 때 삭제해 다음 계정이 이전 예약 ID·공연·좌석·사유·환불액을 보지 못하게 함
- 마지막 로그인 공급자 localStorage 키의 목적과 현재 무기한 수명을 고지하고 로그아웃·계정 삭제 또는 별도 "로그인 정보 초기화" 중 확정된 시점에 삭제한다. 이 변경은 소셜 로그인 보호 범위이므로 전역 금지 철회 뒤 공유 브라우저·계정 전환 회귀 테스트와 함께 수행한다.
- 게이트 오프라인 QR 대기열에 payload 만료 기반 TTL을 적용하고 전송 성공, 게이트 토큰 초기화·폐기, 운영자 로그아웃 시 비운다. 초기화·장비 폐기/분실은 로컬 토큰 삭제에 그치지 않고 서버 gate session을 폐기하며, 세션 만료·회전과 공유 장비 운영 절차를 추가
- 7절 골격 기반으로 신규 방침·약관 페이지 작성

> **보호된 인증 경계:** 30일 bearer credential의 `localStorage` 저장은 위험으로 등록한다. 목표안은 HttpOnly·Secure·SameSite 쿠키 또는 동등한 보호, 서버 측 폐기·회전, XSS 및 로그아웃 회귀 테스트다. 그러나 세션 코드는 간편로그인 수정 금지 범위이므로 전역 금지가 명시적으로 철회되기 전에는 구현하지 않는다.

> **보호된 권한 경계:** 세션 없는 `resolveActorId`/`resolvePurchaseUserId`가 요청 본문의 사용자 ID를 신뢰하는 fallback은 일반 배포에서 제거하고, 명시적 비운영 데모 플래그에서만 허용해야 한다. 본인인증·구매·재판매·신뢰기기·QR 변경 API는 인증 principal을 필수로 하고 익명 호출 거부 테스트를 둔다. 이 작업도 세션·인증 전역 금지가 철회되기 전에는 코드로 구현하지 않는다.

> **주의:** "정보주체 권리행사방법" 섹션은 셀프서비스 삭제 기능이 실제로 동작하기 전까지 이메일·고객센터 수동 채널로만 기술한다. 작동하지 않는 셀프서비스를 방침에 먼저 적어두는 게 오히려 더 나쁘다.

### Stage 3 — iOS `[구현 필요]`
네이티브 계정삭제. App Store 가이드라인 5.1.1(v) 대응이 목적이므로 개인정보보호법 일정과 무관하게 별도 데드라인으로 취급한다. 동의 링크도 네이티브에서 동일하게 노출.

### Stage 4 — 하드닝 · 검증 `[구현 필요]`
회귀 테스트 + 자발적 점검
- 미등록 API path·잘못 유입된 관리자 path·무인증 보호 경로 요청이 작은 기본 body limit과 전역 동시 본문/누적 바이트 budget으로 dispatch·큰 버퍼 할당 전에 거부되고, 인증 경로는 확인된 principal만, 공개 단체·판매자 신청은 requester/IP·전역 admission을 통과한 요청만 route별 큰 budget을 받아 정상 접수되는지 검증
- 신규·기존 판매자 신청과 레거시 단체 신청 모두에서 사업자등록증·포스터 원문 blob과 평문 계좌번호·예금주명이 DB·API 응답에 남지 않는지, 마이그레이션을 재실행해도 안전한지 확인하는 회귀 테스트
- 사업자등록증·포스터 각각의 ACL 다운로드 URL과 관리자 심사 링크·상세·카탈로그 prefill 이미지가 마이그레이션 뒤에도 동작하고, 세션 없음·권한 부족·다른 신청 ID 요청이 거부되는지 확인
- 단체신청의 `parseBody` 이전 요청자·전역 동시 본문/바이트 제한과 전체 저장 quota·PENDING/파일 수·바이트 상한이 분산 IP·프록시 주소 변동·동시 8 MB 본문에서도 버퍼 할당 전에 적용되고, 거부 후 슬롯 반환·만료 정리 뒤 정상 신청이 재개되는지 확인
- 단체신청 각 텍스트 필드·전체 직렬화 레코드와 DB/원장 aggregate byte 상한, 제출 멱등키가 최대 멀티바이트·회전 requester·동시/응답 유실 재시도에서도 한 신청/원장만 저장하는지 확인
- 신규 단체 신청의 DB 저장 실패·재시도에서 파일/레코드가 원자적으로 롤백되고 고아 파일이 남지 않는지 확인
- 판매자 신청의 `parseBody` 이전 throttling·전역 동시 본문/바이트 제한, 저장 quota·DB 실패 롤백·고아 파일 정리가 다수 IP의 동시 16 MB 초과 제출을 메모리 할당 전에 차단하고 슬롯을 누수하지 않는지 확인
- 판매자 신청 requester rate·전역 PENDING/DB/원장 byte admission과 제출 멱등키가 작은 문서 churn·회전 requester·동시/응답 유실에도 한 신청/원장만 저장하고 종료 뒤 capacity를 반환하는지 확인
- 판매자 행사 생성·수정의 seller principal별·전역 사전 본문 제한과 파일 quota가 동시 16 MB 제출을 버퍼 할당 전에 차단하고, DB 실패 시 새 포스터를 롤백하며 수정 성공 시 미참조 교체 전 포스터만 지우는지 확인
- 판매자 행사 동일 수정은 inventory·DB·원장 no-op이고 실제 변경은 seller/event 멱등키·rate·ledger bound를 적용해 동시 편집·응답 유실에도 한 번만 반영되는지 확인
- 단체 신청 IP limiter가 회전 IP·신뢰 프록시 주소 변동에서도 최대 크기를 넘지 않고, 축출·거부 뒤 기존 정상 요청의 rate-limit 의미와 1시간 만료 정리가 유지되는지 확인
- 결제상태 webhook이 작은 사전 본문 상한·엄격한 enum/길이·rate/storage bound를 적용하고, 서명/replay 검증 또는 Toss 서버 조회·기존 거래 대조 뒤 멱등 기록하며 위조·중복·초과·조회 실패가 원장과 DB를 늘리지 않는지 확인
- production에서 Toss live credential 누락·오타·test mock flag가 startup/checkout을 fail-closed하고 mock 영수증·티켓 할당을 만들지 않는지 확인
- 판매자 로그인이 작은 스트리밍 본문 상한·전역 동시 admission을 `parseBody` 전에 적용하고, IP limiter가 회전 주소에서도 5분 창 최대 크기를 넘지 않으며 축출·만료 뒤 정상 로그인을 허용하는지 확인
- 신규 하드닝 경로가 기존 ACL로 실제로 게이팅되는지 확인
- 계정 삭제가 대기열·홀드·예약 초안을 취소해 좌석을 반환하고 모든 연결 컬렉션·티켓 홀드/예약/QR 필드를 처리하며 완료 후 활성·삭제 전용 세션을 폐기하는지 확인. 승인된 보관 예외만 비식별 상태로 남기고 응답 유실·부분 실패 뒤 기존 세션의 축소 권한 또는 deletion operation token으로 동일 job만 재개되며 다른 기능·계정·완료 job에는 쓰이지 않는 백엔드·클라이언트 테스트 추가
- 계정 삭제 API가 인증된 principal만 삭제하고 익명·타계정·본문 ID 위조 요청을 거부하며, iOS Keychain native credential·user ID 삭제와 서버 세션 폐기를 함께 완료하는지 확인
- 계정 삭제 API가 최근 재인증/삭제 전용 확인을 요구하고 탈취한 장기 credential·확인 토큰 재생·다른 principal·쿠키 CSRF 요청을 거부하는지 확인
- 동시 checkout에서 원자적 PAYMENT_PENDING claim 승자만 Toss 확인을 호출하고, provider 캡처 후 로컬 실패·응답 유실은 내구 UNKNOWN 조정과 자동 환불/보상으로 수렴하며 이중 판매·이중 캡처·수동 미환불 패자가 남지 않는지 확인
- 공연 전·후 및 재판매 중 유료 티켓 각각에 선택된 환불/이전/가명보관 정책을 적용해 ownerId·virtualQr·입장 자격·재고가 고아 상태로 남지 않는지 확인
- 삭제 작업의 공연 전 환불이 환불별 멱등키와 내구 상태를 provider 호출 전후에 보존하고, 응답 유실·DB 저장 실패·재개·중복 요청에서 공급자 조정 전 재환불하지 않으며 단 한 번만 완료되는지 확인
- 기관 구매자와 pending·consumed·expired nativeAuthHandoffs, 상태별 신청 레코드·참조 파일의 만료·철회·파기를 검증
- 기업/단체·판매자 신청 API가 동의 누락·구버전·UI 우회를 거부하고 정책 버전·동의 시각을 보존하는지 확인
- `/api/state` 비인증 응답의 최상위·중첩 DTO 어디에도 안정 사용자 식별자가 남지 않는지 확인
- 원장 신규 `actorId`, payload 사용자 참조·deviceId·관리자/판매자 username·관리자 ipAllowlist, emailHash·소셜 subjectHash·전화·본인 파생값이 keyed/crypto-erasable 형태이고 금지된 평문 필드가 없으며, 계정/기기/관리자 수명주기와 기존 원장 마이그레이션 후 체인 검증이 통과하는지 확인
- 기존 본인인증은 원문 없이 HMAC으로 변환하지 않고 재인증 만료·성공 교체·구 해시 종료·중복검사를 검증하며, confirm 응답 유실·동시 replay는 PortOne 재조회/중복 원장을 만들지 않고 기존 VERIFIED 결과로 복구하는지 확인
- production에서 PortOne identity test mode·credential 누락/오타가 fail-closed하고 caller phone만으로 VERIFIED나 구매 gate 통과 상태를 만들지 않는지 확인
- 프로필 동일 PATCH와 직거래 동일 위반 반복은 no-op/집계 처리되고 principal rate·멱등키·sanction/ledger bound가 동시·응답 유실에도 중복 원장 행을 만들지 않는지 검증
- 소비자·판매자 계정 삭제가 각 세션을 폐기하고 관심목록·알림·문의 사본·거래·재판매·위험 프로필·연결 행사를 정책대로 삭제 또는 비식별화하는지 확인
- 양수 잔액과 진행 중 재판매 정산의 지급·이전·채권 보관이 성공하기 전 탈퇴 완료를 막고, 실패 재시도에서 중복 지급이나 잔액 소실이 없는지 확인
- gate offline QR의 TTL/토큰 초기화/로그아웃 삭제, Toss customerKey 정책, 취소 이력 사용자 분리와 계정 전환·탈퇴 삭제를 브라우저·iOS·공유기기 시나리오로 검증
- 활성 계정의 queueEntries·seatHolds·reservationDrafts가 EXPIRED·RELEASED·CANCELED·CONVERTED 상태별 보관기간 뒤 파기되고 좌석 재고·완료 예매는 훼손되지 않는지 검증
- 활성 계정의 supportThreads와 연결 operatorAlerts가 문의 상태별 보관기간 뒤 함께 파기되고, 재개·분쟁 보관·부분 실패에서 필요한 메시지만 남는지 검증
- 본인인증 시작의 principal/requester rate·principal별 PENDING·전역 레코드/바이트 상한과 문의 생성/답글의 principal/thread rate·OPEN 스레드·메시지 수/바이트·PENDING alert 상한이 churn/flood를 막고, 원자적 capacity 확인·멱등 재시도·만료/종료 정리 뒤 정상 용량을 반환하는지 검증
- watchlist 제거와 notificationJobs SENT·CANCELED 상태별 보관기간 뒤 user/event/channel 사본이 독립 파기되고 재등록·부분 실패에서 고아 job이 남지 않는지 검증
- 활성 계정의 resalePools가 CANCELED·MATCHED 상태별 보관기간 뒤 참여자·당첨자·티켓·가격·정산 사본을 파기/가명화하고 진행 중 풀과 결제·정산·분쟁 최소 기록은 보존하는지 검증
- admitted queue entry 없는 홀드를 거부하고 principal/행사/전역 active hold 상한과 비연장 TTL이 한 계정의 좌석 독점을 막으며, 만료·취소 후 재고와 capacity가 원자적으로 반환되는지 검증
- 대기열 생성이 실제 performance/date·입력 길이를 검증하고 principal/performance 단일 entry와 principal/전역 active 상한·rate를 적용해 가짜 ID churn과 동시 요청을 막는지 검증
- 관심목록 동일 PUT과 기존 resale pool 재참여가 no-op/멱등 결과를 반환하고 반복·동시·응답 유실에도 알림 job·membership·원장 항목을 한 번만 생성하는지 검증
- resale list/cancel cooldown·멱등키·pool/ledger bound가 반복 교대를 제한하고, purchase/draw가 provider 결제 성공·조정 뒤에만 winner·소유권·seller balance를 변경하며 실패·응답 유실을 자동 보상하는지 검증
- resale draw가 전용 execute 권한·최근 재인증을 요구해 익명·customer·security/admission/read-only finance 역할을 거부하는지 검증
- seller 로그아웃·비밀번호 변경·비활성화 후 복사한 기존 서명 토큰으로 행사 조회·생성·수정을 재시도해 모두 거부되는지 검증
- 웹·iOS가 인증 own-listings 계약으로 조회·취소하도록 먼저 전환된 뒤 공개 `/api/state`의 users·resale sellerId 제거 상태에서도 discovery·checkout·상태 decode가 성공하는지 검증
- 마지막 로그인 공급자 키가 정책상 정한 로그아웃·탈퇴·명시적 초기화 시점에 제거되고 공유 브라우저의 다음 사용자에게 이전 공급자가 표시되지 않는지 검증
- DELETION_PENDING·삭제 완료 identity의 native/social 로그인·session/handoff 발급·교환을 거부하고, 삭제 sweep과 동시 발급 경합 뒤 신규 credential이 남지 않는지 검증
- 게이트 초기화·폐기·분실 시 서버 세션 폐기, 만료·회전과 복제 토큰 거부를 검증
- QR 발급이 user/device/ticket별 20초 창·rate·로그 retention/storage bound를 적용하고 같은 창 재시도를 중복 기록하지 않으며, 게이트별 rate/storage bound와 QR nonce·결과 멱등 처리가 위조·사용됨·만료 QR 반복의 원장 증가를 제한하면서 응답 유실 뒤 합법 재시도는 기존 결과로 복구하는지 검증
- 신뢰기기 등록이 principal별 활성 기기·전역 레코드/원장 상한과 rate를 적용하고 동일 device 재등록·교체·폐기를 멱등 처리해 deviceId churn이 다른 정상 기기를 축출하지 않는지 검증
- OTP·지연 위험대 QR 발급이 caller boolean 대신 principal/device/ticket/action/nonce/time에 묶인 서버 challenge/proof를 단 한 번 소비하고 변조·재생·cross-device/ticket 요청을 거부하는지 검증
- 기관 대량티켓 승인이 비입장 INVOICE_PENDING reservation만 만들고 결제 조정 성공 뒤 소유권·입장 credential을 원자 발급하며, UNKNOWN은 provider/manual 증빙으로 확정하거나 성공 결제의 내구 환불·보상이 끝나기 전 재고를 풀지 않아 만료·실패·동시 승인/입금에서도 이중판매 없이 복구되는지 검증
- 자발적 개인정보 영향평가(PIA) 여부 법무 검토
- 쿠키·로컬스토리지 고지 문구 재검토

---

## 6. 작업 분류

실제 정책 판단이 필요한 항목은 임의로 정하지 않고, 코드만으로 착수 가능한 일과 명시적으로 분리했다.

### 지금 가능 `[구현 필요]` — 보호된 인증 경계 밖에서 바로 착수
1. 판매자 사업자서류·신청 포스터 신규 저장 하드닝·남용 통제·마이그레이션 도구, 판매자 행사 포스터의 quota·원자적 교체/고아 정리, 단체 신청 레거시 이관·신규 저장 롤백. 실제 legacy blob 제거, ACL 조회 소비자 전환과 `server.js` 사전 본문 제한은 아래 보호 라우팅 작업이 먼저 배포된 뒤 수행
2. 기업/단체·판매자 신청 UI와 API의 동의 강제, 정책 버전·동의 시각 저장
3. 공개 `/api/state`의 최상위·중첩 사용자 식별자 제거 또는 공개 목적 결정을 위한 제품 검토
4. 원장 `actorId`·사용자 참조·평문 payload 인벤토리, 정제·재체인 또는 암호화/키 파기 설계
5. 전화번호·CI/DI/identityKey/customerId 파생값의 HMAC/pepper 전환과 기존 계정 재인증 전환(소셜 로그인 코드 제외)
6. 소비자·판매자·기관 구매자 계정, 유료 티켓·잔액/정산과 전체 연결 컬렉션을 포함하는 인증 principal 전용 삭제/익명화·세션 폐기·멱등 파기 배치 골격
7. 방침·푸터에 담당자·사업자정보 슬롯 구현
8. 호스팅 리전과 벤더별 클라이언트·서버 통신 경로 실환경 확인
9. 관리자 열람 로깅 여부 읽기 전용 확인
10. 신청 상태별 레코드·파일 보관/철회 파기와 판매자 로그인 IP Map 주기 정리
11. 게이트 QR·Toss customerKey·취소 이력의 로컬 저장 TTL·계정 분리·초기화

### 보호 경계 해제 후 가능 `[보호 영역 결정 필요]`
1. 30일 브라우저 bearer credential을 HttpOnly·Secure 쿠키 또는 동등 구조로 전환하고 폐기·회전·XSS·로그아웃 테스트 추가
2. 일반 배포에서 모든 `TIG_DEMO_*` caller-selected ID 읽기·변경 fallback을 무조건 비활성화하거나 인증 principal에 묶는다. 세션·티켓·관심목록·전체 문의 스레드 읽기와 본인인증·구매·재판매·신뢰기기·QR 변경에 대해 익명 요청·다른 사용자 ID·운영 demo flag 조합이 모두 거부되는 집중 테스트를 추가
3. 계정 삭제 트랜잭션에서 해당 principal의 모든 미소비 nativeAuthHandoffs를 먼저 폐기해 익명화 stub으로 새 native session을 만들 수 없게 한다. 계정 삭제와 동시에 발급/교환하는 경합, 삭제 후 코드 교환 거부, 다른 principal handoff 불변을 집중 테스트하고, unrelated consumed/expired 전역 정리는 별도 배치로 수행
4. 로그인 동의 문구·링크 수정이 필요하면 간편로그인 보호 규칙을 먼저 명시적으로 철회하고 별도 PR과 인증 회귀 테스트로 수행
5. 관리자 로그인 IP Map의 기존 5분 주기 정리가 계속 작동하는지 검증하고 최대 크기를 추가한다. `adminAccounts`의 퇴직·계약종료 수명주기에는 원장뿐 아니라 groupBookingRequests.reviewedBy, sellerApplications.review.log[].by, gateSessions.createdBy의 보관·가명화/파기와 참조 무결성 회귀 테스트를 포함한다. `bootstrap-admin`은 초기 설정 뒤 비활성화할 수 있게 하고 담당자 이탈·침해 시 환경 credential 회전과 모든 bootstrap session 즉시 폐기를 원자적으로 수행한다. `server.js`·`backend/admin.js` 보호 경계 변경이므로 명시적 승인과 bootstrap/DB 관리자 로그인·세션·CSRF·IP ACL·권한 회귀 테스트를 먼저 갖춤
6. 만료된 `nativeSessions` DB 레코드와 서버 메모리 `adminSessions`의 주기 정리·최대 크기·만료/로그아웃 회귀 테스트를 추가한다. 관리자 계정의 비밀번호 변경·비활성화 때 account session version을 회전하거나 해당 계정의 모든 세션을 원자적으로 폐기하고, 기존 cookie로 보호된 읽기·변경 라우트 재사용이 거부되는지 검증한다. 네이티브·관리자 세션 보호 경계이므로 전역 금지 철회 후 별도 인증 PR로 수행
7. 판매자 사업자등록증·포스터의 `sellerApplications.manage` 다운로드 라우트를 `server.js` 권한 allowlist에 추가하고 신청 DTO·관리자 심사 링크·상세·카탈로그 prefill UI를 전환한다. 저장 이관과 한 릴리스로 묶고 관리자 로그인·세션·CSRF·IP ACL·권한 없음·다른 신청 접근 회귀 테스트를 통과한 뒤 legacy blob을 제거
8. 브라우저 `localStorage` bearer credential과 안정 user ID에 만료 메타데이터를 두고 서버의 만료·삭제 계정 응답 시 두 키를 함께 제거하며, 공유 브라우저의 30일 idle 만료·계정 삭제·계정 전환 테스트를 추가
9. Google `name` claim이 없을 때 이메일을 이름으로 저장하지 않는 비이메일 fallback을 적용하고, 기존 이메일 형태 `users[].name`을 별도 표시명 재설정 또는 가명화로 이관한다. 공개 DTO 제거와 함께 신규·기존 계정 회귀 테스트를 거치며 간편로그인 전역 금지 철회 후 수행
10. 정산 실행 권한을 `backend/admin-acl.js`에 등록하고 `server.js`에 서버측 지급 또는 JIT 단건 복호화 엔드포인트를 allowlist한다. 기존 관리자 정산 UI를 제한 경로로 전환하고 신청/정산채무별 멱등키·provider operation ID·내구 상태 머신과 `UNKNOWN` 공급자 조정을 구현한다. 로그인·세션·CSRF·IP ACL·권한 없음·재인증·감사·다른 신청 접근 및 timeout·응답 유실·DB 저장 실패·중복 클릭/재시도에서 단일 지급만 발생하는 테스트를 통과한 뒤 암호화/마스킹 전환과 함께 출시
11. `server.js`의 단체 신청 IP rate-limit Map을 최대 크기 또는 동등한 bounded limiter로 전환하고, 회전 IP·신뢰 프록시 주소 변동·축출·만료를 회귀 테스트한다. 판매자 행사 생성·수정 라우트에도 seller principal별·전역 사전 본문 admission control을 연결하며 기존 판매자·관리자 인증, 세션, CSRF, IP ACL 동작을 함께 검증한다.
11. `TIG_ADMIN_TOKEN`의 세션·CSRF·역할 우회를 제거하거나 최소 권한·엔드포인트 범위·짧은 수명·회전·폐기·감사 가능한 서비스 자격증명으로 대체한다. 토큰 요청도 계정·지원·재무·판매자 신청·정산 JIT 권한을 임의로 획득하지 못하는 집중 회귀 테스트를 추가
12. account·support·finance·seller-application·admission·audit workspace의 민감정보 열람 이벤트를 `backend/admin.js`에서 감사하되 원문 개인정보는 로그 payload에 복제하지 않는다. 명시 승인 후 관리자 로그인·세션·CSRF·IP ACL·권한 없음·성공/실패 열람·로그 위변조 회귀 테스트를 포함한 별도 보호 PR로 수행
13. 소셜 로그인 보호 금지 철회 뒤 마지막 로그인 공급자 localStorage 키에 확정 수명을 적용하고 로그아웃·계정 삭제 또는 명시적 초기화에서 제거한다. 공급자 선택 UX, 신규·기존 로그인, 공유 브라우저 계정 전환 회귀 테스트를 별도 인증 PR에서 수행한다.
14. 소셜·native 로그인과 session/handoff 발급·교환이 `DELETION_PENDING`·삭제 완료 identity를 원자적으로 거부하도록 삭제 트랜잭션과 직렬화한다. 삭제 sweep 동시 로그인, callback, handoff 발급/교환과 삭제 완료 후 재로그인 회귀 테스트를 별도 보호 PR에서 수행하며 이 작업 전 계정 삭제를 출시하지 않는다.
15. 계정 삭제 시작에는 최근 provider 재인증 또는 짧은 TTL·일회성 삭제 확인 토큰을 요구하고, HttpOnly 쿠키 전환 시 CSRF/Origin 검증을 함께 적용한다. 탈취한 30일 credential·확인 토큰 재생·다른 계정·cross-site 요청 회귀 테스트를 통과한 뒤 출시한다.
16. 모든 native provider·handoff 발급 경로에 principal별·전역 issuance rate와 계정별 활성 세션 상한, 명시적 oldest/revoked 교체 규칙, 로그인 원장 storage admission을 적용한다. 같은 provider credential/handoff replay·동시 발급·device churn이 세션과 원장을 무제한 늘리거나 다른 계정 세션을 축출하지 않는 회귀 테스트를 별도 보호 PR에서 수행한다.
17. 관리자 Toss 취소는 `security.manage`가 아니라 별도 `refund.execute` 권한과 최근 재인증을 요구하고 security·admission·finance.read-only 역할을 거부한다. 취소 요청별 고유 operation/idempotency key와 provider operation ID, 호출 전 내구 `PENDING`, 응답 유실·저장 실패의 `UNKNOWN`, 조회 조정·`SUCCEEDED/FAILED` 상태를 부분 환불 단위까지 저장해 중복 클릭·timeout·재시도에서도 한 번만 환불한다.
18. 재판매 draw 라우트를 `resale.draw.execute` 관리자/시스템 권한으로 `server.js` allowlist에 등록하고 최근 재인증·CSRF·IP ACL을 적용한다. 익명·customer session·security/admission/read-only finance 역할 거부와 결제 성공 전 draw/소유권/정산 변경 금지를 집중 검증한다.
19. `NODE_ENV=production` startup은 `TIG_GOOGLE_AUTH_TEST_MODE` 또는 `TIG_NEXT_DEV=1` 중 어느 하나라도 활성화되면 무조건 거부한다. 공개 Google test credential의 웹/native session 발급, 약한 bootstrap admin, 필수 secret 검사 생략, admin cookie Secure 누락이 production에서 불가능함을 별도 보호 인증 테스트로 검증한다.
20. `TIG_ADMIN_TOKEN` 축소 전에 gate-session 목록·발급·폐기 GET/POST/revoke 경로를 `admission.manage` session permission allowlist에 매핑한다. admission 관리자 성공과 익명·다른 역할·CSRF·IP ACL 실패를 검증해 긴급 gate credential 폐기가 서비스 토큰에 의존하지 않게 한다.
21. MATCHED resale 강제취소는 `refund.execute`와 최근 재인증을 요구하고 buyer provider 결제에 내구 cancellation idempotency key·PENDING/UNKNOWN/조정 상태를 기록한다. 실제 공급자 환불 성공과 이미 실행된 seller payout 회수/상계 보상이 확정되기 전 seller debit·buyer ownership 반환·로컬 REFUNDED 확정을 하지 않으며 timeout·응답 유실·부분 보상을 검증한다.

### 결정 필요 `[구현 필요]` — 결정 후 엔지니어링 착수
1. 원장 평문 정제·소거 설계와 보관 예외 최종 승인
2. 보관기간 실제 수치 확정
3. 미성년자 정책 — 가입 차단 / 만14세 이상 허용 / 법정대리인 동의 구축 중 택1
4. 마케팅 동의 범위 — 현재 기능 없음, "해당없음" 확정 여부
5. `seller-applications.js` 계좌번호·개인 예금주명 보호 방식 — 암호화 알고리즘/키 관리를 KMS로 할지 앱 레벨로 할지
6. 벤더별 처리위탁 vs 제3자제공 구분, 실제 위탁계약서 존재 확인
7. 공개 사용자 이름·안정 ID가 제품에 필요한지와 공개 처리의 적법 근거
8. 입장·기기·QR·게이트 이력의 항목별 보관기간과 탈퇴 시 익명화 범위

### 콘텐츠만 필요 `[구현 불필요]` — 코드 변경 없이 사실만 있으면 됨
1. 사업자등록번호·대표자명·주소·통신판매업 신고번호
2. 개인정보보호책임자 성명·직책·연락처·부서
3. 공개할 보관기간 수치 문구화
4. 미성년자 정책 문구
5. 제3자제공·위탁 목록 최종 확인(코드에 안 보이는 벤더 유무)
6. 정보주체 권리행사 접수 채널(주소·연락처)

---

## 7. 방침 문서 골격

한국 이용자·규제기관이 기대하는 표준 순서. 법률 문구 자체가 아니라, 각 절에 무엇이 들어가고 지금 바로 쓸 수 있는지 표시했다.

| 절 | 내용 | 상태 |
|---|---|---|
| 1. 수집하는 개인정보 항목 및 수집방법 | 저장값뿐 아니라 Google ID 토큰 이메일과 `name` 부재 시 이름 필드 저장 가능성, Naver 실명·닉네임 선택, 입력·PortOne 응답 전화번호·CI·DI·identityKey·customerId, 환불계좌, 요청 IP처럼 백엔드가 처리하는 원문을 출처·선택 우선순위·목적·전달처·저장/비저장 또는 메모리 보관기간과 함께 기재. 로그인 식별자·실명·닉네임·이메일 파생값·native handoff, 전화·선택 본인식별자 해시·마스킹값, 소비자·판매자·기관 구매자·관리자 계정과 역할·IP 허용목록, 대기열·좌석 홀드·예약 초안, 유료 티켓 소유권, 결제 거래·재판매 참여, 잔액·신뢰점수·제재, 판매자신청 대표자·주소·웹사이트·사업자번호·담당자 이름/직함/전화/이메일·행사/판매조건/수수료/기타 자유서술·사업자서류·포스터·정산계좌, 기업신청 담당자와 서류, 문의 user ID·제목·분류·전체 고객 메시지·관리자 답변·운영자 알림 사본, 관심목록·알림 채널, 기기·입장·QR·게이트 이력, 게이트 오프라인 QR과 gate bearer token·label, Toss customerKey, 취소 이력도 포함 | 작성 가능 · 원문/저장 구분 필요 |
| 2. 수집 및 이용 목적 | 계정생성/로그인, 본인확인·중복가입 방지, 결제·환불·정산, 공식 재판매, 부정이용 위험관리, 판매자·기업예매 이행, 문의·알림, 신뢰 기기·입장 검증, IP 기반 남용 방지·접근통제. 공개 사용자 식별자는 노출 제거 또는 적법 근거 결정 전 보류 | 일부 보류 · 공개 목적 결정 필요 |
| 3. 보유 및 이용기간 | 전자상거래법 법정 최소치(결제·계약 5년, 소비자불만 3년)를 기본값으로 초안 | 보류 · 결정 필요 |
| 4. 제3자 제공 | 위탁 구조로 정리된다면 "해당없음"이 유력 | 보류 · 법무 확인 필요 |
| 5. 개인정보처리 위탁 | TossPayments(결제), PortOne/다날(본인인증), Google/Kakao/Naver(로그인) | 거의 작성 가능 |
| 6. 국외 이전 | JWKS 조회가 아니라 Google GIS·네이티브 SDK, 기타 벤더의 실제 전송 경로와 호스팅·저장 리전을 확인한 뒤 이전받는자/국가/시기·방법/목적/항목/보유기간/거부방법을 기재 | 보류 · 실환경 조사 필요 |
| 7. 파기 절차 및 방법 | Stage 1의 삭제/익명화 메커니즘이 만들어진 뒤 그 방식대로 기술 | Stage 1 이후 · `[구현 필요]` |
| 8. 정보주체의 권리·행사방법 | Stage 0 임시 방침에는 이메일/고객센터 수동 채널을 즉시 게시하고, Stage 2의 실제 셀프서비스 출시 뒤 검증된 절차만 추가 | 임시안 즉시 작성 가능 · 셀프서비스는 Stage 2 이후 |
| 9. 안전성 확보조치 | 0600 파일권한과 scrypt 비밀번호 해싱은 기재 가능. 관리자 ACL은 `TIG_ADMIN_TOKEN` 우회를 제거·범위화하기 전까지 전체 관리자 접근에 강제된다고 표현하지 않고, 원장의 평문 payload와 복구 가능한 본인인증 SHA-256도 개선 완료 전 안전조치로 과장하지 않음 | 일부 작성 가능 |
| 10. 자동 수집 장치(쿠키 등) | 추적 쿠키·분석스크립트는 없으나 필수 인증 쿠키인 `tig_seller_session`(판매자 ID·CSRF·발급시각, 8시간), `tig_admin_session`(랜덤 세션 ID·서명, 8시간), `tig_oauth_state_*`(공급자·클라이언트·nonce·발급시각, 10분), `tig_social_session_*`(공급자·user ID·발급시각, 5분)의 목적·기간·HttpOnly/SameSite/Secure 조건과 실제 만료·로그아웃·콜백/교환 삭제 범위를 고지한다. 판매자 로그아웃·비밀번호 변경 뒤에도 복사 토큰이 8시간 유효한 현재 한계와 개선 계획을 과장 없이 밝힌다. 브라우저 `localStorage` bearer credential·user ID와 마지막 로그인 공급자(`google`·`kakao`·`naver`)는 현재 만료 메타데이터가 없어 무기한 남을 수 있다는 사실, 목적, 삭제 방법을 고지한다. 공유 게이트의 raw bearer token·label도 현재 로컬·서버 만료가 없다는 사실, 목적, 기기 인계·분실·폐기 시 서버 폐기/로컬 초기화를 명시하고, iOS Keychain native credential·user ID, 브라우저 `localStorage`·iOS `UserDefaults` Toss customerKey, 취소 이력, 게이트 QR도 각각의 목적·전달처·기간·계정 범위·삭제 방법을 정확히 고지 | 작성 가능 · 소셜 인증 저장 변경은 보호 영역, 로컬 하드닝 별도 필요 |
| 11. 아동의 개인정보보호 | 미성년자 정책 결정에 따라 문구 확정 | 보류 · 결정 필요 |
| 12. 개인정보보호책임자 | 성명·직책·연락처·부서 | 보류 · 콘텐츠 필요 |
| 13. 권익침해 구제방법 | 개인정보분쟁조정위원회·118 등 표준 안내 | 작성 가능(법무 검토) |
| 14. 고지 의무·시행일·개정이력 | 공개 시점에 실제 시행일 확정 | 작성 가능 |

**이용약관 별도 보강:** 사업자정보(등록번호·주소·대표자명)와 통신판매업 신고번호가 `/terms`에도 빠져있다 — 전자상거래법이 요구하는 별개 항목이라 방침과 별도로 챙겨야 한다.

---

## 8. 추가로 점검할 것

범위를 확정하기 전에 짚고 넘어가야 코드만 봐서는 답이 안 나오는 것들.

- **처리위탁 vs 제3자제공 구분.** TossPayments·PortOne은 위탁 쪽이 유력하지만, 실제 위탁계약서(DPA) 존재 여부는 코드로 확인할 수 없다.
- **본인식별정보 별도 동의.** CI/DI뿐 아니라 PortOne fallback `identityKey`·`customerId`와 각 파생 해시 처리는 일반 "필수 약관 동의"와 별개의 명시적 동의 화면이 있어야 할 수 있다.
- **자발적 개인정보 영향평가(PIA).** 법적 의무는 아니지만 CI/DI급 데이터를 다루는 규모라면 자문 권고 대상.
- **마케팅 동의(법 제22조의2).** 일반 수집동의와 별개 범주 — 현재 마케팅 발송 기능 자체가 없다는 것을 명시적으로 확정할지 결정 필요.
- **유출 대응 절차(법 제34조).** 사용자 대상 방침과 별개로, 내부 침해대응 매뉴얼 자체가 저장소 어디에도 없다.
- **관리자 "열람" 로깅.** 현재 원장은 상태 변경 이벤트만 기록한다 — 조회 자체도 기록되는지 별도 확인 필요.
- **ACL만으로는 부족.** 기술적 접근통제 외에 정기 접근로그 검토, 임직원 서약, 보안교육 등 관리적 조치도 방침에 과장 없이 기술해야 한다.
- **국외 이전 전수 확인.** Google의 공개 JWKS 다운로드는 개인정보 이전이 아니다. 브라우저 GIS, 네이티브 SDK, Toss·PortOne 등 원격 SDK/API, Vercel 실행·DB·파일 저장 리전을 네트워크·계약·배포 설정으로 각각 확인한다.
- **공개 노출 목적 확인.** `/api/state`의 사용자 ID·이름 공개가 실제 제품 요구가 아니라면 방침으로 정당화하지 말고 응답에서 제거한다.
- **입장 이력 보관 기준.** 신뢰 기기, 입장 자격, QR 발급 로그, 티켓 QR, 게이트 사용 이력을 결제·분쟁 기록과 구분해 최소 보관기간과 파기 방법을 정한다.
- **소비자·판매자 계정 수명주기 분리.** 판매자 계정·세션·소유 행사와 소비자 거래·재판매·잔액·신뢰점수·제재는 목적과 법정 보관 근거가 다르므로 폐쇄·익명화 규칙을 각각 정한다.
- **정산 완료 전 파기 금지.** 양수 잔액과 재판매 정산 채권은 개인정보 파기와 별개인 금전 의무다. 지급·이전·법정 채권 보관이 실패한 계정은 삭제 완료로 표시하지 않는다.
- **자동 수집 IP 보관.** in-memory rate-limit Map의 최대 유지시간, 서버 재시작 시 파기, 로그인 접근통제 처리와 외부 프록시 헤더 신뢰 범위를 운영 문서와 방침에 일치시킨다.
- **신청 상태별 파기.** 소비자 계정과 연결되지 않은 판매자·단체 신청도 상태와 마지막 처리시각으로 만료시켜 레코드와 모든 참조 파일을 함께 지운다. 신청자 철회 접수와 법정 보관 예외를 별도로 둔다.
- **공유기기 로컬 데이터.** 취소 이력, Toss customerKey, 게이트 오프라인 QR은 계정·운영자 전환 때 다른 사람에게 이어지지 않도록 범위와 초기화 시점을 명시한다.
- **본인인증 재인증 전환.** 기존 DB에는 HMAC 원문이 없으므로 구 SHA-256을 새 HMAC으로 변환했다고 주장하지 않는다. 만료 고지·재인증 유예기간·중복 계정 충돌·미완료 계정 제한을 제품과 법무가 확정한다.

---

## 9. 열린 질문

이 계획서를 실행에 옮기기 전에 확정이 필요한 항목.

1. `seller-applications.js`의 계좌번호·개인 예금주명을 암호화할 것인가 마스킹 표시만 할 것인가 — 정산에 실제로 필요한 값이라 해시로는 안 됨(2절).
2. 보관기간은 법정 최소치를 그대로 쓸 것인가, 분쟁 대응 등을 이유로 더 길게 가져갈 것인가?
3. 미성년자 가입을 아예 막을 것인가, 만14세 이상만 허용할 것인가, 법정대리인 동의 플로우를 구축할 것인가?
4. 개인정보보호책임자는 누구로, 어떤 연락처로 공시할 것인가?
5. Vercel 배포 리전은 고정돼 있는가, 아니면 지금 정해야 하는가?
6. 기존 원장 payload는 재체인 마이그레이션할 것인가, 항목별 암호화 후 키 파기할 것인가?
7. 공개 `/api/state`에 사용자 ID·이름이 필요한가, 즉시 제거할 것인가?
8. 입장·기기·QR·게이트 이력은 각각 얼마나 보관하고 탈퇴 시 무엇을 익명화할 것인가?
9. 간편로그인 보호 규칙을 유지해 세션 bearer 위험을 보류할 것인가, 별도 세션 작업 전에 전역 금지를 명시적으로 철회할 것인가? 본인인증 HMAC 전환은 이 결정과 분리해 진행한다.
10. 판매자 계정 폐쇄 시 소유 행사와 정산·분쟁 기록을 누구에게 이관하고 무엇을 비식별화할 것인가?
11. 요청 IP와 환불계좌 원문의 실제 메모리 보관시간, 로그 유입 여부, Toss 전달 후 파기 시점을 어떻게 검증할 것인가?
12. 기존 본인인증 계정의 재인증 유예기간과 미완료 시 구매 제한, 중복 계정 충돌 처리 기준은 무엇인가?
13. 간편로그인 보호 규칙을 철회할 때 비인증 caller-selected ID fallback 제거를 세션 저장 하드닝보다 먼저 긴급 처리할 것인가?
14. 탈퇴 시 공연 전·후 및 재판매 중 유료 티켓을 환불·본인확인 이전·가명 보관 중 어떤 방식으로 처리할 것인가?
15. 판매자·단체 신청의 상태별 보관기간과 신청자 철회 시 법정 예외, 기관 구매자 레코드의 만료 기준은 무엇인가?
16. Toss customerKey를 계정별로 재발급할지 설치별로 유지할지, 로그아웃·탈퇴·공유기기에서 언제 초기화할 것인가?
17. 양수 잔액·진행 중 재판매 정산을 지급·이전·채권 보관 중 어떤 방식으로 완료하고 실패 계정은 어떤 상태로 둘 것인가?
18. 게이트 세션의 만료·회전 주기와 분실 장비 긴급 폐기 채널은 무엇인가?

---

*TIG 개인정보처리방침 실행 계획서 · 백엔드 코드 전수 조사 및 Fable 5 자문 교차검증 기반 · 법률 자문 대체 불가*
*Artifact: https://claude.ai/code/artifact/f62ff0a0-a373-454a-8983-2e2e064aa093*

# TIG 개인정보처리방침 실행 계획서

> Fable 5 Advisor 자문 · 백엔드 코드 전수 조사 기반
>
> 이 문서는 엔지니어링·제품 실행계획이다. 방침에 들어갈 법률 문구 자체와 각 조항의 최종 근거는 법무 검토를 거쳐야 하며, 이 문서가 그 검토를 대신하지 않는다.

지금 `/privacy`와 `/terms`는 각각 세 문장, 다섯 문단짜리 안내문이다. 실제 서비스가 어떤 개인정보를 어디서 얼마나 수집하고 있는지 코드를 직접 읽어 확인한 뒤, 방침을 다시 쓰는 데 필요한 엔지니어링·제품·법무 작업을 순서대로 정리했다.

**현재 상태: 이 계획서의 구현 항목은 아직 하나도 착수되지 않았다.** 아래 `[구현 필요]` 표시는 전부 미착수 상태를 뜻하며, 별도 구현 계획서(`개인정보보호정책-구현계획서.md`)에서 착수 순서를 다룬다.

---

## 목차

1. [현황 진단](#1-현황-진단)
2. [조사 결과 정정](#2-조사-결과-정정)
3. [핵심 격차 9가지](#3-핵심-격차-9가지)
4. [무엇을 먼저 고칠까](#4-무엇을-먼저-고칠까)
5. [단계별 실행 계획](#5-단계별-실행-계획)
6. [작업 분류](#6-작업-분류)
7. [방침 문서 골격](#7-방침-문서-골격)
8. [추가로 점검할 것](#8-추가로-점검할-것)
9. [열린 질문](#9-열린-질문)

---

## 1. 현황 진단

백엔드 소스를 직접 읽어 확인한, 실제로 존재하는 개인정보 처리 흐름.

> **검증 기준과 실행 제한:** 아래 표는 원문을 일시적으로 처리하는 단계, 영구 저장, 제3자 전달, 공개 노출을 구분한다. 또한 현재 정상화된 간편로그인 영역은 전역 보호 규칙의 적용 대상이다. Google·Kakao·Naver 로그인, 세션, 관련 테스트와 공급자 설정은 이 계획서 때문에 변경하지 않는다. 해당 영역의 보완은 위험과 검증 조건만 기록하고, 사용자가 전역 금지를 명시적으로 철회한 뒤 별도 승인·집중 인증 테스트를 거쳐 착수한다.

| 영역 | 수집·저장 방식 | 평가 |
|---|---|---|
| **계정 로그인**<br>Google·Kakao·Naver | 공급자 식별자와 닉네임을 처리한다. Google ID 토큰의 이메일 원문은 계정 생성·검증 중 백엔드가 일시 처리하지만 DB에는 저장하지 않는다. 공급자 식별자는 안정 ID/해시 형태로 저장한다. | 원문 입력의 출처·목적·비저장 사실을 고지해야 함 |
| **본인인증**<br>PortOne · 다날 | 입력 전화번호와 PortOne 응답의 전화번호·CI/DI 원문을 확인 과정에서 일시 처리하고, DB에는 SHA-256 해시와 마스킹 값(010-\*\*\*\*-1234)을 저장한다. 마스킹 값 때문에 후보 공간이 작아 비밀키 없는 해시는 원문 추측에 취약하다. | HMAC/pepper 전환과 기존 해시 마이그레이션 필요 · `[구현 필요]` |
| **결제·환불**<br>TossPayments | 카드 입력은 위젯이 직접 처리하고 백엔드는 거래키·사용자·티켓·금액·수단·상태를 저장한다. 관리자의 계좌 환불 시 `refundReceiveAccount` 원문을 일시 처리해 Toss에 전달하지만 DB에는 저장하지 않는다. | 거래·환불계좌의 목적·전달·보관 구분 필요 |
| **기업/단체 대량티켓 신청**<br>`backend/group-booking.js` | 신규 사업자등록증은 `fs.writeFile`로 비공개 디렉토리에 `0o600` 권한·랜덤 파일명으로 저장하고 DB에는 참조만 기록한다(#142). 다만 구버전 신청에는 인라인 `businessRegistrationFileUrl`이 남을 수 있다. | 신규 경로 완료 · 레거시 마이그레이션 필요 `[구현 필요]` |
| **기업 판매자 등록 신청**<br>`backend/seller-applications.js` | 사업자등록증 파일을 base64 데이터 URL 그대로 JSON DB에 인라인 저장. **정산 계좌(은행명·계좌번호·예금주)까지 원문 저장**하며, 관리자 목록 조회 API가 이를 그대로 반환. | **최우선 조치 대상 · `[구현 필요]`** |
| **기업 판매자 계정** | 승인 후 `sellerAccounts`에 법인명, 사용자명, 비밀번호 파생값, 신청·판매자 ID를 저장하고 행사·원장과 연결한다. 발급·비밀번호 변경은 있으나 탈퇴·파기 경로는 없다. | 보유기간·비활성화/삭제·세션 폐기·연결 행사 처리 필요 `[구현 필요]` |
| **예매·재판매·위험 프로필** | 결제 거래에 사용자·티켓·금액·수단·상태를, 재판매 풀에 판매자·구매자 참여와 당첨 이력을, 계정에 잔액·신뢰점수·제재를 저장한다. | 항목별 목적·보관·탈퇴 시 익명화 기준 필요 `[구현 필요]` |
| **입장·신뢰 기기** | `trustedDevices`에 사용자·기기 ID, 기기명, 플랫폼, 검증시각을 저장하고 `admissionCredentials`, `qrIssueLogs`, 티켓의 현재 QR 필드에 사용자·티켓·기기·입장 이력을 연결해 저장. | 수집 고지, 보관기간, 탈퇴 시 삭제·익명화 범위 필요 · `[구현 필요]` |
| **관심목록·문의 알림** | `watchlist`, `notificationJobs`에 사용자별 공연 선호·채널을, `operatorAlerts`에 사용자 ID와 문의 내용 일부를 중복 저장한다. | 탈퇴·파기 범위와 알림 취소 검증 필요 `[구현 필요]` |
| **공개 상태 API** | 인증 없는 `GET /api/state`가 전체 사용자 안정 ID·이름뿐 아니라 `resalePools[].sellerId` 등 중첩 DTO의 안정 식별자도 반환한다. | 모든 공개 DTO의 사용자 식별자 제거 또는 목적·근거 확정 `[구현 필요]` |
| **감사 원장** | 해시체인의 최상위 `actorId`와 payload의 buyerId·sellerId·targetUserId 외에도 공급자 닉네임, 문의 제목, 법인·기관명, 자유서술 검토 메모가 평문으로 들어갈 수 있다. | 현재 데이터를 정제하기 전 삭제 예외로 취급 불가 · `[구현 필요]` |
| **자동 수집 IP** | 단체 신청, 판매자·관리자 로그인에서 요청 IP를 남용 방지·접근통제 목적으로 처리하며 일부 rate-limit Map에는 최대 1시간 유지한다. | 출처·목적·메모리 보관기간·파기 고지 필요 |
| **회원 탈퇴 / 파기** | 삭제·보관기간·익명화 로직이 백엔드 어디에도 없음. 프론트에도 탈퇴 진입점 없음. | **기능 자체가 없음 · `[구현 필요]`** |
| **관리자 접근 통제**<br>`backend/admin-acl.js` | 개인정보 범주별로 세분화된 권한 키(`accounts.manage`, `groupBooking.manage`, `finance.read` 등)가 실제로 강제되고 있음. | 공개 가능한 자산 |
| **쿠키·트래킹·세션 저장** | 분석·광고 스크립트는 없지만 30일 유효 bearer credential이 브라우저 `localStorage`에 원문 저장된다. 동일 출처 스크립트 주입 시 계정 탈취 위험이 있다. | HttpOnly·Secure 쿠키 또는 동등 보호, 폐기·회귀 테스트 필요 · `[보호 영역 결정 필요]` |
| **국외 이전** | Google JWKS 요청은 공개 서명키 조회이며 개인정보 이전 근거가 아니다. 실제 Google GIS/네이티브 SDK 흐름, 다른 벤더의 브라우저·서버 통신, 배포·저장 리전은 아직 확정하지 못했다. | 전수 인벤토리 완료 전 고지 확정 불가 · `[조사 필요]` |
| **방침·약관 페이지** | 각각 3문단·5문단짜리 요약문. 항목별 수집내역, 보관기간, 제3자 목록, 담당자 연락처 전부 없음. | **전면 재작성 필요 · `[구현 필요]`** |
| **동의 UI** | 로그인 폼의 동의 문구는 실제 방침에 연결되지 않지만 전역 보호 범위라 이 계획에서 변경하지 않는다. 기업/단체 대량티켓 신청은 동의가 없고, 판매자 신청은 `true`만 저장해 정책 버전 증거가 없다. | 신청 API의 버전 동의 구현 필요 · 로그인은 보호 영역 |

---

## 2. 조사 결과 정정

이 계획서는 두 차례 재검증을 거쳤다. 1차 조사, 2차 Fable 교차검증, 그리고 실제 `origin/main` 코드와의 직접 대조 — 세 번째 단계에서 2차 교차검증 자체의 오류가 드러났다. 최종 결론만 신뢰하지 말고 이 과정을 그대로 남긴다.

> **🔴 2차 검증도 틀렸다 — "System A"는 실제로 존재한다**
> 1차 조사는 사업자등록번호·대표자명·정산 계좌번호를 저장하는 "개별 판매자 셀프서비스 신청" 시스템이 있다고 보고했다. 2차 Fable 검증은 이를 "코드베이스 어디에도 없다"고 정정했는데, **이 정정이 틀렸다.** `origin/main`의 `backend/seller-applications.js`(`createSellerApplicationBackend`)를 직접 읽어 확인한 결과, 이 시스템은 실제로 존재하며 `bizRegistrationDocDataUrl`(사업자등록증을 base64로 JSON DB에 인라인 저장) 외에 **`settlement.{bankName, accountNumber, accountHolder}` — 정산 계좌번호를 원문으로 저장**한다. `listSellerApplications()`는 이 값을 관리자 API 응답에 그대로 포함해 반환하며, 별도로 걸러내는 DTO가 없다. 은행 계좌번호는 사업자등록증보다 더 민감한 항목이라, 이 계획서의 "최우선 조치 대상"은 아래에서 `seller-applications.js`로 옮겨졌다.

> **✅ group-booking.js의 신규 쓰기 경로는 이미 고쳐져 있었다**
> 2차 검증은 `backend/group-booking.js`가 사업자등록증 파일을 아무 하드닝 없이 저장한다고 결론지었는데, 이는 조사 시점의 스냅샷이 `origin/main`보다 뒤처져 있었기 때문이었다. 실제 `origin/main`의 커밋 #142는 신규 파일을 `fs.writeFile` + `0o600` 권한 + `businessRegistrationDir` 옵션으로 저장하고 DB에는 파일 참조만 기록한다. 그러나 DTO가 구버전 인라인 `businessRegistrationFileUrl`을 계속 지원하므로 **신규 쓰기 하드닝과 별개로 기존 레코드 마이그레이션은 필요하다.**

이 정정을 반영해, 우선순위와 실행 계획은 `seller-applications.js`를 최우선 조치 대상으로 놓고 다시 짰다. 앞으로 이 코드베이스를 다시 조사할 일이 있다면, 로컬 체크아웃이 아니라 `git show origin/main:<path>`로 항상 직접 대조할 것.

---

## 3. 핵심 격차 9가지

1. **회원 탈퇴·파기 기능이 전혀 없다.** 개인정보보호법 제21조가 요구하는 삭제 요구권에 대응할 코드 경로가 프론트·백엔드 어디에도 없다. `[구현 필요]`
2. **`seller-applications.js`가 사업자등록증 파일과 정산 계좌번호를 하드닝 없이 저장한다.** base64 blob과 원문 계좌번호가 공유 JSON DB 안에 그대로 들어있고, 관리자 조회 API가 걸러내지 않고 그대로 반환한다. `group-booking.js`는 신규 쓰기만 #142로 해결됐고 구버전 인라인 문서는 별도 이관 대상이다. `[구현 필요]`
3. **개인정보보호책임자·사업자등록번호 등 필수 고지가 어디에도 없다.** 개인정보보호법 제30조·전자상거래법이 요구하는 최소 요건이다. 값 자체는 콘텐츠지만 표시할 슬롯은 `[구현 필요]`
4. **원장(ledger)과 삭제 요구권이 구조적으로 충돌하고, 현재 payload에도 개인정보가 있다.** 기존 평문 payload를 마이그레이션·재체인하거나 암호화 후 키 파기하는 설계가 먼저다. 단순히 “해시 원장”이라는 이유로 삭제 예외를 적용할 수 없다. `[구현 필요]`
5. **기업/단체 신청 동의가 서버에서 강제되지 않는다.** UI 체크만으로는 API 직접 호출을 막지 못하며 정책 버전과 동의 시각도 남지 않는다. `[구현 필요]`
6. **공개 API가 계정 ID와 이름, 재판매 판매자 ID를 인증 없이 노출한다.** `/api/state`의 최상위 사용자 목록만이 아니라 모든 중첩 DTO를 검사해 안정 사용자 식별자를 제거하거나 공개 목적과 근거를 확정해야 한다. `[구현 필요]`
7. **입장·기기 데이터가 파기 범위에서 빠져 있다.** 신뢰 기기, 입장 자격, QR 발급·게이트 사용 이력과 티켓 QR 필드를 함께 다뤄야 한다. `[구현 필요]`
8. **본인인증 해시가 사전대입 공격에 취약하다.** 마스킹 값과 비밀키 없는 SHA-256 조합을 HMAC/pepper로 바꾸고 기존 값을 이관해야 한다. 본인인증 모듈은 소셜 간편로그인 보호 경계 밖이므로 독립 작업으로 진행한다. `[구현 필요]`
9. **국외 이전 인벤토리가 미완성이다.** 클라이언트 SDK·서버 벤더 호출·호스팅 및 저장 리전을 실제 배포 환경에서 확인해야 한다. `[조사 필요]`

---

## 4. 무엇을 먼저 고칠까

저장 취약점과 탈퇴 기능 부재, 둘 다 P0다. 굳이 하나를 먼저 고른다면 탈퇴 기능이다 — 위험이 발생하는 방식이 다르기 때문이다.

| | 저장 취약점 (seller-applications.js) | 탈퇴·파기 기능 부재 |
|---|---|---|
| **위험이 발생하는 조건** | 뭔가 잘못돼야 터진다 — 외부 침해, 실수로 남긴 로그, 잘못된 익스포트. 다만 계좌번호가 걸러지지 않고 관리자 API 응답에 그대로 실리므로, 그 API를 호출할 수 있는 누구에게나 이미 노출돼 있다는 점에서 group-booking.js의 예전 문제보다 노출 표면이 넓다. | 아무것도 잘못될 필요 없다 — 사용자가 삭제를 요청하는 순간 바로 미이행 상태가 된다 |
| **이미 있는 완화 장치** | DB 파일 자체에 `0600` 권한, `sellerApplications.manage` ACL로 조회 자체는 게이팅됨 — 그러나 저장 형태 자체(평문 계좌번호, 인라인 blob)는 무방비 | 없음 |
| **별도 데드라인** | 없음 | Apple App Store 가이드라인 5.1.1(v) — 계정 생성을 지원하는 앱은 인앱 계정 삭제가 필수. iOS 앱이 이미 출시돼 있다면 지금 이 순간 심사 위반 상태일 수 있다. |

**결론:** 둘 다 이번 스프린트에서 같이 처리한다. 하나만 먼저 끝낼 수 있다면 탈퇴 기능부터 — 관리자가 수동으로 처리하는 버전이라도, "요청해도 아무 경로가 없는" 지금보다는 낫다. iOS 심사 위반 여부는 별도로 지금 바로 확인할 가치가 있다.

---

## 5. 단계별 실행 계획

TossPayments 연동 때와 같은 방식 — 백엔드 → 웹 → iOS → 하드닝 순서로 단계를 나누되, 사실수집은 0단계로 전체 기간 동안 병행한다.

### Stage 0 — 병행 · 즉시 시작 · 전체 기간 지속
**비즈니스·법무 사실수집.** 담당자 연락처, 사업자등록번호, 보관기간 확정치, 미성년자 정책, 마케팅 동의 여부, 벤더별 위탁계약서 존재 확인. 엔지니어링을 막지 않는다 — 지금 시작해서 가장 오래 걸리는 일이 나중에 병목이 되지 않게 한다.

### Stage 1 — 백엔드 프리미티브 `[구현 필요]`
저장 하드닝 + 삭제 기반 구축
- `seller-applications.js`의 사업자등록증 파일을 `group-booking.js`(#142)와 동일한 패턴으로 `fs.writeFile` + 비공개 디렉토리 + 랜덤 파일명으로 저장, DB에는 참조만 기록
- 정산 계좌번호는 되돌려 쓸 수 있어야 하므로(실제 지급 시 필요) 해시가 아니라 암호화 저장 + 마스킹 표시값으로 전환, 관리자 조회 API 응답에서 원문 필드 제거
- **기존 `sellerApplications` 전수 마이그레이션:** 인라인 사업자등록증을 비공개 파일로 추출하고 기존 계좌번호를 암호화한 뒤, legacy `bizRegistrationDocDataUrl`·평문 `accountNumber`가 DB와 관리자 응답에서 사라졌는지 건수·샘플·재실행으로 검증
- **기존 `groupBookingRequests` 마이그레이션:** 인라인 `businessRegistrationFileUrl`을 동일한 비공개 파일 저장소로 이관하고 legacy 필드를 제거한 뒤 관리자 다운로드·재실행·실패 롤백을 검증
- **판매자 공개 업로드 남용 방지:** 신청 API에 요청자 기준 throttling, 전체·신청별 저장용량 quota, 파일 저장 후 DB 실패 시 롤백, 미완료·만료 파일 정리와 집중 남용 테스트를 추가
- **원장 선행 정제:** 최상위 `actorId`, payload의 buyerId·sellerId·targetUserId와 닉네임, 문의 제목, 법인·기관명, 자유서술 메모를 분류하고 가명화 후 재체인 또는 항목별 암호화와 키 파기 방식을 선택한다. 기존 연결 식별자·평문 정제가 끝난 뒤에만 승인된 최소 필드를 보관 예외로 검토
- **본인인증 해시 전환:** 전화번호·CI/DI 파생값을 키 버전이 있는 HMAC/pepper로 전환하고 기존 해시를 안전하게 재생성한다. 비밀키 관리·회전·이전 버전 비교 기간과 마이그레이션 재실행 검증을 포함하며, 소셜 로그인 코드는 변경하지 않는다.
- 계정 삭제·익명화 백엔드 함수 구현. `users`, `identityVerifications`, `nativeSessions`, `supportThreads`, `operatorAlerts`, `watchlist`, `notificationJobs`, `trustedDevices`, `admissionCredentials`, `qrIssueLogs`, 티켓 `currentQr`, `paymentTransactions`, `resalePools`, 잔액·신뢰점수·제재, 결제·예매·기업신청 연결키를 함께 분류하고, 보관 근거가 확정된 항목만 비식별화해 예외 보관
- 삭제 시작 즉시 활성 네이티브 세션을 폐기하고, 부분 실패 후 재시도해도 같은 결과가 되는 멱등 작업과 감사 결과를 남김
- 판매자 계정의 비활성화·삭제, seller session 무효화, 신청·소유 행사·원장 연결키의 보관/이관 절차를 별도 수명주기로 구현
- 파기·보관 배치 작업 골격 구현
- 관리자의 민감정보 "열람" 자체가 로깅되는지 확인, 안 되면 추가

### Stage 2 — 웹 서피스 `[구현 필요]`
탈퇴 UI + 방침 페이지 재작성
- 마이페이지에 계정탈퇴 진입점 추가
- 기업/단체 신청과 판매자 신청의 동의 체크박스를 실제 `/privacy` · `/terms`에 연결하고 수집항목 안내 문구를 보강. 로그인 UI·동의 문구·링크는 보호 범위이므로 이 계획에서 변경하지 않음
- `POST /api/group-booking/requests`와 `POST /api/seller-applications`에서 동의값·정책 버전을 필수 검증해 동의 시각과 함께 저장. 누락·구버전 직접 호출은 거부
- 공개 `/api/state`의 전체 사용자 ID·이름과 `resalePools[].sellerId`를 포함한 모든 중첩 DTO의 안정 사용자 식별자를 제거하는 것을 기본안으로 하고, 제품상 공개가 꼭 필요하면 공개 목적·범위·근거·사용자 통제를 먼저 확정
- 7절 골격 기반으로 신규 방침·약관 페이지 작성

> **보호된 인증 경계:** 30일 bearer credential의 `localStorage` 저장은 위험으로 등록한다. 목표안은 HttpOnly·Secure·SameSite 쿠키 또는 동등한 보호, 서버 측 폐기·회전, XSS 및 로그아웃 회귀 테스트다. 그러나 세션 코드는 간편로그인 수정 금지 범위이므로 전역 금지가 명시적으로 철회되기 전에는 구현하지 않는다.

> **주의:** "정보주체 권리행사방법" 섹션은 셀프서비스 삭제 기능이 실제로 동작하기 전까지 이메일·고객센터 수동 채널로만 기술한다. 작동하지 않는 셀프서비스를 방침에 먼저 적어두는 게 오히려 더 나쁘다.

### Stage 3 — iOS `[구현 필요]`
네이티브 계정삭제. App Store 가이드라인 5.1.1(v) 대응이 목적이므로 개인정보보호법 일정과 무관하게 별도 데드라인으로 취급한다. 동의 링크도 네이티브에서 동일하게 노출.

### Stage 4 — 하드닝 · 검증 `[구현 필요]`
회귀 테스트 + 자발적 점검
- 신규·기존 판매자 신청과 레거시 단체 신청 모두에서 사업자등록증 원문 blob과 평문 계좌번호가 DB·API 응답에 남지 않는지, 마이그레이션을 재실행해도 안전한지 확인하는 회귀 테스트
- 판매자 신청의 요청 제한·저장 quota·DB 실패 롤백·고아 파일 정리가 반복 대용량 제출을 차단하는지 확인
- 신규 하드닝 경로가 기존 ACL로 실제로 게이팅되는지 확인
- 계정 삭제가 모든 연결 컬렉션과 티켓 QR 필드를 처리하고 활성 세션을 폐기하며, 승인된 보관 예외만 비식별 상태로 남기고, 부분 실패 뒤 재시도해도 완료되는지 확인하는 백엔드·클라이언트 테스트
- 기업/단체·판매자 신청 API가 동의 누락·구버전·UI 우회를 거부하고 정책 버전·동의 시각을 보존하는지 확인
- `/api/state` 비인증 응답의 최상위·중첩 DTO 어디에도 안정 사용자 식별자가 남지 않는지 확인
- 원장 신규 `actorId`와 payload 사용자 참조가 가명화되고 금지된 평문 필드가 없으며, 기존 원장 마이그레이션 후 체인 검증이 통과하는지 확인
- 소비자·판매자 계정 삭제가 각 세션을 폐기하고 관심목록·알림·문의 사본·거래·재판매·위험 프로필·연결 행사를 정책대로 삭제 또는 비식별화하는지 확인
- 자발적 개인정보 영향평가(PIA) 여부 법무 검토
- 쿠키·로컬스토리지 고지 문구 재검토

---

## 6. 작업 분류

실제 정책 판단이 필요한 항목은 임의로 정하지 않고, 코드만으로 착수 가능한 일과 명시적으로 분리했다.

### 지금 가능 `[구현 필요]` — 보호된 인증 경계 밖에서 바로 착수
1. 판매자 신규 저장 하드닝·남용 통제·기존 데이터 마이그레이션과 레거시 단체 신청 문서 이관
2. 기업/단체·판매자 신청 UI와 API의 동의 강제, 정책 버전·동의 시각 저장
3. 공개 `/api/state`의 최상위·중첩 사용자 식별자 제거 또는 공개 목적 결정을 위한 제품 검토
4. 원장 `actorId`·사용자 참조·평문 payload 인벤토리, 정제·재체인 또는 암호화/키 파기 설계
5. 전화번호·CI/DI 파생값의 HMAC/pepper 전환과 기존 해시 마이그레이션(소셜 로그인 코드 제외)
6. 소비자·판매자 계정과 전체 연결 컬렉션을 포함하는 삭제/익명화·세션 폐기·멱등 파기 배치 골격
7. 방침·푸터에 담당자·사업자정보 슬롯 구현
8. 호스팅 리전과 벤더별 클라이언트·서버 통신 경로 실환경 확인
9. 관리자 열람 로깅 여부 확인

### 보호 경계 해제 후 가능 `[보호 영역 결정 필요]`
1. 30일 브라우저 bearer credential을 HttpOnly·Secure 쿠키 또는 동등 구조로 전환하고 폐기·회전·XSS·로그아웃 테스트 추가
2. 로그인 동의 문구·링크 수정이 필요하면 간편로그인 보호 규칙을 먼저 명시적으로 철회하고 별도 PR과 인증 회귀 테스트로 수행

### 결정 필요 `[구현 필요]` — 결정 후 엔지니어링 착수
1. 원장 평문 정제·소거 설계와 보관 예외 최종 승인
2. 보관기간 실제 수치 확정
3. 미성년자 정책 — 가입 차단 / 만14세 이상 허용 / 법정대리인 동의 구축 중 택1
4. 마케팅 동의 범위 — 현재 기능 없음, "해당없음" 확정 여부
5. `seller-applications.js` 계좌번호 보호 방식 — 암호화 알고리즘/키 관리를 KMS로 할지 앱 레벨로 할지
6. 벤더별 처리위탁 vs 제3자제공 구분, 실제 위탁계약서 존재 확인
7. 공개 사용자 이름·안정 ID가 제품에 필요한지와 공개 처리의 적법 근거
8. 입장·기기·QR·게이트 이력의 항목별 보관기간과 탈퇴 시 익명화 범위

### 콘텐츠만 필요 `[구현 불필요]` — 코드 변경 없이 사실만 있으면 됨
1. 사업자등록번호·대표자명·주소·통신판매업 신고번호
2. 개인정보보호책임자 성명·직책·연락처·부서
3. 공개할 보관기간 수치 문구화
4. 미성년자 정책 문구
5. 제3자제공·위탁 목록 최종 확인(코드에 안 보이는 벤더 유무)
6. 정보주체 권리행사 접수 채널(주소·연락처)

---

## 7. 방침 문서 골격

한국 이용자·규제기관이 기대하는 표준 순서. 법률 문구 자체가 아니라, 각 절에 무엇이 들어가고 지금 바로 쓸 수 있는지 표시했다.

| 절 | 내용 | 상태 |
|---|---|---|
| 1. 수집하는 개인정보 항목 및 수집방법 | 저장값뿐 아니라 Google ID 토큰 이메일, 입력·PortOne 응답 전화번호·CI/DI, 환불계좌, 요청 IP처럼 백엔드가 일시 처리하는 원문을 출처·목적·전달처·비저장 또는 메모리 보관기간과 함께 기재. 로그인 식별자·닉네임, 본인인증 해시·마스킹값, 판매자 계정, 결제 거래·재판매 참여, 잔액·신뢰점수·제재, 판매자/기업신청 담당자·사업자서류·정산계좌, 문의·운영자 알림, 관심목록·알림 채널, 기기·입장·QR·게이트 이력도 포함 | 작성 가능 · 원문/저장 구분 필요 |
| 2. 수집 및 이용 목적 | 계정생성/로그인, 본인확인·중복가입 방지, 결제·환불·정산, 공식 재판매, 부정이용 위험관리, 판매자·기업예매 이행, 문의·알림, 신뢰 기기·입장 검증, IP 기반 남용 방지·접근통제. 공개 사용자 식별자는 노출 제거 또는 적법 근거 결정 전 보류 | 일부 보류 · 공개 목적 결정 필요 |
| 3. 보유 및 이용기간 | 전자상거래법 법정 최소치(결제·계약 5년, 소비자불만 3년)를 기본값으로 초안 | 보류 · 결정 필요 |
| 4. 제3자 제공 | 위탁 구조로 정리된다면 "해당없음"이 유력 | 보류 · 법무 확인 필요 |
| 5. 개인정보처리 위탁 | TossPayments(결제), PortOne/다날(본인인증), Google/Kakao/Naver(로그인) | 거의 작성 가능 |
| 6. 국외 이전 | JWKS 조회가 아니라 Google GIS·네이티브 SDK, 기타 벤더의 실제 전송 경로와 호스팅·저장 리전을 확인한 뒤 이전받는자/국가/시기·방법/목적/항목/보유기간/거부방법을 기재 | 보류 · 실환경 조사 필요 |
| 7. 파기 절차 및 방법 | Stage 1의 삭제/익명화 메커니즘이 만들어진 뒤 그 방식대로 기술 | Stage 1 이후 · `[구현 필요]` |
| 8. 정보주체의 권리·행사방법 | Stage 2까지는 이메일/고객센터 수동 채널만 기술 | Stage 2 이후 · `[구현 필요]` |
| 9. 안전성 확보조치 | 0600 파일권한, 관리자 ACL, scrypt 비밀번호 해싱은 기재 가능. 원장의 평문 payload와 복구 가능한 본인인증 SHA-256은 개선 완료 전 안전조치로 과장하지 않음 | 일부 작성 가능 |
| 10. 자동 수집 장치(쿠키 등) | 추적 쿠키·분석스크립트는 없으나 `localStorage`에 30일 bearer credential이 저장되는 목적·기간·위험과 삭제 방법을 정확히 고지 | 작성 가능 · 하드닝 별도 필요 |
| 11. 아동의 개인정보보호 | 미성년자 정책 결정에 따라 문구 확정 | 보류 · 결정 필요 |
| 12. 개인정보보호책임자 | 성명·직책·연락처·부서 | 보류 · 콘텐츠 필요 |
| 13. 권익침해 구제방법 | 개인정보분쟁조정위원회·118 등 표준 안내 | 작성 가능(법무 검토) |
| 14. 고지 의무·시행일·개정이력 | 공개 시점에 실제 시행일 확정 | 작성 가능 |

**이용약관 별도 보강:** 사업자정보(등록번호·주소·대표자명)와 통신판매업 신고번호가 `/terms`에도 빠져있다 — 전자상거래법이 요구하는 별개 항목이라 방침과 별도로 챙겨야 한다.

---

## 8. 추가로 점검할 것

범위를 확정하기 전에 짚고 넘어가야 코드만 봐서는 답이 안 나오는 것들.

- **처리위탁 vs 제3자제공 구분.** TossPayments·PortOne은 위탁 쪽이 유력하지만, 실제 위탁계약서(DPA) 존재 여부는 코드로 확인할 수 없다.
- **고유식별정보 별도 동의.** CI/DI 해시값 처리는 일반 "필수 약관 동의"와 별개의 명시적 동의 화면이 있어야 할 수 있다.
- **자발적 개인정보 영향평가(PIA).** 법적 의무는 아니지만 CI/DI급 데이터를 다루는 규모라면 자문 권고 대상.
- **마케팅 동의(법 제22조의2).** 일반 수집동의와 별개 범주 — 현재 마케팅 발송 기능 자체가 없다는 것을 명시적으로 확정할지 결정 필요.
- **유출 대응 절차(법 제34조).** 사용자 대상 방침과 별개로, 내부 침해대응 매뉴얼 자체가 저장소 어디에도 없다.
- **관리자 "열람" 로깅.** 현재 원장은 상태 변경 이벤트만 기록한다 — 조회 자체도 기록되는지 별도 확인 필요.
- **ACL만으로는 부족.** 기술적 접근통제 외에 정기 접근로그 검토, 임직원 서약, 보안교육 등 관리적 조치도 방침에 과장 없이 기술해야 한다.
- **국외 이전 전수 확인.** Google의 공개 JWKS 다운로드는 개인정보 이전이 아니다. 브라우저 GIS, 네이티브 SDK, Toss·PortOne 등 원격 SDK/API, Vercel 실행·DB·파일 저장 리전을 네트워크·계약·배포 설정으로 각각 확인한다.
- **공개 노출 목적 확인.** `/api/state`의 사용자 ID·이름 공개가 실제 제품 요구가 아니라면 방침으로 정당화하지 말고 응답에서 제거한다.
- **입장 이력 보관 기준.** 신뢰 기기, 입장 자격, QR 발급 로그, 티켓 QR, 게이트 사용 이력을 결제·분쟁 기록과 구분해 최소 보관기간과 파기 방법을 정한다.
- **소비자·판매자 계정 수명주기 분리.** 판매자 계정·세션·소유 행사와 소비자 거래·재판매·잔액·신뢰점수·제재는 목적과 법정 보관 근거가 다르므로 폐쇄·익명화 규칙을 각각 정한다.
- **자동 수집 IP 보관.** in-memory rate-limit Map의 최대 유지시간, 서버 재시작 시 파기, 로그인 접근통제 처리와 외부 프록시 헤더 신뢰 범위를 운영 문서와 방침에 일치시킨다.

---

## 9. 열린 질문

이 계획서를 실행에 옮기기 전에 확정이 필요한 항목.

1. `seller-applications.js`의 계좌번호를 암호화할 것인가 마스킹 표시만 할 것인가 — 정산에 실제로 필요한 값이라 해시로는 안 됨(2절).
2. 보관기간은 법정 최소치를 그대로 쓸 것인가, 분쟁 대응 등을 이유로 더 길게 가져갈 것인가?
3. 미성년자 가입을 아예 막을 것인가, 만14세 이상만 허용할 것인가, 법정대리인 동의 플로우를 구축할 것인가?
4. 개인정보보호책임자는 누구로, 어떤 연락처로 공시할 것인가?
5. Vercel 배포 리전은 고정돼 있는가, 아니면 지금 정해야 하는가?
6. 기존 원장 payload는 재체인 마이그레이션할 것인가, 항목별 암호화 후 키 파기할 것인가?
7. 공개 `/api/state`에 사용자 ID·이름이 필요한가, 즉시 제거할 것인가?
8. 입장·기기·QR·게이트 이력은 각각 얼마나 보관하고 탈퇴 시 무엇을 익명화할 것인가?
9. 간편로그인 보호 규칙을 유지해 세션 bearer 위험을 보류할 것인가, 별도 세션 작업 전에 전역 금지를 명시적으로 철회할 것인가? 본인인증 HMAC 전환은 이 결정과 분리해 진행한다.
10. 판매자 계정 폐쇄 시 소유 행사와 정산·분쟁 기록을 누구에게 이관하고 무엇을 비식별화할 것인가?
11. 요청 IP와 환불계좌 원문의 실제 메모리 보관시간, 로그 유입 여부, Toss 전달 후 파기 시점을 어떻게 검증할 것인가?

---

*TIG 개인정보처리방침 실행 계획서 · 백엔드 코드 전수 조사 및 Fable 5 자문 교차검증 기반 · 법률 자문 대체 불가*
*Artifact: https://claude.ai/code/artifact/f62ff0a0-a373-454a-8983-2e2e064aa093*

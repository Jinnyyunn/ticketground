# 공연장 날씨·실시간 주차 연동 계획서

- 대상: https://github.com/Jinnyyunn/ticketground `main` 브랜치 (커밋 `b90c50d` 기준)
- 스택: Next.js 16 / React 19 / Tailwind v4, 백엔드는 `backend/*.js`(순수 Node, 인메모리 JSON DB) + `server.js`
- 작성일: 2026-08-04
- 범위: 사용자가 티켓 예매를 완료하면(예약 확인 화면·마이페이지 티켓 상세) 공연장 근처 날씨와 실시간 주차 현황을 보여준다.

---

## 1. 목적

예매 완료 후 사용자가 관람 당일 "날씨를 챙겨야 하나", "주차가 가능한가"를 확인하려면 지금은 외부 검색을 따로 해야 한다. 예약 확인 화면(`src/app/reservation/[id]/page.tsx`)과 마이페이지 티켓 상세에 공연장 인근 날씨·실시간 주차 정보를 붙여서, 예매 흐름 안에서 관람 준비까지 끝나도록 한다.

`티켓판매흐름.md`에 이미 "굿즈·F&B·주차 패키지 판매"가 향후 수익 모델로 언급되어 있어(132·270·320·338행), 주차 정보 연동은 이후 주차권 판매 연계로 확장할 수 있는 기반이 된다.

---

## 2. 현재 상태 진단

| 항목 | 현황 |
|---|---|
| 공연장 위치 데이터 | `backend/catalog-data.js`의 venue blueprint에 `address`(문자열 주소)만 존재, 위경도·기상청 격자 좌표 없음. `src/types/index.ts`의 `venue` 필드도 전부 `string`(이름만), 좌표 필드 자체가 없음 |
| 외부 API 연동 사례 | `backend/bootpay.js`(결제), `backend/identity.js`(포트원 본인인증), `backend/social-oauth.js`류가 이미 있음 — 전부 **"env var 미설정 시 mock, 설정 시 실 API 호출"** 패턴(`bootpay.js:15-16` `isBootpayConfigured()`)을 따름. 날씨·주차도 이 패턴을 그대로 재사용 가능 |
| 예약 확인 화면 | `src/app/reservation/[id]/page.tsx` — 공연 정보(장소·일시·좌석)는 이미 렌더링 중(91·97·173행에서 `reservation.venue` 사용). 새 위젯을 끼워 넣을 위치가 명확함 |
| 환경변수 컨벤션 | `.env.example`/`.env.local.example`에 `TIG_` 접두사로 공개용/비밀 분리(공개 키는 `.env`, 비밀 키는 `.env.local`) — 새 API 키도 동일 규칙 적용 |

**결론**: 순수 신규 기능이며 기존 코드와 충돌 없음. 다만 "공연장 좌표"라는 데이터가 아예 없으므로 이 계획의 절반은 백엔드 연동이 아니라 **데이터 모델 확장**이다.

---

## 3. 외부 API 선정안

### 3-1. 날씨 — 기상청(KMA) 단기예보 조회서비스
- 공공데이터포털 제공, 무료, 국내 좌표 커버리지 우수.
- 위경도가 아니라 **기상청 격자좌표(nx, ny)** 를 요구 — 위경도→격자 변환 공식(또는 공공데이터포털 제공 변환 유틸)이 별도로 필요.
- 예보는 3시간 단위 갱신이라 실시간성은 필요 없음 → 캐싱에 유리.
- 대안: OpenWeatherMap(위경도 직접 지원, 해외 API라 국내 정확도는 기상청보다 낮음) — 격자 변환을 피하고 싶으면 이쪽도 후보.

### 3-2. 주차 — 지자체별 실시간 주차장 정보 API
- 서울은 서울열린데이터광장의 "서울시 주차장 안내 정보"(실시간 가용면수 포함)가 있으나, **지자체마다 API가 다르고 전국 통합 API는 없음** — 이게 이 계획의 가장 큰 리스크(§7 참고).
- 1차 범위는 서울 소재 공연장(현재 카탈로그 21곳 중 다수가 서울)으로 한정하고, 나머지는 "실시간 정보 미제공 · 인근 주차장 목록만 표시"로 단계적으로 축소.
- 공연장 자체 주차장이 아니라 **반경 내 공영/민영 주차장 목록 + 가용 여부**를 보여주는 형태(정확히 "그 공연장 주차장" 데이터가 존재하지 않는 경우가 많음).

---

## 4. 데이터 모델 변경

1. `backend/catalog-data.js`의 venue blueprint(`arenaVenueBlueprint`/`theaterVenueBlueprint`, 그리고 4개 하드코딩 venue)에 `coordinates: { lat, lng }` 필드 추가. 21개 공연장 전부 실좌표 입력 필요(수작업, 주소는 이미 있으므로 좌표만 채우면 됨).
2. 기상청 격자(nx, ny)는 위경도에서 서버에서 변환해서 구하거나, 정확도를 위해 벤뉴별로 같이 하드코딩.
3. `src/lib/ticketground-api-types.ts`의 `ApiEvent`에 `venueCoordinates?: { lat: number; lng: number }` 같은 필드 추가하고 `apiEventSchema`(zod)에도 반영 — 기존 PR(#125)에서 `ApiSeatMapPosition`을 추가한 것과 같은 방식.

---

## 5. 백엔드 설계

```
backend/
  weather.js   (신규) — createWeatherBackend({ httpError, now }) 형태로 기존 모듈 컨벤션 따름
  parking.js   (신규) — createParkingBackend({ httpError, now })
```

- `bootpay.js`와 동일하게 `isWeatherConfigured()`/`isParkingConfigured()`로 env var 유무 판정, 미설정 시 고정 mock 데이터 반환(개발·데모 환경에서 그대로 동작해야 함 — 이 저장소는 `AGENTS.md`에 명시된 "Website Reverse-Engineer Template" 데모이므로 실키 없이도 화면이 비어보이면 안 됨).
- 신규 라우트(`backend/api-router.js`에 추가):
  - `GET /api/venues/:venueId/weather`
  - `GET /api/venues/:venueId/parking`
- **캐싱 필수**: 공공 API는 호출량 제한이 있음. 날씨는 TTL 10~30분, 주차는 TTL 1~2분으로 인메모리 캐시(간단히 `Map<venueId, { data, expiresAt }>`) — 매 예약 확인 페이지 방문마다 외부 API를 직접 때리지 않도록.
- 환경변수(`.env.local.example`에 추가): `TIG_KMA_API_KEY`, `TIG_SEOUL_OPEN_DATA_API_KEY` (또는 선정한 주차 API에 맞는 키).

---

## 6. 프론트엔드 설계

- 신규 컴포넌트 `src/components/ticketing/venue-weather-parking-panel.tsx` — `venue-seat-map.tsx`처럼 자체 데이터 페칭(useEffect + fetch) 후 로딩/에러/미제공 3-state 처리.
- 배치 위치: `src/app/reservation/[id]/page.tsx`의 공연 정보 카드 하단(§2 확인한 91·97·173행 근처).
- 마이페이지 티켓 상세(`src/components/mypage/backend-ticket-panel.tsx`)에도 동일 컴포넌트 재사용.
- 날씨: 아이콘 + 기온 + 강수확률 정도의 요약 카드. 주차: "인근 주차장 N곳 · 실시간 가용 M면" 요약 + 목록(상위 3곳) 펼침.
- API 미제공 지역(§3-2)은 "이 공연장은 실시간 주차 정보를 제공하지 않습니다" 안내로 자연스럽게 축소 — 빈 화면이나 에러로 보이면 안 됨.

---

## 7. 리스크 및 미해결 사항

| # | 리스크 | 메모 |
|---|---|---|
| 1 | 전국 공연장 실시간 주차 API 부재 | 지자체별 API가 제각각이라 완전한 전국 커버리지는 사실상 불가능. 서울 우선 + 나머지는 정적 안내로 축소하는 방향을 이 계획의 전제로 함 |
| 2 | 좌표 데이터 수작업 | 21개 벤뉴 좌표를 정확히 입력해야 날씨·주차 API가 의미 있는 값을 반환함. 잘못된 좌표는 엉뚱한 지역 날씨를 보여주는 사고로 이어짐 — §4 작업 시 실제 지도 서비스로 좌표 교차검증 필요 |
| 3 | API 키 발급 필요 | 기상청·서울열린데이터광장 모두 사전 신청 절차가 있어 실 연동 전까지는 mock으로만 개발·검증 가능 |
| 4 | 캐시 미적용 시 API 쿼터 소진 | §5의 TTL 캐싱을 건너뛰고 바로 붙이면 트래픽 증가 시 공공 API 일일 호출 한도를 빠르게 소진할 수 있음 |

---

## 8. 단계별 로드맵

1. **Phase 1 (데이터 모델)**: venue 좌표 필드 추가, 21개 공연장 좌표 입력, `ApiEvent`/zod 스키마 반영.
2. **Phase 2 (백엔드, mock)**: `weather.js`/`parking.js`를 mock 모드로 구현 + 라우트 연결, 프론트 없이 API 응답 형태부터 확정.
3. **Phase 3 (프론트)**: `venue-weather-parking-panel.tsx` 구현, 예약 확인·마이페이지에 배치, 로딩/에러/미제공 상태 QA.
4. **Phase 4 (실 API 연동)**: 기상청·주차 API 키 발급 후 실 연동, TTL 캐싱 적용, 서울 외 지역 폴백 문구 검증.
5. **Phase 5 (확장, 선택)**: 주차권 사전 판매 연계(`티켓판매흐름.md` 언급 사항과 연결).

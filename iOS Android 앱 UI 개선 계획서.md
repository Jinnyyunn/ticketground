# iOS·Android 앱 UI 개선 계획서

- 대상: TicketGround iOS 앱(`ios/TicketGroundApp`, SwiftUI, bundle `kr.ticketground.app`)과 Android 앱(`android/TicketGroundApp`, Kotlin/Jetpack Compose, `kr.ticketground.app.dev`/`kr.ticketground.app`). 관리자 콘솔, 판매자 콘솔은 범위 밖.
- 작성일: 2026-08-17
- 근거: 코드 정독뿐 아니라 iOS는 시뮬레이터를 실제로 빌드·설치해 탭 조작하며 화면을 실사했고, Android는 실기기/에뮬레이터에 설치해 실제 조작으로 검증했다(§7 참고). 이후 fable5를 설계 자문(advisor)으로 투입해 우선순위·로드맵·화면별 방향에 대한 2차 의견을 받아 반영했다. **이 문서는 계획서이며 코드를 변경하지 않는다** — 승인 후 이 프로젝트의 표준 작업 방식(브랜치 → PR → 검증 → 병합)으로 단계별 구현 라운드를 진행한다.
- TossPayments 결제, NICE 본인인증, 좌석 선택/홀드 로직 등 기존 백엔드 계약과 핵심 트랜잭션 로직은 이번 범위에서 바꾸지 않는다. Apple Pay/Google Pay 등 결제 "수단" 추가는 신규 기능이라 §6 미결정 사항에서 별도로 다룬다.

---

## 1. 배경과 목적

운영자가 실제 iOS/Android 앱을 보고 "개발하다 만 것 같다", "너무 비어 보인다"고 지적했다. 두 앱 모두 기존 웹사이트의 기능·플로우를 그대로 네이티브로 옮기는 방식으로 만들어졌다.

이번 조사 결과, "비어 보인다"는 인상은 하나의 원인이 아니라 성격이 다른 두 가지 문제가 겹친 것으로 확인됐다.

1. **실제로 끊겨 있거나 완전히 비어 있는 화면·버튼** — 디자인 방향과 무관하게 버그로 봐야 하는 것들 (§2)
2. **네이티브 iOS/Android 화면 구성요소(탭바, 헤더, 스켈레톤 로딩, 햅틱, 이미지 밀도 등)를 쓰지 않고 웹 화면 구조를 그대로 컨테이너에 얹은 구조적 문제** (§3)

두 플랫폼의 진행 상태도 다르다. iOS는 좌석 선택(핀치줌·대기열·홀드), TossPayments 네이티브 SDK 결제, 티켓 라이프사이클(취소·재판매·QR 발급), 다크모드·Dynamic Type 등 상당 부분이 견고하게 구현돼 있고 "실패 시 가짜 데이터로 채우지 않는" 방어적 설계 철학까지 갖추고 있다 — 다만 그 방어적 설계가 화면을 비워 보이게 만드는 부작용을 내고 있다. Android는 결제(TossPayments SDK)와 좌석도 로직은 구현돼 있지만, **로그인 화면 자체가 앱에 없다** — 이 하나가 Android의 가장 큰 구조적 공백이다.

---

## 2. 즉시 수정 대상 (디자인 방향 결정을 기다릴 필요 없음)

fable5 자문 결과, "버그부터 고치고 미학은 나중에"로 한 줄로 줄 세우면 안 된다는 지적을 반영했다 — 공수가 거의 안 드는 미학적 결함(예: Android 카드 이미지, 좌석 범례 색상)은 데이터가 이미 있는 순수 렌더링 작업이라 버그 수정과 같은 라운드에 묶어도 리스크가 없다. 아래 목록은 그 기준으로 구성했다.

### iOS

**2-1. 홈 히어로 배너 "예매하기" CTA가 죽은 링크 (최우선)**

`DiscoveryFeaturedSection`(`DiscoverySectionViews.swift:123-245`)의 히어로 배너 CTA를 탭하면 `queue:iu-world-tour`라는 **내부 라우트 ID가 화면에 그대로 노출**되는 플레이스홀더로 이동한다(`DiscoveryRouteView.swift:34-49` default case). 앱에서 가장 크고 눈에 띄는 버튼이 막다른 길이자 내부 식별자 노출이라는 경미한 정보 노출이기도 하다. fable5도 이 항목을 전체 목록 중 "오늘 당장 고쳐야 할 최우선"으로 별도 지목했다.

**2-2. "찜" 탭이 데모 모드에서 완전히 빈 플레이스홀더**

하단 탭 4개 중 하나가 fixture(데모) 모드에서 raw 라우트 ID(`watchlist`)가 타이틀로 노출되는 빈 화면이다. 최소한 empty-state 뷰(아이콘 + 안내 문구)로 교체한다.

**2-3. "마이페이지" 탭이 "전체메뉴"와 동일 화면 재사용**

`DiscoveryRouteView.swift:24`의 `case .menu, .mypage: DiscoveryMenuView()` — fixture 모드에서 4대 주 탭 중 하나가 독립 화면 없이 햄버거 메뉴와 완전히 같은 화면을 재사용한다.

**2-4. 로그인 화면 하단 60%가 완전 공백**

소셜 로그인 버튼 3개(Google/Kakao/Naver) 이후 화면 하단이 통째로 빈 흰 여백이다 — 스크린샷상 "미완성" 인상을 가장 강하게 주는 화면. 구체적 채움 방향은 §5.

**2-5. 햅틱 피드백 전무**

전체 소스 grep 결과 `UIImpactFeedbackGenerator` 등 햅틱 관련 코드가 0건. 좌석 선택, 구매 성공, QR 발급, 찜 토글에 SwiftUI `.sensoryFeedback`(또는 `UIImpactFeedbackGenerator`)를 추가한다 — 엔지니어링 비용이 낮다.

**2-6. 모든 로딩이 스피너뿐**

`TicketgroundStateSurfaces.swift:3-24`, `TicketgroundMedia.swift:452-465` 등 로딩 표현이 전부 `ProgressView()` 하나뿐이다. SwiftUI 기본 기능인 `.redacted(reason: .placeholder)`로 목적지 레이아웃 모양을 딴 스켈레톤으로 교체한다. fable5는 이 항목을 "공수 대비 체감 효과가 가장 큰 항목"으로 별도 강조했다.

### Android

**2-7. 시스템 뒤로가기가 앱을 통째로 종료 (실기기 재현 확인)**

`AppDestination.kt` + `CustomerApp.kt`의 수작업 sealed class 상태 전환에는 백스택이 없다 — 이벤트 상세/좌석도/결제 화면에서 시스템 뒤로가기를 누르면 화면 전환이 아니라 앱이 통째로 런처로 튕긴다. 전면 `NavHost` 마이그레이션은 아키텍처 변경이라 §4 Phase 2로 미루되, **우선 기존 상태 머신에 수동 push/pop 스택만 추가하는 임시 패치를 즉시 적용**한다 — fable5는 "낮은 blast radius로 사용자 체감 크래시부터 막아야 한다"고 조언했다.

**2-8. 찜 화면과 마이페이지가 동일한 "미로그인" 상황을 다른 톤으로 처리**

`CustomerApp.kt:136-142`의 `loadWatchlist()`는 미로그인 실패를 `AsyncContent.Error`("문제가 발생했어요" + 재시도)로 처리하는 반면, `144-158`의 `loadAccount()`는 같은 원인을 "로그인이 필요합니다"라는 차분한 안내로 특별 처리한다. 동일 원인에 다른 UX — 하나의 차분한 안내 상태로 통일한다.

**2-9. 이벤트 카드에 포스터·뱃지·캐스트 정보 미표시 (데이터는 이미 있음)**

`EventCard`(`CustomerScreens.kt:287-298`)는 제목·장소·판매상태 텍스트 3줄뿐이지만, `CatalogEvent.image`/`.badge`/`.casts`(`PublicApiModels.kt:45-48`) 필드는 API 응답에 이미 존재한다. 백엔드 변경 없이 순수 렌더링 작업만으로 해결 가능 — 즉시 착수한다.

**2-10. 좌석도 범례에 색상 스와치 없음**

`GraphicalSeatMapScreen.kt:148-152`의 범례가 텍스트만 나열돼 있어 색이 무슨 상태인지 시각적으로 매칭이 안 된다. 색상 dot을 추가한다.

### 공통

**2-11. 두 플랫폼 모두 로딩이 스피너뿐** — Android는 shimmer 브러시(Compose)로 교체(iOS 2-6과 동일 취지).

---

## 3. 두 플랫폼 공통 근본 원인 (구조적 — "웹을 감싼 컨테이너"로 느껴지는 이유)

**iOS**: 커스텀 웹스타일 헤더(로고 + 검색 pill + 햄버거, `TicketgroundNavigation.swift:3-72`)가 네이티브 내비게이션 바를 완전히 대체하고 있고, `ContentView.swift:112`에서 `.toolbar(.hidden, for: .navigationBar)`를 전역으로 설정해 시스템 내비게이션 바 자체를 꺼버렸다. 하단 탭바도 네이티브 `TabView` 대신 커스텀 `HStack`(`TicketgroundNavigation.swift:99-144`)이다. 감사·fable5 자문 모두 이 항목을 "앱 전체가 웹사이트를 감싼 컨테이너처럼 느껴지는 가장 근본적인 원인"으로 지목했다.

**Android**: `androidx.navigation.compose`와 `material3.adaptive.navigation.suite`가 의존성으로는 선언돼 있지만 실제로는 한 번도 쓰이지 않는다(`NavHost`/`rememberNavController` grep 결과 0건) — 손으로 만든 sealed class 상태 머신이 내비게이션을 대신한다. 더 근본적으로, `KeystoreSessionVault.store()` 호출 지점이 앱 전체에 0건이다 — **로그인된 세션을 앱으로 들여올 경로(딥링크/App Link 포함) 자체가 없다.** 이는 "버그"라기보다 "로그인 기능 자체가 아직 없다"에 가깝다.

fable5는 이 두 가지를 하나의 "세션 핸드오프 문제"로 뭉치면 안 된다고 지적했다 — **iOS는 이미 독립된 네이티브 소셜 로그인이 있고 로그인 이후 화면들이 미완성인 문제**(기능 불완전)이고, **Android는 로그인이라는 기능 자체가 존재하지 않는 문제**(기능 부재)로 성격이 다르다. 두 문제를 같은 아키텍처 작업으로 묶어 계획하면 안 된다.

---

## 4. 단계별 로드맵

iOS와 Android는 **실행은 독립적으로, 디자인 언어는 공통으로** 진행한다 — empty-state 패턴, 스켈레톤 모양·모션, 햅틱 매핑, 카드 이미지 처리 규칙을 한 번만 정의해 양쪽에 동일 적용한다(fable5 권고). iOS의 Phase 2(TabView 도입)는 백엔드 의존성이 없어 빠르게 끝낼 수 있는 반면 Android의 Phase 2(로그인 아키텍처)는 제품 결정이 선행돼야 하는 긴 작업이라, 두 플랫폼을 억지로 같은 속도로 맞추지 않는다.

### Phase 1 — 빠르고 체감 크고 저위험 (수일, 구조 결정 불필요)
§2의 모든 항목(2-1 ~ 2-11)이 이 단계에 해당한다. 별도 아키텍처 결정이나 §6 미결정 사항 확정을 기다리지 않고 바로 착수 가능하다.

### Phase 2 — 구조적 (수주, 아키텍처 변경)
- iOS: 네이티브 `TabView` 도입 + 전역 `.toolbar(.hidden, for: .navigationBar)` 제거, 시스템 내비게이션 바 복원 — "웹처럼 보이는" 가장 큰 원인이므로 이 Phase 안에서도 우선순위를 앞에 둔다.
- iOS: 로그인 화면 콘텐츠 보강 (§5)
- Android: `androidx.navigation.compose` 기반 실제 `NavHost` 마이그레이션 — Phase 1의 임시 백스택 패치를 정식 대체. 기존 `AppEnvironment`/계약 검증 테스트가 화면 전환 순서나 싱글톤 상태를 가정하고 있을 수 있어 별도 회귀 테스트를 동반한다(§6-6).
- Android: 로그인 화면 + 세션 수립 경로 신설 — §6-1 결정 후 착수.
- Android: FCM 수신 파이프라인(`FirebaseMessagingService` 서브클래스 + `NotificationChannel`) — 현재 토큰 등록(발신)만 있고 수신·표시 경로가 없다. 제품 결정 없이 바로 착수 가능.
- Android: 좌석도 제스처 입력(핀치/팬) 추가 — 기존 텍스트 버튼 7개는 접근성(스위치 컨트롤 등) 대체 수단으로 **남겨두고** 제스처를 추가하는 방향으로 한다(버튼을 완전히 제거하면 접근성 회귀 위험, fable5 지적).

### Phase 3 — 신규 네이티브 기능 (폴리시가 아니라 기능 자체 추가)
- Apple Wallet/PassKit (iOS), Google Wallet 상당 기능 검토 (Android) — QR 발급 로직은 이미 있음
- Apple Pay / Google Pay — TossPayments SDK가 지원
- 위젯(iOS WidgetKit / Android 홈 화면 위젯) — 오늘의 공연, D-day, 보유 티켓 등
- Universal Links(iOS) / App Links(Android) — 웹 링크로 앱을 열 수 있게
- 로그인·결제 단계 생체인증 게이트(Face ID / BiometricPrompt) — 현재는 "기기 신뢰 등록"에만 국한된 인프라를 재사용
- 네이티브 공유(ShareLink / Intent.ACTION_SEND)
- 첫 실행 온보딩 플로우
- 선제적 푸시 사전 동의(soft-ask) 화면 — 시스템 권한 프롬프트 전에 맥락 설명 (Apple 가이드라인 권장 패턴)
- Android: WorkManager 기반 백그라운드 동기화
- 앱 아이콘 배지(뱃지 카운트)
- 각 신규 기능에 최소한의 이벤트 계측 동반 — 투자 효과를 나중에 확인할 수 있도록(fable5 제안)

---

## 5. "비어 보이는" 화면의 구체적 채움 방향

**iOS 로그인 화면 하단 공백**: 온보딩성 캐러셀 같은 과설계는 피하고(그건 Phase 3 영역), 위에서 아래로 — ① 홈 히어로 배너와 톤을 맞춘 브랜드 히어로 아트, ② 기존 소셜 버튼 3개(변경 없음), ③ 아이콘을 곁들인 신뢰 신호 카피 3줄(예: "실시간 좌석 확보", "안전한 결제", "빠른 티켓 확인"), ④ 하단에 약관/개인정보처리방침 링크 + 앱 버전. 국내 티켓 마켓플레이스 관례상 이 자리는 추가 액션보다 신뢰 신호에 쓰는 편이다.

**Android 이벤트 카드**: 데이터는 이미 API 모델에 있으므로 순수 렌더링 작업 — 포스터 썸네일(로딩 중 shimmer, URL이 비어 있을 때는 그라디언트+이니셜 폴백으로 대체하고 절대 빈 칸으로 두지 않음) + 제목 + 장소/일시 + 상태 뱃지("예매오픈"/"마감임박") + 선택적 캐스트 칩. iOS의 랭킹/디스커버리 카드가 이미 검증한 패턴을 그대로 따른다.

**양 플랫폼 로딩 상태**: 중앙 스피너 대신 목적지 레이아웃 모양을 딴 스켈레톤(회색 카드 + shimmer 스윕)을 홈 피드, 티켓 리스트, 좌석도 최초 로딩, 검색 결과 전체에 적용한다. fable5가 "공수 대비 체감 효과가 가장 큰 항목"으로 별도 강조한 항목이다.

---

## 6. 미결정 사항 (사람 결정 필요 — 착수 전 확정)

1. **Android 로그인/세션 수립 방식 (이 계획서에서 가장 큰 미결정 사항)** — (a) iOS처럼 독립 네이티브 소셜 로그인(빠르지만 "웹 세션 이어받기"는 아님) (b) 기존 웹 세션에서 QR/토큰 방식으로 세션을 넘겨받는 방식(카카오톡·토스 스타일 UX, 비용이 크고 재전송·남용 방지 등 별도 보안 검토 필요) (c) 둘 다. 이 결정 전에는 Android Phase 2의 로그인 관련 작업에 착수할 수 없다.
2. **Apple Pay / Google Pay 추가 여부** — TossPayments 네이티브 SDK가 지원해 엔지니어링 리스크는 낮지만, Apple/Google 가맹점 등록·수수료 등 사업적 결정이 선행돼야 한다. NICE 본인인증과는 결합되지 않는 별개 플로우로 판단되나, 착수 전 1회 확인을 권장한다(fable5).
3. **Apple Wallet/PassKit 범위** — 정적 `.pkpass` 내보내기만 할지, 게이트 변경 등 실시간 패스 업데이트를 지원하는 전체 passes 웹서비스(백엔드 신규 엔드포인트 필요)까지 할지. Pass Type ID 인증서를 Apple Developer에 등록해야 해 착수 리드타임이 있다 — 범위 결정이 선행돼야 일정 산정이 가능하다.
4. **로그인/결제 단계 생체인증 게이트 적용 여부** — "기기 신뢰 등록"용 인프라(App Attest/LAContext, BiometricPrompt)는 이미 있지만, "기기 등록"과 "결제 승인"은 Secure Enclave 키 바인딩 관점에서 의미가 다를 수 있어 결제 경로에 연결하기 전 별도 보안 리뷰를 권장한다. 간편로그인 관련 코드는 이 저장소의 보호 대상 인증 경계(`PROTECTED_AUTH_PATTERNS`)에 해당할 가능성이 높아, 착수 시 반드시 집중 인증 테스트를 동반해야 한다.
5. **Universal Links(iOS) 활성화** — 웹 도메인에 `apple-app-site-association` 파일을 호스팅해야 해 웹/CDN 배포 담당과의 협업이 필요하다(이 저장소 단독 작업이 아니다).
6. **Android 백스택 임시 패치 vs `NavHost` 전면 마이그레이션 시점** — 임시 패치(Phase 1)로 우선 크래시부터 막고, 전면 마이그레이션(Phase 2)은 기존 `AppEnvironment`/계약 검증 테스트에 대한 별도 회귀 테스트와 함께 독립적으로 진행한다 — 두 작업을 한 번에 서두르지 않는다.

---

## 7. 조사 방법 (요약)

- **iOS**: Xcode 시뮬레이터(iPhone 17 Pro, iOS 26.5)에 앱을 실제로 빌드·설치한 뒤 탭 조작으로 화면을 실사하며 스크린샷 10장을 확보했다(홈/로그인/전체메뉴/찜/마이페이지/검색/다크모드 등).
- **Android**: `assembleDevDebug`로 빌드해 에뮬레이터(`kr.ticketground.app.dev`)에 설치 후 `adb shell input tap/swipe`로 실제 조작했다. `MainActivity.kt:32-35`의 `FLAG_SECURE`(의도된 보안 경계, 변경하지 않음)로 인해 `screencap`이 검은 화면만 반환해 대신 `uiautomator dump`로 화면상의 실제 텍스트·좌표·요소를 추출해 코드와 대조 검증했다.
- 두 플랫폼 전체 소스 코드에 대해 grep으로 햅틱/Wallet/공유시트/TabView/위젯/Apple Pay 등 네이티브 패턴 존재 여부를 정량 확인했다.
- fable5를 설계 자문으로 투입해 우선순위·로드맵·화면별 방향에 대한 2차 의견을 받아 §1, §3~6에 반영했다. fable5는 "P0 버그가 미학 이슈보다 우선"이라는 초기 틀에 대해, 공수가 거의 안 드는 미학적 결함은 버그와 같은 라운드(Phase 1)에 묶어야 한다는 점과, iOS·Android의 "세션 핸드오프" 문제를 하나로 뭉치면 안 된다는 점을 직접 지적했고 이를 그대로 반영했다.

---

## 8. 다음 단계

이 계획서는 코드를 변경하지 않는다. 승인되면 §4 Phase 1부터 이 프로젝트의 표준 작업 방식(브랜치 → PR → 검증 → 병합)으로 순차 착수한다. §6의 1~4번 미결정 사항은 각각 해당하는 Phase 2/3 항목 착수 전 확정이 필요하다.

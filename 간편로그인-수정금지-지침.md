# 간편 로그인(카카오/네이버/구글) 영역 — 무단 수정 금지 지침

## 이 문서를 왜 만들었나

간편 로그인이 여러 차례 연속으로 고장났다가 고쳐지기를 반복했습니다. 실제 서비스 담당자(오너)가 매번 `http://localhost:5501`에서 직접 버튼을 눌러 테스트하는데, 그때마다 실제 로그인이 아니라 QA mock으로 강제 전환되어 "간편 로그인이 안 된다"는 신고가 반복됐습니다. 이 문서는 **같은 실수를 다시 만들지 않기 위한 규칙**입니다.

**핵심 규칙: 아래 "간편 로그인 영역" 파일은, 작업 지시가 명시적으로 간편 로그인/소셜 로그인/OAuth를 언급하지 않는 한 절대 건드리지 마세요.** 다른 UI 개선, 리팩터링, 홈 화면 작업 등을 하다가 우연히 이 파일들을 고치고 싶어지더라도 하지 말고, 정말 필요하면 별도로 사용자에게 확인부터 받으세요.

## 간편 로그인 영역 (건드리면 안 되는 파일)

- `src/components/ticketing/social-login-buttons.tsx` (카카오/네이버 버튼)
- `src/components/ticketing/google-sign-in-card.tsx` (구글 버튼)
- `src/components/ticketing/login-panel.tsx`
- `src/components/ticketing/login-session-panel.tsx` (로그인 후 닉네임 설정 UI)
- `src/app/auth/social-config/route.ts`
- `src/app/auth/google-config/route.ts`
- `src/lib/auth-preview-host.ts`
- `backend/social-oauth.js`, `backend/social-oauth-config.js`
- `.env`/`.env.local`의 `TIG_KAKAO_*`, `TIG_NAVER_*`, `TIG_GOOGLE_*`, `TIG_AUTH_FORCE_PROVIDER`, `*_QA_MOCK_ENABLED` 관련 변수 — 어느 파일에 뭘 넣는지는 아래 "키 값 위치" 절 참고

## 지난번에 실제로 있었던 사고 (재발 방지용 기록)

커밋 `ed2d2fd`에서 `src/lib/auth-preview-host.ts`를 추가하면서, **요청 Host가 `localhost`/`127.0.0.1`/사설 IP(RFC1918)/Tailscale 대역이면 실제 카카오·네이버·구글 키가 설정되어 있어도 무조건 QA mock 로그인으로 강제 전환**하는 로직이 들어갔습니다 (`social-config/route.ts`, `google-config/route.ts`의 `preferMock`).

문제는 오너가 실제로 테스트하는 환경이 바로 `http://localhost:5501` 라는 점입니다. 즉, 이 로직 때문에 실제 OAuth 키를 아무리 정확히 설정해도 오너 환경에서는 절대 실제 로그인이 동작하지 않았고, mock이 인증 없이 바로 데모 세션을 가져오는 바람에 "로그인 버튼을 눌렀는데 곧바로 닉네임 설정 창이 뜬다"는 증상까지 동시에 발생했습니다.

**당시 임시 조치**: 코드에 이미 내장되어 있던 탈출구 `TIG_AUTH_FORCE_PROVIDER=1`을 로컬 `.env`에 추가해 mock을 끄고 실제 로그인이 동작하도록 복구했었습니다. 이 값은 `.env`에만 있고 git에는 커밋되지 않아서, 체크아웃이 새로 생길 때마다 이 문제가 다시 재발했습니다. **아래 "2026-07-08 최종 조치"에서 이 임시방편을 코드 수정으로 대체했습니다 — 더 이상 `TIG_AUTH_FORCE_PROVIDER`에 의존하지 않아도 됩니다.**

## 2026-07-08 최종 조치 — 근본 원인 코드 수정 완료 (하나도 빠짐없이 기록)

이번엔 `TIG_AUTH_FORCE_PROVIDER` 같은 임시방편이 아니라 **코드 자체를 고쳤습니다.** 아래는 실제로 무엇을 했는지 빠짐없는 기록입니다.

### 1. 근본 원인 코드 수정 (커밋 `f40ed34`)

`src/app/auth/social-config/route.ts`, `src/app/auth/google-config/route.ts`에서 `preferMock`이 `mockEnabled`(호스트/`NODE_ENV` 기반 판단)의 단순 별칭이었던 게 문제였습니다. 이제 `preferMock`은 명시적 QA 강제 플래그(`TIG_SOCIAL_QA_MOCK_ENABLED`/`TIG_GOOGLE_QA_MOCK_ENABLED`)만 반영하고, 호스트나 `NODE_ENV`는 더 이상 `preferMock`에 영향을 주지 않습니다. `mockEnabled`(호스트 기반 감지)는 "키가 아예 설정 안 됐을 때"의 폴백 조건으로만 남아 있습니다. **이 로직을 다시 "호스트가 미리보기면 무조건 mock"으로 되돌리지 마세요** — 위 "지난번에 실제로 있었던 사고"가 재발합니다.

### 2. 테스트 3개 수정 (같은 커밋 `f40ed34`)

`tests/social-login-page-flow.test.mjs`, `tests/google-auth-login.test.mjs`, `tests/auth-preview-host-boundary.test.mjs`가 예전에는 "설정되어 있어도 호스트가 미리보기면 mock이 이긴다"는 **틀린 동작을 정답으로 assert**하고 있었습니다(이게 이 버그가 계속 재발한 실제 이유 중 하나 — 누군가 고쳐도 이 테스트들이 다시 "틀리게" 만들었을 겁니다). 지금은 "설정되어 있으면 호스트와 무관하게 실제 로그인이 이긴다"로 assert가 고쳐져 있습니다. **이 세 테스트의 assert를 다시 예전 방향으로 되돌리지 마세요.**

### 3. 키 값 저장 위치 분리 + 실제 값 채움 (커밋 `0ad7a7c`, `a496d93`, `f7e58b2`, `d7e3ada`)

바로 위 "키 값 위치" 절에서 설명한 `.env`(공개, 커밋) / `.env.local`(비밀, 미커밋) 분리를 실제로 적용했고, 오너에게 받은 실제 값을 채웠습니다:

- **`.env`에 채워진 실제 공개 값**: `TIG_KAKAO_REST_API_KEY`, `NEXT_PUBLIC_KAKAO_JAVASCRIPT_KEY`(현재 코드에서는 미사용, 추후 대비용), `TIG_NAVER_CLIENT_ID`, `TIG_GOOGLE_CLIENT_ID`/`NEXT_PUBLIC_GOOGLE_CLIENT_ID`, `NEXT_PUBLIC_GOOGLE_ALLOWED_ORIGINS=http://localhost:5501,http://127.0.0.1:5501,http://100.67.223.117:5501`. 전부 git에 커밋되어 있어서 새 체크아웃에도 그대로 존재합니다 — **이 값들은 다시 물어볼 필요 없습니다.**
- **`.env.local`에 채워진 실제 비밀 값**: `TIG_KAKAO_CLIENT_SECRET`, `TIG_NAVER_CLIENT_SECRET`. `TIG_KAKAO_CLIENT_SECRET_REQUIRED`는 처음엔 시크릿이 없어서 임시로 `0`으로 뒀다가, 실제 시크릿을 받은 뒤 `1`(정상 기본값)로 되돌렸습니다. **`.env.local`은 git에 없으므로, 이 서버가 뜨는 머신이 바뀌면(새 체크아웃 등) 이 값들은 사라집니다 — 그건 버그가 아니라 비밀 값의 당연한 성질입니다.** 값이 없어지면 위 "앞으로 Claude/Codex 등 에이전트가..." 절의 규칙대로(파일/git 뒤지지 말고 한 번만 물어보기) 대응하세요.

### 4. Google 버튼이 "승인 안 된 도메인"에 영구히 멈춰 있던 문제 (커밋 `f7e58b2`)

`NEXT_PUBLIC_GOOGLE_ALLOWED_ORIGINS`가 비어 있으면 `clientId`가 설정돼 있어도 `google-sign-in-card.tsx`의 `currentOriginCanLoadGoogleIdentityServices`가 항상 `false`를 반환해서 버튼을 눌러도 아무 반응이 없었습니다(에러도 없이 조용히 막힘). 실제 접속 도메인들을 넣어서 해결했습니다. **이 도메인 목록에 새로운 접속 주소(새 Tailscale IP, 실제 배포 도메인 등)가 생기면 여기에도 추가하고, 동시에 Google Cloud Console의 "승인된 자바스크립트 원본"에도 똑같이 등록해야 합니다** — 둘 중 하나만 되어 있으면 여전히 막힙니다.

### 5. 검증 방법 — 진짜 카카오/구글 계정 없이 확인한 방법

에이전트는 실제 카카오/구글 계정으로 동의 화면을 통과할 수 없습니다(그리고 시도해서도 안 됩니다). 대신 이렇게 확인했습니다:

- **설정 확인**: `curl http://localhost:5501/auth/social-config`, `/auth/google-config`로 `kakaoConfigured`/`naverConfigured`/`clientId`가 실제 값으로 채워지고 `preferMock`이 `false`인지 확인.
- **Kakao "가짜 코드" 테스트**: `/api/auth/kakao/start`로 받은 `state`를 그대로 `/api/auth/kakao/callback?code=bogus-test-code&state=...`에 넣어서 호출하면, 우리 쪽 설정 문제일 땐 `kakao_not_configured`가 뜨고, 우리 쪽 설정은 끝나고 카카오 서버가 가짜 코드를 거부하는 정상적인 경우엔 `kakao_callback_failed`가 뜹니다. `kakao_not_configured` → `kakao_callback_failed`로 바뀌는 걸 확인하면, 우리 쪽 게이트는 통과했고 이제 진짜 사용자 로그인만 남았다는 뜻입니다.
- **최종 확인은 항상 오너가 실제로 버튼을 눌러서** 진짜 카카오/구글 계정으로 로그인이 완료되는지 확인해야 합니다. 위 방법들은 "우리 쪽 설정 문제는 없다"까지만 증명하고, 카카오/구글 콘솔 쪽 설정(예: Client Secret 기능 on/off, 승인된 리다이렉트 URI, 승인된 오리진)까지는 증명하지 못합니다.

## 운영상 주의 — `.next` 캐시 손상 (간편 로그인과 무관하게도 반복 발생)

**`npm run dev`(또는 `node server.js`) 개발 서버가 떠 있는 상태에서 `npm run build`/`npm test`/`npm run check`(내부적으로 `next build` 실행)를 같이 돌리면 `.next/dev/required-server-files.json`이 깨져서 전체 사이트가 "Internal Server Error"를 뱉습니다.** 이 세션에서 이 문제가 실제로 두 번 발생했고, 둘 다 겉으로는 "간편 로그인이 또 안 됨"처럼 보였지만 원인은 로그인 코드가 아니라 이 캐시 손상이었습니다.

- **증상**: 아무 페이지나 눌러도 반응 없음, 서버 로그에 `ENOENT: ... .next/dev/required-server-files.json` 반복.
- **대처**: 개발 서버를 완전히 내리고 → `rm -rf .next` → 서버 재기동.
- **예방**: `npm run build`/`npm test`/`npm run check`를 돌려야 한다면, 먼저 개발 서버(포트 5501)를 반드시 내리고 실행하세요. 타입 검사만 필요하면 `npx tsc --noEmit -p .`를 쓰면 됩니다(빌드를 하지 않아서 안전).

## 앞으로 이 영역을 다시 만질 때 지켜야 할 것

1. **"미리보기 호스트(localhost/사설 IP)는 무조건 mock"이라는 가정을 다시 넣지 마세요.** 실제 키가 설정되어 있으면(`kakaoConfigured`/`naverConfigured`/`clientId`가 존재하면) 호스트가 무엇이든 실제 로그인을 우선해야 합니다. mock은 "키가 아예 설정 안 됐을 때"의 폴백으로만 존재해야 합니다.
2. 자동화 테스트(Playwright)가 `127.0.0.1`에서 실제 OAuth 리다이렉트를 못 타는 문제가 있다면, `TIG_AUTH_FORCE_PROVIDER=1` 같은 **테스트 전용 환경변수로 명시적으로 실제 provider 모드를 켜서** 테스트하세요. 기본값이 "호스트만 보고 mock 여부를 정하는" 방식이 되면 안 됩니다.
3. 닉네임 설정 화면(`login-session-panel.tsx`)은 반드시 **실제 로그인(또는 명시적 mock 로그인 클릭) 이후에만** 떠야 합니다. 로그인 버튼 클릭과 동시에, 또는 로그인 없이 뜨면 버그입니다.
4. 이 영역을 수정했다면, 수정 후 반드시 아래를 확인하세요:
   - `curl http://localhost:5501/auth/social-config` 와 `/auth/google-config` 응답에서 실제 키가 설정된 상태일 때 `preferMock`/`mockEnabled`가 `false`로 나오는지
   - `curl -sv http://localhost:5501/api/auth/kakao/start` 등이 실제 provider 인증 페이지로 302 리다이렉트되는지 (mock 페이지나 에러 페이지로 새지 않는지)
   - `npm run check` (lint + typecheck + 전체 테스트) 통과

## 정말 이 영역을 고쳐야 한다면

작업 지시에 "간편 로그인", "소셜 로그인", "카카오/네이버/구글 로그인", "OAuth" 중 하나라도 명시적으로 언급되어 있을 때만 진행하세요. 언급이 없다면 절대 먼저 손대지 말고, 필요하다고 판단되면 작업을 멈추고 오너에게 먼저 확인을 받으세요.

## 키 값 위치 — 공개 키(.env, 커밋 가능) vs 비밀 키(.env.local, 커밋 금지)

간편 로그인이 반복적으로 "설정이 없어서" mock으로 빠지는 문제를 근본적으로 줄이기 위해, 값을 두 파일로 나눕니다.

- **`.env` — 공개 식별자. git에 커밋합니다.** Client ID류는 어차피 로그인 리다이렉트 URL에 평문으로 노출되므로(네트워크 탭에서 누구나 볼 수 있음) 숨겨봐야 보안 이득이 없습니다. 여기에 넣는 값: `TIG_KAKAO_REST_API_KEY`, `TIG_NAVER_CLIENT_ID`, `TIG_GOOGLE_CLIENT_ID`/`NEXT_PUBLIC_GOOGLE_CLIENT_ID`, `NEXT_PUBLIC_KAKAO_JAVASCRIPT_KEY`, 각 redirect URI. 템플릿: `.env.example`.
- **`.env.local` — 진짜 비밀. 항상 `.gitignore`에 있고 절대 커밋하지 않습니다.** 유출되면 실제 권한이 생기는 값들입니다: `TIG_KAKAO_CLIENT_SECRET`, `TIG_NAVER_CLIENT_SECRET`, `TIG_ADMIN_TOKEN`, `TIG_APP_ATTESTATION_SECRET`, `TIG_SECRET`. 템플릿: `.env.local.example`.

**앞으로 Claude/Codex 등 에이전트가 이 영역을 작업할 때:**

1. 먼저 이 프로젝트 루트의 `.env`를 확인하세요. `.env`는 git에 커밋되는 파일이므로, 한 번 실제 공개 키 값으로 채워지면 이후 어떤 체크아웃/클론에서도 다시 물어볼 필요가 없습니다.
2. `.env.local`에 있어야 할 비밀 값이 없다면 — **파일시스템이나 git 히스토리, reflog, stash를 뒤져서 찾으려 하지 마세요.** 이건 자격증명 탐색으로 간주되어 보안 정책상 차단됩니다(실제로 이 문서를 작성하게 된 계기인 세션에서 두 번 다 차단당했습니다). 대신 정확히 어떤 값이 비어 있는지 사장님에게 한 번만 명확히 말씀드리고, `.env.local`에 직접 붙여넣어 달라고 요청하세요.
3. 비밀 값은 정의상 git으로 절대 전파될 수 없습니다. "다음부터는 안 물어봐도 되게" 만들 수 있는 건 공개 키(`.env`)뿐이고, 비밀 키(`.env.local`)는 서버를 실제로 띄우는 그 머신에 항상 별도로 존재해야 합니다. 이건 에이전트의 한계가 아니라 비밀 값이라는 개념 자체의 성질이니, 있는 그대로 설명하세요.

## 회귀 방지 — 간편 로그인과 무관한 작업 중에도 확인할 것

이 문서가 처음 만들어진 이유 자체가 "다른 작업을 하다가 실수로 이 영역이 깨졌다"는 사고였고, 이후에도 같은 패턴(호스트 기반 mock 강제 전환)이 반복됐습니다. `src/lib/auth-preview-host.ts`, `server.js`의 env 로딩 순서, `next.config.*`처럼 여러 라우트가 공유하는 파일을 건드렸다면, 로그인 자체를 작업 지시받지 않았더라도 아래 한 줄을 실행해 회귀가 없는지 확인하세요 (Playwright 없이 몇백ms 안에 끝납니다):

```
NODE_ENV=production node --test tests/auth-preview-host-boundary.test.mjs
```

## 독립 검증(advisor)에서 추가로 확인된 사항

1. **재테스트 전 브라우저 저장소를 비우세요.** 예전 mock 로그인을 눌러본 브라우저에는 `localStorage`에 `ticketground:session-user-id=user_fan_a`가 남아있을 수 있고, `login-panel.tsx`가 이를 읽어 자동으로 로그인된 것처럼 `/`로 리다이렉트합니다. 실제 로그인 화면이 정상 동작하는지 확인하려면 해당 사이트 데이터를 지우거나 시크릿 창에서 테스트하세요.
2. **Host 헤더는 클라이언트가 조작 가능한 값입니다.** `requestHostIsPrivatePreview`는 요청의 `Host` 헤더만 보고 판단하므로, `TIG_AUTH_FORCE_PROVIDER=1`이 없는 배포 환경에서는 `Host: 127.0.0.1` 같은 헤더를 보내 mock UI로 전환시키는 것이 이론상 가능합니다. 실서비스 배포본에는 이 플래그를 반드시 켜두거나, 이 판단 로직 자체를 신뢰 가능한 값(예: 서버 자체 설정)으로 바꾸는 것을 고려하세요.
3. `GET /api/users/:id/session`이 인증 없이 id만으로 세션을 반환하는 점도 advisor가 별도로 지적했습니다 — 데모 백엔드 구조상 원래 그런 설계라 이번 수정 범위에서는 그대로 두었지만, 실서비스로 넘어갈 때는 반드시 재검토가 필요합니다.

# Ticketground 나라별 언어 적용 계획서

> 적용 대상 언어: 한국어(기본), 영어, 일본어, 중국어 간체
>
> 1차 적용 범위: Ticketground 공개 홈페이지와 공통 헤더·푸터
>
> 구현 전제: Next.js 16 App Router의 공식 국제화 가이드와 현재 프로젝트 구조를 기준으로 진행한다.

## 1. 목표

Ticketground의 기존 한국어 서비스와 URL을 유지하면서 영어, 일본어, 중국어 간체 화면을 제공한다. 사용자는 헤더의 언어 선택 메뉴에서 언어를 직접 바꿀 수 있고, 선택한 언어로 같은 화면을 계속 이용할 수 있어야 한다.

이번 작업은 우선 공개 홈페이지에 적용한다. 예매, 결제, 회원, 고객센터처럼 정확한 법률·정책 문구가 필요한 화면은 번역 검수와 데이터 준비가 끝난 뒤 순차적으로 확장한다.

문서 제목은 요청한 파일명을 따르지만, 구현에서는 국가가 아니라 언어와 지역 규칙을 나타내는 locale을 기준으로 구분한다.

## 2. 지원 언어와 URL

| 표시 언어 | 내부 locale | 대표 URL | 비고 |
| --- | --- | --- | --- |
| 한국어 | `ko` | `/` | 기존 URL과 검색 노출을 유지하는 기본 언어 |
| 영어 | `en` | `/en` | 특정 국가에 한정하지 않는 영어 |
| 일본어 | `ja` | `/ja` | 일본어 |
| 중국어 간체 | `zh-CN` | `/zh-CN` | 1차 범위는 간체만 지원 |

중국어 번체(`zh-TW`, `zh-HK`)는 이번 범위에 포함하지 않는다. 추후 번체를 추가할 때 간체 번역을 그대로 재사용하지 않고 별도 사전과 검수 절차를 둔다.

### URL 운영 원칙

- 기존 한국어 주소(`/`, `/goods/...`, `/group-booking`, `/seller` 등)는 변경하지 않는다.
- 외국어 주소에는 locale 접두사를 사용한다. 예: `/en`, `/ja`, `/zh-CN`.
- 1차 오픈에서는 외국어 홈페이지까지만 공개하고, 아직 번역하지 않은 상세 화면 링크는 한국어 화면임을 표시하거나 숨긴다. 번역되지 않은 페이지에 외국어 URL만 붙여 한국어 본문을 노출하지 않는다.
- 사용자가 선택한 언어는 `tig_locale` 쿠키에 저장하되, URL에 locale이 있으면 URL을 우선한다.
- `/` 방문자를 브라우저 언어만으로 강제 이동시키지 않는다. 기존 한국어 사용 흐름과 외부 링크를 보존하고, 언어 선택은 사용자가 직접 결정하게 한다.
- `groupbooking-dev.ticketground.co.kr`과 `sellcorp-dev.ticketground.co.kr`의 루트 호스트 라우팅은 각각 `/group-booking`, `/seller`를 계속 우선한다. 다국어 라우팅이 이 전용 도메인 리다이렉트를 가로채지 않도록 별도 회귀 테스트를 둔다.

## 3. 번역 범위

### 1차: 공개 홈페이지

- 전역 메타데이터: 제목, 설명, Open Graph 문구
- 헤더: 검색, 로그인/로그아웃, 회원가입, 관심공연, 예매내역, 카테고리, 모바일 메뉴, 테마 전환 접근성 문구
- 홈 본문: 히어로, 실시간 랭킹, 티켓 오픈, 공식 양도, 장르별 추천, 기획전, 바로가기, 단체 예매 배너
- 푸터: 서비스 소개, 정책/회사/고객센터 링크, 저작권 문구
- 버튼, 링크, 빈 상태, 로딩 상태, 오류 안내, 스크린리더용 `aria-label`
- 날짜, 시간, 숫자, 가격 표시

### 2차: 탐색·상세·전용 판매 화면

- 검색, 장르, 랭킹, 오픈 캘린더, 공연장, 공연 상세
- 기업·기관 단체 예매(`/group-booking`)
- 기업 티켓 판매(`/seller`)
- 공지와 도움말

### 3차: 회원·거래 화면

- 로그인, 회원가입, 마이페이지, 관심공연
- 예매, 좌석 선택, 결제 전 확인, 예매 취소, 공식 양도
- 개인정보처리방침, 이용약관, 환불·취소 정책

3차 문구는 자동 번역만으로 배포하지 않는다. 결제 금액, 취소 기한, 본인 인증, 개인정보 동의처럼 사용자의 권리와 비용에 영향을 주는 문구는 담당자와 원어민 검수를 필수 승인 조건으로 둔다.

## 4. 기술 설계

### 4.1 locale 정의

`src/i18n/config.ts`에서 지원 언어를 단일 원천으로 관리한다.

```ts
export const locales = ["ko", "en", "ja", "zh-CN"] as const;
export type Locale = (typeof locales)[number];
export const defaultLocale: Locale = "ko";
```

- 문자열을 임의로 locale로 단언하지 않고 `isLocale()` 타입 가드로 검증한다.
- 지원하지 않는 locale은 한국어로 조용히 바꾸지 않고 404 또는 명시적인 기본 경로 이동으로 처리한다.
- locale 표기, URL 접두사, `<html lang>`, `hreflang`에 같은 값을 사용한다.

### 4.2 번역 사전

Next.js 서버 컴포넌트에서 언어별 사전을 동적으로 불러온다. 초기 범위에서는 새 국제화 라이브러리를 추가하지 않고 공식 가이드의 서버 전용 dictionary 패턴을 사용한다.

```text
src/i18n/
  config.ts
  get-dictionary.ts
  dictionaries/
    ko.ts
    en.ts
    ja.ts
    zh-CN.ts
```

사전은 화면 단위 namespace로 나눈다.

```text
metadata
common
header
home
footer
errors
```

- `ko.ts`를 기준 스키마로 삼고 나머지 사전이 동일한 키를 갖도록 TypeScript의 `satisfies`로 검사한다.
- 번역 값 안에 JSX나 HTML을 넣지 않는다. 강조, 링크, 줄바꿈은 컴포넌트가 담당한다.
- 버튼 문구와 접근성 문구를 분리하지 않는다. 화면 텍스트를 번역할 때 `aria-label`, `title`, 대체 텍스트도 함께 검수한다.
- 번역 누락 시 운영 화면에서 키 이름을 노출하지 않는다. 개발·CI에서는 누락을 실패로 처리하고, 운영에서는 승인된 한국어 fallback만 사용한다.

### 4.3 라우팅과 레이아웃

1차에는 기존 한국어 경로를 보존하고 외국어 홈페이지 경로를 추가한다. 현재 라우트 전체를 한 번에 `app/[locale]` 아래로 옮기면 예매·로그인·관리자 화면까지 동시에 영향을 받으므로, 먼저 Next.js 16의 `proxy.ts`에서 외국어 접두사를 검증하고 locale 준비가 끝난 경로만 내부 기존 경로로 rewrite한다. 이때 요청 헤더에 검증된 locale을 전달한다.

- `src/app/page.tsx`: 기존 한국어 홈페이지 유지
- `src/i18n/routing.ts`: locale이 붙은 동일 경로 생성과 제거
- `src/i18n/request-locale.ts`: 서버 컴포넌트에서 검증된 요청 locale 조회
- `src/proxy.ts`: `/en`, `/ja`, `/zh-CN`을 내부 `/`로 rewrite하고, `_next`, 이미지, API, 관리자 요청은 제외

예를 들어 사용자가 `/ja`로 접속하면 브라우저 주소는 `/ja`로 유지하되, 서버는 기존 홈페이지 컴포넌트를 일본어 locale로 렌더링한다. `src/app/layout.tsx`의 현재 고정값 `<html lang="ko">`는 검증된 요청 locale에 맞게 설정한다. 메타데이터도 locale별로 생성하고, 한국어와 외국어 페이지 사이에 `alternates.languages`를 선언한다.

홈페이지 이후에는 locale 준비 경로 allowlist를 검색·공연 상세·전용 판매 화면 순서로 확장한다. 모든 공개 화면의 번역이 끝난 뒤에는 Next.js 공식 가이드의 `src/app/[locale]/...` 구조로 통합할지 별도 설계 검토를 진행한다. 그 전에는 준비되지 않은 `/en/...`, `/ja/...`, `/zh-CN/...` 경로를 한국어 본문으로 rewrite하지 않는다.

### 4.4 언어 선택 UI

공통 헤더에 `LanguageSwitcher`를 추가한다.

- 데스크톱: 헤더 유틸리티 영역의 언어 메뉴
- 모바일: 모바일 메뉴 안의 언어 선택 목록
- 현재 언어를 텍스트로 표시하고 아이콘만으로 구분하지 않는다.
- 키보드로 열기, 항목 이동, 선택, 닫기가 가능해야 한다.
- 언어 변경 시 가능한 경우 현재 페이지의 같은 경로와 쿼리 문자열, 해시를 유지한다.
- 번역되지 않은 화면에서는 선택 가능한 언어를 비활성화하고 “해당 언어 준비 중”을 표시한다.
- 선택 후 locale 쿠키를 갱신하되 로그인 세션이나 테마 설정에는 영향을 주지 않는다.

### 4.5 링크와 활성 상태

현재 `src/components/header-links.ts`, `src/components/site-header.tsx`, `src/components/mobile-nav.tsx`에는 한국어 라벨과 locale 없는 링크가 함께 들어 있다. 다음과 같이 역할을 분리한다.

- 라우트 식별자는 언어와 무관한 키로 관리한다.
- 표시 라벨은 사전에서 가져온다.
- 내부 링크는 `localizeHref(locale, href)`를 통과시킨다.
- 카테고리 활성 상태를 계산할 때 locale 접두사를 제거한 경로를 사용한다.
- 로그인, 검색, 마이페이지처럼 아직 번역하지 않은 목적지는 1차 범위에서 한국어 페이지로 이동함을 명확히 표시한다.

### 4.6 데이터와 포맷

현재 홈 화면에는 두 종류의 문구가 있다.

1. 코드에 포함된 UI 문구: 사전으로 이동한다.
2. 카탈로그 API가 반환하는 공연명, 장소, 회차, 안내 문구: 데이터 번역 필드가 필요하다.

1차에는 UI 문구를 모두 번역하되 공연명·아티스트명·공연장명은 원문을 유지할 수 있다. 설명이나 정책처럼 문장형 카탈로그 데이터는 한국어 원문과 섞여 보이지 않도록 해당 영역을 숨기거나 “원문 제공”으로 표시한다.

2차 데이터 모델은 다음 형태를 권장한다.

```ts
type LocalizedText = Readonly<Partial<Record<Locale, string>>> & {
  readonly ko: string;
};
```

- 관리자 입력 화면에서 한국어를 필수값으로 두고 영어·일본어·중국어 간체를 선택 입력한다.
- API는 요청 locale을 받고, 해당 번역과 fallback 여부를 함께 반환한다.
- 검색은 선택 언어와 한국어 원문을 모두 인덱싱한다.
- 좌석 등급, 통화 코드, 예매 상태 같은 값은 번역 문구가 아니라 안정적인 enum으로 전송한다.

날짜·숫자·가격은 문자열 조합 대신 `Intl.DateTimeFormat`, `Intl.NumberFormat`을 사용한다. 판매 통화는 당분간 KRW를 유지하고, 언어 변경을 환율 변환으로 오해하지 않도록 결제 단계에 `KRW`를 표시한다. 공연 시간대는 `Asia/Seoul`을 기준으로 명시한다.

### 4.7 글꼴

현재 `Noto_Sans_KR`만 사용하므로 일본어와 중국어 글리프의 표시 품질을 확인해야 한다.

- 한국어: 기존 `Noto Sans KR`
- 일본어: `Noto Sans JP`
- 중국어 간체: `Noto Sans SC`
- 영어: 기존 라틴 글리프 또는 서비스용 라틴 글꼴

언어별 폰트를 필요한 locale에서만 로드하고, 실제 배포 빌드에서 글꼴 요청 수와 레이아웃 이동을 측정한다. 중국어·일본어 긴 제목이 카드 높이와 버튼 폭을 깨뜨리지 않는지 모바일·태블릿·데스크톱에서 확인한다.

## 5. SEO와 공유

- 각 locale 페이지의 `title`, `description`, Open Graph locale을 번역한다.
- canonical은 현재 언어 URL을 가리킨다.
- `hreflang`은 `ko`, `en`, `ja`, `zh-CN`, 기본값 `x-default`를 제공한다.
- 사이트맵에는 실제 번역이 완료되어 공개된 URL만 넣는다.
- `<html lang>`이 현재 화면 언어와 일치하는지 테스트한다.
- 외국어 페이지가 한국어 페이지와 중복 콘텐츠로 판정되지 않도록 번역 미완료 페이지는 인덱싱하지 않는다.

## 6. 번역 제작과 검수

### 공통 용어집

먼저 다음 핵심 용어의 번역을 확정하고 모든 화면에서 동일하게 사용한다.

- 예매, 예매내역, 티켓 오픈, 좌석 등급, 공연 회차
- 공식 양도, 클린티켓, 정가, 취소, 환불
- 기업·기관 단체 예매, 기업 티켓 판매
- 관심공연, 마이페이지, 고객센터

브랜드명 `Ticketground`와 상품·공연의 공식 영문명은 임의 번역하지 않는다. 공식 표기가 없는 고유명사는 한국어 원문을 병기하는 원칙을 정한다.

### 검수 순서

1. 한국어 기준 문구 확정
2. 번역 초안 작성
3. 제품 담당자의 의미 검수
4. 영어·일본어·중국어 간체 원어민 검수
5. 실제 화면에서 길이, 줄바꿈, 맥락 검수
6. 법률·결제 문구 담당자 승인

번역 파일 변경도 코드 리뷰 대상으로 관리한다. 번역 키 삭제나 의미 변경이 있으면 사용하는 화면과 다른 언어 파일을 함께 수정한다.

## 7. 구현 단계

### 단계 1. 기반과 계약 테스트

**대상 파일**

- 생성: `src/i18n/config.ts`
- 생성: `src/i18n/get-dictionary.ts`
- 생성: `src/i18n/dictionaries/ko.ts`
- 생성: `src/i18n/dictionaries/en.ts`
- 생성: `src/i18n/dictionaries/ja.ts`
- 생성: `src/i18n/dictionaries/zh-CN.ts`
- 생성: `tests/i18n-dictionaries.test.mjs`

**완료 조건**

- 네 사전의 키 구조가 동일하다.
- 지원하지 않는 locale이 거부된다.
- 서버에서 요청한 언어 사전만 불러온다.
- 빈 번역과 임시 문구가 CI에서 발견된다.

### 단계 2. locale 라우팅

**대상 파일**

- 생성: `src/i18n/routing.ts`
- 생성: `src/i18n/request-locale.ts`
- 생성: `src/proxy.ts`
- 수정: `src/app/layout.tsx`
- 생성: `tests/i18n-routing.test.mjs`
- 수정: `tests/public-host-routing.test.mjs`

**완료 조건**

- `/`, `/en`, `/ja`, `/zh-CN`이 각각 올바른 locale로 렌더링된다.
- `_next`, 이미지, API, 관리자 요청은 locale 처리에서 제외된다.
- 잘못된 locale은 404 또는 정해진 기본 경로로 처리된다.
- 두 Cloudflare 전용 개발 도메인의 루트 라우팅이 그대로 동작한다.

### 단계 3. 공통 UI와 홈페이지 번역

**대상 파일**

- 생성: `src/components/language-switcher.tsx`
- 수정: `src/components/site-header.tsx`
- 수정: `src/components/mobile-nav.tsx`
- 수정: `src/components/site-footer.tsx`
- 수정: `src/components/header-links.ts`
- 수정: `src/components/home/home-sections.tsx`
- 수정: `src/components/home/home-content.ts`
- 수정: `src/app/page.tsx`

**완료 조건**

- 홈 화면에 번역되지 않은 한국어 UI 문구가 남지 않는다.
- 언어를 바꾸면 같은 화면과 쿼리·해시를 최대한 유지한다.
- 언어 메뉴가 키보드와 스크린리더로 동작한다.
- 모바일, 태블릿, 데스크톱에서 긴 번역이 겹치거나 잘리지 않는다.

### 단계 4. 포맷과 메타데이터

**대상 파일**

- 생성: `src/i18n/format.ts`
- 수정: `src/app/layout.tsx`
- 수정: 홈 화면의 날짜·가격 표시 컴포넌트
- 생성: `src/app/sitemap.ts`

**완료 조건**

- locale별 날짜·숫자 형식이 일관된다.
- 통화와 시간대가 명확히 표시된다.
- `<html lang>`, canonical, `hreflang`, Open Graph가 올바르다.
- 사이트맵에는 공개된 번역 경로만 포함된다.

### 단계 5. 데이터 다국어화

**대상 영역**

- 카탈로그 이벤트 타입과 저장 형식
- 관리자 이벤트 입력 화면
- `/api/catalog` 응답
- `src/data/catalog-server.ts`
- 검색과 상세 화면

**완료 조건**

- 한국어 원문은 항상 보존된다.
- locale별 번역과 fallback 여부를 구분할 수 있다.
- 번역 누락 때문에 예매 가능 여부, 가격, 날짜, 좌석 정보가 달라지지 않는다.
- 기존 카탈로그 데이터와 API 소비자의 하위 호환성이 유지된다.

### 단계 6. 단계적 배포

1. 로컬에서 네 locale의 홈 화면 테스트
2. 개발 도메인에서 기능 플래그로 내부 공개
3. 원어민 및 제품 담당자 화면 검수
4. 검색엔진 인덱싱을 끈 상태로 제한 공개
5. 오류와 사용자 행동 지표 확인
6. 영어 공개 후 일본어, 중국어 간체를 한 언어씩 순차 공개
7. 번역 완료 URL만 사이트맵과 검색 인덱스에 추가

언어를 동시에 모두 켜지 않고 한 언어씩 공개하면 번역 누락, 레이아웃 깨짐, 검색 유입 문제를 분리해 확인할 수 있다.

## 8. 테스트 계획

### 자동 테스트

- locale 허용 목록과 타입 가드
- 사전 간 키 일치, 빈 값, 임시 문구 검사
- locale URL 생성·제거와 쿼리/해시 보존
- locale별 `<html lang>`과 메타데이터
- 한국어 기존 URL 회귀 테스트
- 전용 판매 도메인 루트 라우팅 회귀 테스트
- 날짜·숫자·KRW 포맷 스냅샷이 아닌 의미 기반 테스트
- 외국어 홈 HTML에 한국어 UI 문구가 남지 않는지 검사

### 실제 화면 QA

각 언어마다 다음 시나리오를 모바일, 태블릿, 데스크톱에서 확인한다.

1. 홈페이지 직접 진입
2. 언어 선택 메뉴 열기와 키보드 조작
3. 언어 변경 후 URL, 본문, `<html lang>` 확인
4. 새로고침과 재방문 후 선택 언어 유지
5. 검색창, 카테고리, 로그인 링크, 단체 예매 배너 이동
6. 긴 제목과 버튼의 줄바꿈, 카드 높이, 가로 스크롤 확인
7. 로딩·빈 상태·API 오류 화면의 번역 확인
8. 라이트·다크 테마 전환 후 번역과 접근성 문구 확인
9. `groupbooking-dev.ticketground.co.kr`, `sellcorp-dev.ticketground.co.kr`의 기존 전용 경로 확인

## 9. 배포 승인 기준

아래 조건을 모두 충족해야 해당 언어를 공개한다.

- 해당 언어 사전 누락 0건
- 홈페이지와 공통 UI의 한국어 혼용 0건. 단, 공식 고유명사와 명시된 원문 표기는 제외
- 원어민 및 제품 담당자 검수 완료
- 모바일·태블릿·데스크톱 실제 화면 QA 완료
- 키보드와 스크린리더 핵심 동선 확인
- 기존 한국어 URL과 전용 판매 도메인 회귀 없음
- 빌드, 타입 검사, 관련 자동 테스트 통과
- canonical, `hreflang`, 사이트맵 검증 완료
- 결제·정책 화면은 담당자 승인 전 외국어 경로 비공개

## 10. 예상 위험과 대응

- **한국어와 외국어 혼용:** 정적 문구와 API 데이터를 분리하고, 외국어 화면의 한국어 잔존 검사를 CI에 추가한다.
- **기존 URL 손상:** 한국어 URL을 유지하고 locale 라우팅 범위를 공개 화면으로 제한한다.
- **전용 개발 도메인 충돌:** 호스트 기반 `/group-booking`, `/seller` 라우팅을 locale 처리보다 먼저 평가하고 회귀 테스트한다.
- **긴 번역으로 인한 UI 깨짐:** 고정 높이와 과도한 한 줄 말줄임을 점검하고 실제 해상도별 시각 QA를 수행한다.
- **번역되지 않은 동적 콘텐츠:** fallback 여부를 표시하고, 문장형 데이터는 승인 전 숨기거나 원문임을 알린다.
- **법률·결제 의미 변경:** 자동 번역으로 공개하지 않고 담당자·원어민 이중 승인을 적용한다.
- **검색 중복 콘텐츠:** 번역 미완료 경로를 인덱싱하지 않고 locale별 canonical과 `hreflang`을 설정한다.

## 11. 최종 산출물

- locale 설정과 타입 안전한 번역 사전
- 영어·일본어·중국어 간체 홈페이지
- 데스크톱·모바일 언어 선택 UI
- locale별 메타데이터, canonical, `hreflang`, 사이트맵
- 번역 키·라우팅·포맷 자동 테스트
- 언어별 용어집과 번역 검수 기록
- 실제 화면 QA 결과와 단계별 배포 체크리스트

# 사용자 페이지 UI 전면 개선 계획서

- 대상: 일반 소비자가 보는 공개 사이트(홈/로그인/마이페이지/공연 상세/예매)만. 관리자 콘솔, 판매자 콘솔은 범위 밖.
- 작성일: 2026-08-13
- 근거: 국내 주력 4곳(NOL 인터파크, YES24 티켓, 멜론티켓, 티켓링크) + 해외 보조 1곳(티켓마스터) 실사 조사, Ticketground 현재 화면 진단. 이 문서는 계획서이며 코드를 변경하지 않는다 — 승인 후 별도 구현 라운드에서 진행한다.
- 결제(TossPayments), 로그인(Google/Kakao/Naver), NICE 본인인증 등 백엔드 로직/플로우는 이번 범위에서 바꾸지 않는다. 순수 시각/UI 개선이다.

---

## 1. 배경과 목적

운영자가 실화면을 보고 "개발하다가 만 것 같다"고 지적했다. 실제로 로그인 페이지에는 처리되지 않은 백엔드 에러 문자열이 그대로 노출되고, 마이페이지는 로딩 상태가 최종 콘텐츠처럼 굳어 렌더링되는 등 미완성 신호가 여러 곳에서 확인된다. 여기에 더해 참고 사이트 5곳과 비교했을 때 홈페이지 콘텐츠 절대량, 공연 상세 페이지의 정보량 등에서 격차가 있다 — 다만 이 격차는 스타일 문제가 아니라 "채워야 할 자리가 비어있는" 완성도 문제로 본다(자세한 근거는 §3 참고).

이 계획서의 목적은 두 갈래다.

1. 디자인 방향과 무관하게 지금 바로 고쳐야 하는 버그를 먼저 분리해낸다.
2. 참고 사이트 조사를 근거로 홈/로그인/마이페이지/공연 상세/예매 페이지의 완성도를 보강하는 방향을 제안하고, 단계별로 실행 가능한 로드맵을 제시한다.

**전제(운영자 확인 반영)**: 이번 작업은 새로운 디자인 방향을 도입하는 게 아니다. 현재의 절제된 톤(무채색 베이스, 단순한 카드) 자체는 유지한다 — 다만 그 절제가 "완성된 미니멀"이 아니라 "빈 곳이 많아서 심플해 보이는" 상태에 가깝다는 게 문제다. 아래 §3의 참고 사이트 조사는 "이렇게 화려하게 바꿔라"가 아니라 "구조적으로 무엇이 비어있는가"를 확인하는 용도로만 쓴다. 색상을 늘리거나 타이포를 화려하게 키우는 방향은 이번 범위에서 뺀다.

---

## 2. 즉시 수정 대상 버그 (디자인 방향 결정을 기다릴 필요 없음)

아래 7건은 "리뉴얼" 이슈가 아니라 버그다. 디자인 방향(라이트/다크, 브랜드 컬러 등)이 무엇으로 결정되든 영향받지 않으므로, 이번 계획서 승인·디자인 방향 확정과 무관하게 바로 착수할 수 있다. 이 중 2-2, 2-5, 2-6은 운영자가 실제 화면 스크린샷으로 직접 지적한 항목이라 문구만 바꾸는 순수 카피 수정으로 판단해 계획서 작성과 별개로 이번에 바로 코드에 반영했다(§2-2/2-5/2-6에 "상태" 표시).

### 2-1. 로그인 페이지 에러 문자열 노출

`/login`에서 로그인 안내 문구 아래에 처리되지 않은 백엔드 에러 문자열 **"요청한 API가 없습니다."**가 그대로 화면에 노출된다. 이 문자열은 `backend/api-router.js`(`199`, `205`, `211`, `290`, `1056`행)와 `backend/identity.js:356`에서 `httpError(404, "NOT_FOUND", "요청한 API가 없습니다.")`로 던져지는 기능 플래그 비활성 응답으로 보인다. 프론트 어딘가(로그인 관련 컴포넌트, `src/lib/ticketground-api.ts` 호출 경로 추정)가 이 404 에러를 캐치하지 않고 메시지를 그대로 렌더링하고 있다. 이번 조사에서 발견한 가장 확실한 "미완성" 증거다.

**조치 방향**: 어느 API 호출이 이 404를 유발하는지 특정 → 해당 기능이 실제로 필요 없는 죽은 호출이면 제거, 필요한 호출이면 백엔드 플래그 활성화 또는 프론트에서 에러를 사용자 문구로 매핑(예: 해당 UI 자체를 숨김).

### 2-2. 마이페이지 "세션 확인 중" placeholder-as-content

**상태: 조치 완료 (2026-08-13)**

`src/components/mypage/account-summary-panel.tsx`에서 로그아웃 상태에도 `status` state의 초기값(`31`행) 및 세션 미확인 시 재설정값(`43`행)이 `"세션 확인 중"`이며, `132`행의 `displayName`도 `authState === "loading"`일 때 `"세션 확인 중"`을 그대로 표시했다. "세션"은 개발자 용어라 그 자체로도 문제였고("세션 확인 중"이 로딩이 끝난 뒤에도 콘텐츠처럼 굳어 보이는 문제와 별개로), 실사용자 관점에서 이해하기 어려운 단어였다.

**적용한 수정**: "세션" 계열 문구를 전부 "로그인" 기준 일반 문구로 교체했다 — `"세션 확인 중"` → `"로그인 정보를 확인하는 중입니다"`, `"OO 세션 연결됨"` → `"OO님, 로그인되었습니다."`. 로딩/에러 상태에서 헤딩(`{displayName} 회원`)이 조건 없이 항상 렌더링되던 것도 분기해, 로딩 중에는 "회원 정보를 불러오는 중입니다", 에러 시에는 "회원 정보를 불러오지 못했습니다"를 헤딩 자체로 보여주도록 고쳤다(기존에는 에러 상태에서 `displayName` fallback이 "회원"이라 헤딩이 "회원 회원"으로 겹쳐 보이는 별도 버그도 있었는데 같이 해결됨). 에러 캐치 블록의 `error.message`(원문 백엔드 문자열)를 그대로 노출하던 두 곳(로그인 상태 확인 실패, 회원정보 저장 실패)도 고정된 한국어 안내 문구로 교체했다 — §2-1이 지적한 "백엔드 원문 에러 노출" 패턴의 같은 파일 내 사례였다.

### 2-3. 본인인증 패널의 모순된 상태 동시 노출

`src/components/mypage/identity-verification-panel.tsx`는 `130`행에서 버튼 문구를 `status?.verified ? "본인인증 완료" : busy ? "인증 창 준비 중" : "NICE 본인인증 시작"`으로 결정하는데, 이는 로그인 여부와 무관하게 항상 "NICE 본인인증 시작" 버튼을 노출한다. 반면 `refresh()`(`20-30`행)는 `storedSessionUserId()`가 없으면 `message`를 `"로그인 후 본인인증을 진행할 수 있습니다."`로 세팅한다. 결과적으로 로그아웃 상태에서 "NICE 본인인증 시작" 버튼과 "로그인이 필요합니다" 계열 안내 문구가 한 화면에 동시에 보이는 모순 상태가 된다(버튼 클릭 시 `41`행에서 `/login`으로 리다이렉트되긴 하지만, 클릭 전에 이미 모순된 두 메시지가 함께 보인다).

**조치 방향**: 로그아웃 상태에서는 버튼 자체를 비활성화하거나 "로그인 후 이용 가능" 안내로 대체하고, 인증 시작 버튼은 로그인 상태에서만 노출한다.

### 2-4. Pretendard 폰트 파이프라인 미완성

`src/app/globals.css` 1번째 줄에 다음 TODO가 그대로 남아있다.

```
/* TODO(font): add `src/app/fonts/PretendardVariable.woff2` and enable next/font/local in layout.tsx to populate --font-pretendard. */
```

`theme-vars.css`의 `--font-sans`/`--font-display`는 `"Pretendard Variable"`을 1순위로 요청하지만, `src/app/layout.tsx`는 `Noto_Sans_KR`(`next/font/google`)만 로드하고 있고 `--font-pretendard` 변수는 어디서도 채워지지 않는다. 폰트 파일이 실제로 추가된 적이 없어 사이트 전체가 조용히 Noto Sans KR(또는 시스템 폰트)로 폴백 중이다. "제네릭하고 브랜드감 없어 보인다"는 인상의 구체적이고 바로 고칠 수 있는 원인이다.

**참고**: 이 이슈는 기존 `UI-개선-계획서.md`의 P3a에서도 이미 식별되어 상세 구현 지시가 있다(§3-3 참고). 그 문서 P3a가 아직 착수되지 않았다면, 이번 계획서의 즉시 수정 항목과 통합해 먼저 처리하는 것을 권장한다. 단, 폰트 바이너리(`.woff2`) 확보와 라이선스 확인은 사람 결정 사항이므로 별도로 준비가 필요하다(§8 미결정 사항 참고).

**조치 방향**: Pretendard Variable `.woff2` 파일 확보 → `next/font/local`로 `--font-pretendard` 로드 → `layout.tsx`의 `<html>`/`<body>` className에 적용. 파일 확보 전에는 배선 코드만 준비하고 TODO로 명시.

### 2-5. 마이페이지 회원 정보 카드 — 근거 없는 통계·보안 용어 노출

**상태: 조치 완료 (2026-08-13)**

운영자가 실제 `dev.ticketground.co.kr` 마이페이지 스크린샷으로 직접 지적한 항목. 회원 정보 카드(`account-summary-panel.tsx` `149`행)에 `"Tig 티켓 인증 기기 1대"`라는 문구가 항상 고정 노출되고 있었는데, 코드를 확인한 결과 이 값은 실제 기기 인증 데이터(`backend/mobile-lifecycle.js`, `backend/admission-devices.js`가 다루는 `db.trustedDevices`)와 전혀 연결되지 않은 **하드코딩된 문자열**이었다 — 즉 "1대"라는 숫자 자체가 무의미했고, "인증 기기"라는 보안 개념까지 곁들여져 일반 사용자가 이해하기 어려운 정보 과잉 상태였다.

**적용한 수정**: 근거 없는 "Tig 티켓 인증 기기 1대" 줄을 제거했다. 나머지 예매/양도 카운터는 실제 props로 들어오는 값이라 유지했다.

**참고(이번에 안 고친 것)**: 로그인 페이지(`src/components/ticketing/login-panel.tsx` `88`,`163`,`179`행)에는 로그인 성공 메시지에 내부 부정거래 방지 점수를 `신뢰점수 ${trustScore}` 형태로 그대로 붙이는 동일 계열 문제가 남아 있다 — 마이페이지가 아니라 로그인 페이지라 이번 스크린샷 범위 밖으로 보고 손대지 않았다. §2-7에서 별도로 다룬다.

### 2-6. "3단계 안전 장치" — 내부 정책 용어를 설명 없이 그대로 노출

**상태: 조치 완료 (2026-08-13)**

운영자가 실제 홈 화면 스크린샷으로 직접 지적한 항목. `src/i18n/dictionaries/ko.ts`(`90-95`행, `src/components/home/home-sections.tsx`의 `OfficialResaleSection`이 렌더링)의 "3단계 안전 장치" 카드 중 2단계 제목이 `"정책 자동 판별"`이었다 — 내부 룰 엔진을 가리키는 용어라 "무엇을 판별한다는 건지" 그 자체로는 짐작하기 어렵고, 보조 설명("정가 90~110% 자동 검증")을 같이 읽어야만 겨우 이해되는 구조였다. 3단계 설명의 "동적 QR"도 QR이 주기적으로 갱신된다는 내부 구현 디테일을 굳이 사용자에게 노출하는 표현이었다.

**적용한 수정**: 제목을 사용자가 바로 이해할 수 있는 말로 바꿨다 — 섹션 제목 `"3단계 안전 장치"` → `"안전한 거래를 위한 3단계"`, `"보유 티켓 확인"` → `"내 티켓 등록"`, `"정책 자동 판별"` → `"정가 범위 확인"`(설명도 `"정가 90~110% 자동 검증"` → `"정가 90~110%만 거래 가능"`으로 조사만 다듬음), `"QR 보호"` 설명의 `"입장 직전 동적 QR 전달"` → `"입장 직전에 QR 발급"`("동적" 제거). 홈페이지 i18n phase 1(en/ja/zh-CN, PR #170으로 이미 배포됨)이 이미 이 섹션을 포함하고 있어 4개 언어 사전 파일을 모두 같은 방향으로 맞췄다.

### 2-7. 신뢰점수·QA/mock 문구·원문 에러 노출 — 남은 구조적 패턴 (미해결)

이번 라운드에서 §2-2/2-5/2-6은 실제 코드까지 고쳤지만, grep으로 확인한 아래 항목들은 파일 수가 많고(8개 이상) 일부는 보호 대상 로그인 코드와 맞닿아 있어 이번에는 손대지 않았다. 다음 작업 우선순위로 남긴다.

- **신뢰점수 원시 노출**: `login-panel.tsx`(`88`,`163`,`179`행)가 소셜/구글 로그인 성공 메시지에 `신뢰점수 ${trustScore}`를 숫자 그대로 붙인다. 사용자가 이해할 수 없는 내부 부정거래 방지 점수일 뿐 아니라, 점수 변화를 사용자에게 그대로 보여주면 자신의 행동이 탐지 로직에 미치는 영향을 역산할 여지가 생긴다(보안 관점에서도 바람직하지 않음). `admin-workspaces.tsx`(`584`,`598`행)의 "신뢰도" 노출은 관리자 콘솔이라 범위 밖.
- **"QA"/"mock" 문구의 사용자 노출**: `google-sign-in-card.tsx`, `social-login-buttons.tsx`, `login-mock-form.tsx`, `checkout-panel.tsx`, `identity-verification-panel.tsx` 등에 "QA mock", "로컬 QA 모드입니다" 류 문구가 그대로 들어있다. 로컬/QA 환경 전용으로 보이지만, 실사용자에게 노출될 경우 회사 내부 테스트 화면처럼 보인다.
- **백엔드 원문 에러·상태 코드의 무번역 노출(구조적 패턴)**: §2-1, §2-2가 지적한 `error instanceof Error ? error.message : "..."` 패턴이 `checkout-panel.tsx`, `backend-admission-panel.tsx`, `google-sign-in-card.tsx`, `tosspayments-result-panel.tsx` 등 최소 8개 파일에 반복된다. 상태값도 원문 그대로 노출되는 곳이 있다 — `backend-ticket-panel.tsx:119`의 `{ticket.status}`(예: `ON_SALE` 같은 영문 대문자 코드), `checkout-panel.tsx:349`의 결제 완료 문구 옆 `purchase.payment.status`. `backend/catalog.js`에는 이미 상태를 한국어로 바꾸는 라벨 매핑 패턴이 있으니(예: `ON_SALE` → "예매 가능") 같은 패턴을 프론트 상태 표시 전반에 적용하는 게 권장 방향.
- **"카카오 사용자" 기본 표시 이름**: 소셜 닉네임이 없을 때 `backend/social-oauth.js`의 `normalizeProfile()`(`163`행)이 `"카카오 사용자"`를 fallback 이름으로 쓴다. 정상 동작이지만 사용자 입장에선 자기 이름 대신 이 문구가 뜨면 혼란스러울 수 있다. **주의**: 이 파일은 `간편로그인-수정금지-지침.md`가 지정한 보호 대상 로그인 파일(`PROTECTED_AUTH_PATTERNS`)이라, 고치려면 운영자 승인과 별도의 로그인 테스트가 필요하다 — 이번 계획서 범위에서 코드를 바꾸지 않았고, 바꾼다면 fallback 이름 문구만 다듬는 선에서(로그인 로직 자체는 그대로) 최소 변경으로 접근해야 한다.

---

## 3. 디자인 방향 제안

아래는 참고 사이트 5곳 조사에서 "Ticketground에 구조적으로 빠져있는 것"만 추린 것이다. 참고 사이트들의 화려한 색감·촘촘한 프로모 구역·포스터형 타이포는 대체로 **채택하지 않는다** — 그건 각 사이트의 브랜드 톤이지 Ticketground가 따라야 할 기준이 아니다. Ticketground의 절제된 톤은 유지하고, "완성도가 부족해서 비어 보이는" 지점만 채운다.

### 3-1. 콘텐츠 완성도 보강 (톤은 그대로, 빈 곳만 채움)

**근거**: 참고 사이트 대비 Ticketground 홈은 콘텐츠 절대량이 적다(히어로 2슬라이드, 랭킹 섹션 앞 빈 여백, 그 아래 콘텐츠 행 부족). 색상이나 카드 스타일 때문이 아니라 단순히 "채워야 할 자리가 비어있어서" 휑해 보인다.

**제안**: 기존 카드/섹션 스타일(단색 배경, 단순 테두리)은 그대로 두고 반복 사용한다. 히어로 슬라이드 수를 늘리고, 랭킹 섹션 앞 빈 여백을 제거하고, 카테고리별/장르별 추천 행처럼 이미 있는 컴포넌트 패턴을 재사용해 콘텐츠 행을 추가한다. 새로운 시각 언어(다색 카드, 포스터 타이포 등)는 도입하지 않는다.

### 3-2. 공연 상세 페이지의 정보 완성도

**근거**: 참고 사이트들은 갤러리·캐스팅·공연장 정보·추천 등 부가 정보 섹션을 갖추고 있다. Ticketground는 포스터 1장 + 정보 테이블뿐이라 "정보가 부족해서" 허전해 보인다(장식이 부족해서가 아니라).

**제안**: 기존 3단 레이아웃(포스터/정보 테이블/예매 위젯)과 카드 스타일은 유지한 채, 그 아래에 정보 섹션(캐스팅, 공연장 안내, 추천 공연)을 같은 톤으로 추가한다. 갤러리·후기처럼 실데이터 확보가 어려운 항목은 후순위로 미루고 §8에서 데이터 소싱을 먼저 확정한다.

### 3-3. 타이포 파이프라인 정상화 (완성도 문제이지 스타일 변경이 아님)

**근거**: §2-4에서 다룬 대로 Pretendard 폰트가 실제로는 로드되지 않아 시스템 기본 폰트로 렌더링 중이다. 이건 "브랜드 타이포를 화려하게 키우자"는 제안이 아니라, 원래 의도했던 폰트가 실제로 적용되게 고치는 것 — 지금 상태 자체가 미완성이다.

**제안**: §2-4 버그 수정과 동일 — Pretendard Variable 로드만 완료한다. 포스터형 대형 타이포, 스트로크/아웃라인 텍스트 같은 장식적 확장은 이번 범위에서 제외한다.

### 3-4. 히어로 이미지 크롭 수정

**근거**: 현재 히어로 대표 이미지(IU 2026 World Tour)가 이마/헤어라인만 보이도록 잘못 크롭되어 있다. 이는 스타일 선택이 아니라 명백한 실수다.

**제안**: 정상적인 구도로 크롭 수정. 히어로 레이아웃 구조(배너+캡션) 자체는 바꾸지 않는다.

### 보류 (이번 범위에서 제외)

아래는 조사 과정에서 확인됐지만, "심플한 톤 유지"라는 이번 방향과 맞지 않아 제안하지 않는다. 필요해지면 별도로 다시 논의한다.

- 이미지 위에 대형 디스플레이 타이포를 얹는 포스터형 히어로 (NOL 인터파크/YES24/티켓링크 패턴)
- 프로모 카드마다 다른 배경색을 쓰는 색상 다양화 (NOL 인터파크/티켓링크 패턴) — 신규 보조 팔레트 도입 안 함
- 검색 우선 UX 도입 (티켓마스터 패턴) — 현재 헤더 검색 진입점의 발견성 정도만 필요시 별도 점검

---

## 4. 페이지별 개선 계획

### 4-1. 홈 (`/`)

- §3-4에 따라 히어로 대표 이미지(IU 2026 World Tour) 크롭 수정 — 레이아웃 구조는 그대로, 이마/헤어라인만 크게 보이는 잘못된 크롭만 정상 구도로 교체.
- 히어로 슬라이드 수 확대(현재 2개 → 적당한 수준으로. 기존 배너+캡션 구조 유지, 포스터형 재설계는 하지 않음).
- "실시간 예매 랭킹 TOP10" 섹션 앞 큰 빈 여백 제거.
- §3-1에 따라 기존 컴포넌트 패턴을 재사용해 카테고리/장르 추천 행 등 콘텐츠 행 추가 — 새 카드 스타일이나 색상은 도입하지 않는다.

### 4-2. 로그인 (`/login`)

- §2-1 버그 수정(에러 문자열 노출 제거)이 최우선.
- 밝은 회색 안내 패널 + 어두운 폼 패널의 2단 레이아웃 자체는 유지 가능하나, §5 토큰 정비 이후 팔레트 일관성 재검토.
- 구글/카카오/네이버 버튼에 브랜드 가이드라인 범위 내 시각적 마감(간격, 호버 상태 등) 보강 — 로고 자체나 인증 로직은 변경하지 않음.

### 4-3. 마이페이지 (`/mypage`)

- §2-2, §2-3 버그 수정(세션 확인 중 placeholder, 본인인증 패널 모순 상태)이 최우선.
- 로딩 상태는 스켈레톤 UI로 전환.
- 카드 스타일 간 위계(elevation/border) 정리 — 현재 평면적인 카드 스타일에 §5에서 정비할 shadow 토큰(`--shadow-1/2/3`)을 일관되게 적용.

### 4-4. 공연 상세 (`/goods/[slug]`)

- §3-2에 따라 캐스팅, 공연장 안내, 추천 공연 섹션을 기존 톤 그대로 단계적 추가. 갤러리·후기는 데이터 소싱 확정 후 후순위로.
- 포스터/정보 테이블/예매 위젯 3단 레이아웃 자체는 유지하되, 위 섹션들을 정보 테이블 아래 순차 배치.

### 4-5. 예매(부킹) (`/booking/[slug]`)

- 날짜·회차 선택 → 좌석 선택 플로우 동작 자체는 변경하지 않음(백엔드/로직 불변 원칙).
- 시각적 마감 보강: 단순 테두리 박스 스타일을 카드/입체감 있는 스타일로 교체.
- 카운트다운 타이머 박스의 흰 배경을 다크 테마와 어울리는 색상으로 교체(또는 §6에서 라이트/다크 기본값이 확정되면 그에 맞춰 조정).
- 푸터 전 빈 여백 정리.

---

## 5. 디자인 토큰/시스템 작업

### 5-1. 현재 정의된 토큰 (`src/app/theme-vars.css`)

**라이트 모드(`:root`, CSS 기본값)**
- 배경: `--bg:#ffffff`, `--bg-2:#f7f7f8`, `--bg-3:#f3f3f3`, `--bg-4:#ebebed`
- 텍스트: `--ink:#1a1a1d`, `--ink-2:#29292d`, `--ink-3:#6b6b70`, `--ink-4:#999999`
- 강조: `--accent:#ff2d3f`(빨강), `--accent-2:#ffe92e`(노랑), `--link:#1a47ff`
- 상태: `--ok:#1f8a5b`, `--warn:#c47a00`
- 등급색: `--tier-vip/-r/-s/-a/-b`
- radius: `--r-xs:4px` ~ `--r-2xl:32px`, `--r-pill:100px`
- shadow: `--shadow-1`(옅음, `rgba(0,0,0,.08) 0 1px 3px 0`) ~ `--shadow-3`

**다크 모드(`.dark`)**
- 배경: `--bg:#1b1b1e`, `--bg-2:#202024` 등
- 텍스트: `--ink:#f5f5f6` 등
- 강조: `--accent:#ff5a68`, `--accent-2:#ffe14a`
- shadow는 라이트보다 짙음(`--shadow-1: rgba(0,0,0,.32) 0 1px 3px 0`)

### 5-2. 유지할 것

- 기존 spacing(`--s-1` ~ `--s-12`), radius, 시맨틱 색 변수 네이밍 체계는 그대로 유지 — 이미 잘 잡힌 스케일이며 참고 사이트 대비 부족한 것은 "다양성"이지 "체계"가 아니다.
- 라이트/다크 듀얼 토큰 구조 자체는 유지(§6에서 기본 노출 모드만 확인 필요).

### 5-3. 확장/재검토 제안

이번 방향(톤 유지 + 완성도 보강)에 따라 신규 토큰 추가는 최소화한다.

- **폰트 파이프라인**: §2-4 버그 수정과 함께 `--font-pretendard` 실제 로드. 이것 외에 새 타이포 스케일(대형 디스플레이 웨이트, 스트로크 유틸 등)은 추가하지 않는다 — §3의 보류 항목 참고.
- **프로모 카드 보조 팔레트, 디스플레이 타이포 스케일 등은 도입하지 않는다** — 기존 시맨틱 토큰(`--accent`/`--accent-2`, `--fs-*`)만으로 §3-1~3-4를 구현한다. 색상/타이포 확장이 실제로 필요해지면 그건 "완성도 보강"이 아니라 별도의 디자인 방향 논의 대상이다.
- **미결정**: 라이트/다크 중 어느 쪽이 실제 기본 노출 모드인지 확인 필요 — 이번 조사에서 캡처된 모든 페이지가 다크 모드로 렌더링됐으나, 이는 조사에 사용한 브라우저/OS의 `prefers-color-scheme` 설정 때문으로 추정된다(`layout.tsx`의 테마 부트스트랩 스크립트가 `localStorage`에 저장된 테마가 없으면 `matchMedia("(prefers-color-scheme: dark)")` 결과를 따름). CSS 상 실제 기본값(`:root`, `.dark` 미적용)은 라이트 모드이므로, 실사용자 대다수가 어느 모드로 보게 되는지는 별도 확인이 필요하다(§8 참고).

---

## 6. 단계별 실행 로드맵

각 단계는 독립적으로 배포 가능하도록 설계한다 — 전체를 한 번에 갈아엎지 않는다.

```
1단계 — 버그 수정 (즉시, 디자인 방향 결정 불필요)
  - §2-1 로그인 에러 문자열 노출 제거
  - §2-2 마이페이지 "세션 확인 중" placeholder 정리
  - §2-3 본인인증 패널 모순 상태 수정
  - §2-4 Pretendard 폰트 파이프라인 완성
  완료 기준: 4건 모두 화면에서 재현 안 됨, 기존 테스트 그린

2단계 — 라이트/다크 기본 모드 확정
  - §8 확인 후 라이트/다크 기본 모드 확정 — 신규 토큰 추가는 없음(§5-3)
  완료 기준: 기본 노출 모드 확정, 필요 시 부트스트랩 스크립트만 조정

3단계 — 홈페이지
  - §4-1 히어로 이미지 크롭 수정(구조 변경 없음) + 슬라이드 확대
  - 기존 컴포넌트 패턴 재사용한 콘텐츠 행 추가(카테고리/장르 추천 등)
  - 랭킹 섹션 앞 빈 여백 제거
  완료 기준: 주요 뷰포트(390/768/1240px) 시각 확인, 기존 홈 관련 테스트 갱신 후 그린

4단계 — 공연 상세
  - §4-4 캐스팅/공연장 안내/추천 섹션 단계적 추가(기존 톤 유지)
  완료 기준: 섹션별 개별 배포 가능(한 번에 다 넣지 않음), 데이터 소싱 확인된 섹션부터

5단계 — 예매 흐름
  - §4-5 시각적 마감(카드 스타일, 타이머 박스, 여백)
  - 로직/플로우는 불변 — 시각 변경만
  완료 기준: 기존 예매 흐름 테스트(좌석 선택, 타이머 등) 그린 유지
```

1단계는 다른 단계를 기다릴 필요 없이 지금 바로 착수 가능하다. 2단계(라이트/다크 기본 모드 확정)는 3~5단계의 시각 기준을 정하는 선행 조건이므로 순서를 지킨다.

---

## 7. 테스트/검증 계획

`tests/*.test.mjs`(Playwright 기반)에 이미 시각 요소를 검증하는 테스트가 존재한다. 확인된 예:

- `theme-mode.test.mjs`, `theme-bootstrap-core.test.mjs`, `open-calendar-theme.test.mjs` — `window.getComputedStyle()`로 실제 색상값(`backgroundColor`, `color`)을 읽어 대비율(contrast ratio)까지 계산·검증.
- `home-card-routes.test.mjs`, `home-more-links.test.mjs`, `search-home-visible-shows.test.mjs` — 홈 카드/링크 구조와 텍스트 셀렉터 검증.
- `login-redirect-auth-state.test.mjs`, `login-home-shortcut.test.mjs`, `mypage-account-actions.test.mjs` — 로그인/마이페이지 상태 전환 텍스트 검증.

이번 개선 작업 시 다음 두 종류가 구분되어야 한다.

1. **문구/셀렉터 기준 테스트**: DOM 구조나 텍스트가 바뀌면 갱신이 필요하다(예: 버튼 role/텍스트가 바뀌는 §2-3 수정, 히어로 슬라이드 구조가 바뀌는 3단계). 각 실행 단계 완료 기준에 "해당 단계가 건드리는 테스트 파일 목록을 먼저 특정하고 갱신"을 포함시킨다.
2. **색상/대비 기준 테스트**(`theme-mode.test.mjs` 계열): 하드코딩된 hex 비교가 아니라 대비율(WCAG AA 4.5:1) 계산 방식이다. 신규 색상 토큰을 추가하지 않기로 했으므로(§5-3) 이 계열 테스트가 이번 작업으로 깨질 가능성은 낮다 — 2단계의 라이트/다크 기본 모드 전환 자체가 기존 대비 계산에 영향을 주는지만 확인한다.

기존 `UI-개선-계획서.md`의 각 Phase(P1~P6)가 이미 특정 테스트 파일과의 계약(예: `booking-single-checkout-flow.test.mjs`의 "결제하기" link role 계약, `header-utility-label.test.mjs`의 `header > div` 구조 의존)을 상세히 문서화해 두었으므로, 이번 계획서의 3~5단계 구현 시 해당 문서의 관련 섹션을 참고해 동일한 실수(테스트가 의존하는 구조를 모르고 깨뜨리는 것)를 반복하지 않는다.

---

## 8. 미결정 사항 (운영자 확인 필요)

**상태: 운영자 확인 완료 (2026-08-14)**

1. **라이트/다크 기본 노출 모드 확정** → **결정: 라이트 모드를 앞으로의 디자인 QA·스크린샷 기준으로 삼는다.** CSS 실제 기본값과 일치하는 방향으로, 이번 조사가 전부 다크 모드로 캡처된 것은 브라우저/OS 설정 우연이었다고 보고 라이트 모드를 기준으로 재검증한다. (실사용자 다크/라이트 비율 자체는 별도 분석 과제로, 이번 결정과는 별개.)
2. **공연 상세 페이지 신규 섹션(§3-2)의 데이터 소싱** → **결정: 이번 라운드는 보류.** 캐스팅·공연장 안내·추천 섹션은 실데이터 없이는 만들지 않는다(플레이스홀더 금지 원칙). 데이터 소싱 방법이 정해지면 별도로 진행한다.
3. **관리자/판매자 콘솔 포함 여부** → **결정: 범위 밖 유지.** 계획서 원안대로 소비자 공개 페이지만 다룬다.
4. **Pretendard 폰트 파일(§2-4/§3-3)** → **결정: 이번 라운드는 보류.** 라이선스·바이너리 확보는 별도 사람 결정 사항으로 남긴다. CSS 쪽 배선(`globals.css`의 `@theme inline` 블록이 이미 `var(--font-pretendard, var(--font-noto))` 폴백 체인으로 준비되어 있음)은 안전하게 대기 상태이므로, 파일만 추가되면 `layout.tsx`에 `next/font/local` 호출 한 번 추가로 바로 적용 가능하다.

**참고**: 브랜드 컬러 변경, 포스터형 히어로, 프로모 카드 색상 다양화, 검색 우선 UX 도입은 "톤 유지" 전제와 맞지 않아 §3 "보류"로 이번 범위에서 제외했다 — 별도 미결정 사항으로 남기지 않는다.

**이번 결정이 §3/§4에 미치는 영향**: §3-2(공연상세 신규 섹션)와 §3-3(타이포 파이프라인)은 위 보류 결정에 따라 이번 라운드에서 구현하지 않는다. §3-1(콘텐츠 완성도 보강)과 §3-4(히어로 이미지 크롭 수정) 중 실데이터·신규 자산 없이 가능한 부분(레이아웃/여백/크롭 수정, 기존 실데이터 재활용)만 라이트 모드 기준으로 이번 라운드에 진행한다.

---

## 9. 참고 자료

### 조사한 사이트 요약

| 사이트 | 핵심 특징 |
|---|---|
| NOL 인터파크 (`nol.interpark.com/ticket`) | 화이트 테마, 풀블리드 사진 히어로+굵은 헤드라인 직접 얹힘, 프로모 카드마다 다른 배경색/처리 |
| YES24 티켓 (`ticket.yes24.com`) | 풀블리드 히어로 위 금속/포일 효과 대형 타이포(포스터/매거진 커버 방식), 히어로 아래 흰 배경 전환 |
| 멜론티켓 (`ticket.melon.com`) | 흰 배경 + 멜론 초록 강조색, 겹치는 포스터 캐러셀, 콘텐츠 애그리게이터형 다중 행 |
| 티켓링크 (`ticketlink.co.kr`) | 무드있는 인물 사진 히어로+거대 아웃라인 타이포, 히어로 위 플로팅 세로 프로모 카드 스택(색상별 다양) |
| 티켓마스터 (`ticketmaster.com`, 해외/보조) | 검색 우선 UX(히어로보다 먼저 위치/날짜/검색 유틸바), 실용적 2×2 사진 카드 그리드 |

### 관련 코드 위치

- `src/app/theme-vars.css` — 디자인 토큰(라이트/다크 색상, radius, shadow, 타이포 스케일)
- `src/app/globals.css` — Pretendard 폰트 TODO(1행), 다크모드 variant 정의
- `src/app/layout.tsx` — 폰트 로더(`Noto_Sans_KR`만 로드), 테마 부트스트랩 스크립트
- `src/app/page.tsx` — 홈 페이지
- `src/app/login/page.tsx` — 로그인 페이지
- `src/app/mypage/page.tsx` — 마이페이지
- `src/components/mypage/account-summary-panel.tsx` — §2-2 세션 확인 중 버그
- `src/components/mypage/identity-verification-panel.tsx` — §2-3 본인인증 모순 상태 버그
- `backend/api-router.js`, `backend/identity.js` — §2-1 "요청한 API가 없습니다." 에러 발생 지점
- `tests/theme-mode.test.mjs`, `tests/theme-bootstrap-core.test.mjs`, `tests/open-calendar-theme.test.mjs` — 색상/대비 검증 테스트
- `UI-개선-계획서.md` — 기존 UI 개선 계획서(모바일 내비, 예매 플로우 보안, 디자인 시스템 px/hex 정리, 접근성, 다크모드). 이번 계획서와 범위가 다르지만(이쪽은 버그 수정+콘텐츠 완성도, 저쪽은 구조/보안/접근성 부채), P3a(Pretendard 로드)와 P3b(px/hex 토큰 치환)는 §2-4, §5-3과 직접 겹치므로 구현 시 두 문서를 함께 참고해 중복 작업을 피한다.
- `NICE 본인인증 연동 계획서.md`, `휴대폰 본인인증.md` — 톤/형식 참고, 본인인증 백엔드 정책(이번 계획서에서 변경하지 않음)
